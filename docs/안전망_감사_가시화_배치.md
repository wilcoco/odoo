# 전 기능 안전망 감사 + 가시화 배치 (완료)

실행 2026-07-08 · 원칙: **"막는 가드"는 충실했으나 "건너뛰는 경로"가 로그에만 남던 것을 사용자 가시(채터/알림)로 각인.**

## 감사 결과 요약
- 가드 밀도: worksite 48 / planning 13 / provisional 5 / settlement 4 예외가드 + 모듈별 제약. 차단형 안전망(상태가드·동시성 직렬화·멱등·overlap·권한)은 기 검증(5차 시뮬·§8 적대점검).
- 발견: **조용한 실패 8건(S1~S8)** — 수요 누락·배정 탈락 등이 `_logger.warning`으로만.

## 수정·검증·커밋 (8건 중 7건 처리)
| # | 조용한 실패 | 처리 | 커밋 |
|---|---|---|---|
| S1 | Oracle 수요 품목 미매핑 → 행 누락 | 제외 품번·행수 채터 보고 | wilcoco `feat/planning-safety-nets` |
| S2 | 배정 '조합 없음' 탈락 | 경고 수집→요약 채터 | 〃 |
| S3 | capability 없음(풀캐퍼 미적용) | 〃 | 〃 |
| S4 | 다중 활성 BOM limit=1 임의선택 | 감지 시 경고(첫번째 사용 명시) | 〃 |
| S5 | 월마감 recon_ok=False 무알람 | danger·sticky 알림+대상 표기 | odoo_gh `feat/injection-actual-cost` 210af14 |
| S6 | 정단가 확정 소급전표 무통지 | 건수·합계 sticky 알림(+폼 이동) | `feat/provisional-pricing` bbb4eb1 |
| S7 | 백필 SILO 소비 생략 로그만 | 계획MO 채터 경고 | `feat/plc-weight-capture` 713af88 |
| S8 | POP 측정 파싱 except-pass | **보류**(표시용 데이터, 영향 하) | — |

동시 반영: **G1**(BOM 전개 비사출 제외 — is_injection_part/capability 판정, BOM 없는 비사출 수요 제외+경고) · **G3**(배정 납기순 정렬).

## 검증 (cams_sim3)
- 배정 라인 = 사출품만(외주 제외) / 요약 채터 "경고 2건: 다중 활성 BOM·비사출 수요 제외" 게시 ✅
- 월마감 불일치 행 → danger·sticky ✅ / 정단가 확정 → "소급전표 1건(합계 6,000) 초안" 알림+폼 이동 ✅

## 남은 원칙(팀 규칙 제안)
1. 결정은 코드(필드·제약·자동화)로 각인 — 문서/구두만이면 미결정으로 간주.
2. **건너뛰기(skip/continue)를 추가할 때는 반드시 가시 채널(채터/알림/이슈 목록) 병행** — `_logger.warning` 단독 금지.
