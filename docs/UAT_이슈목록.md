# UAT 이슈 목록 (발주없는매입 순서서 1차 · 2026-08-25 테스터 회신)

## 1. 수정 완료 (머지·배포 대기)

| # | 단계 | 증상 | 원인 | 수정 | 브랜치 |
|---|---|---|---|---|---|
| 1 | 계획 3 | 원재료 발주가 협력사 SCM 포털에 미노출 | 원재료 자동발주가 `auto_generated`/알림 없이 생성 — 포털 목록은 `auto_generated=True`만 표시 | 포털 협력사면 플래그+알림 | wilcoco `fix/material-po-portal` (d5b1be4, 미러 동기 완료) |
| 2 | 3 | 정상 최초 매입계산서가 **수정/마이너스분**으로 자동 처리되어 원본번호 강제 | `kr_is_correction`이 `amount_total_signed<0` 판정 — 오두 부호 규약상 **매입청구서는 항상 음수** → 전건 오판 | `amount_total` 기준 정정 | wilcoco `fix/uat-round1` (544be26) |
| 3 | 8 | (동일 원인) 마감 체크리스트 "음수 금액 청구서" 매입분 전건 오탐 | 위와 동일 도메인 | 집계·드릴다운 동일 정정 | 〃 |
| 4 | 6 | 실입고 0인데 **[납품 완료]** 처리 가능 | `action_mark_done` 무가드 | `qty_received` 합 0이면 차단(부분입고 허용) | 〃 |
| 5 | 5 | 사일로 800/5,000kg가 **1600%** | `fill_percent`는 이미 0~100인데 `widget="percentage"`(0~1 기대)가 ×100 | 위젯 제거(목록·폼) | odoo_gh `fix/uat-round1` (ae552a1) |
| 6 | 2 | 무계약 부품이 표준단가로 적립되는데 **경고 없음** | 폴백만 하고 통지 없음 | MO 채터에 폴백 품목·공급사·단가 일괄 경고 | 〃 |
| 7 | 4 | 소급 확정 후 화면 `Cannot read properties of undefined (reading 'map')` (서버 처리는 성공) | 확정 위저드가 반환하는 next 액션에 **`views` 키 누락** → 웹클라이언트 `action.views.map` 크래시 | `views: [[False,"form"]]` 명시 | 〃 |

**검증**: 신선 DB 설치 후 실데이터 확인 — 정상 매입분 `kr_is_correction=False`,
체크리스트 음수 0건, 입고0 완료처리 차단, 사일로 16.0(화면 16%).

## 2. 배포 대기분으로 이미 해결됨 (코드 존재, 서버 미배포)

| 단계 | 증상 | 해당 수정 |
|---|---|---|
| 5 | **QR 전표에 ASN의 LOT 미프리필** | `fix/scm-review-9` #5 (ASN 전표 짝맞추기 품목 기준) — 미배포 |
| 5 | 포털 입고수량 800kg 미표시·품목 수 0, 인수증 PDF 품목/수량/LOT/IQC **행 비어 있음** | 같은 뿌리: LOT·수량 프리필 실패로 **전표가 수량 0으로 확정** → 전표에 라인이 없어 포털 집계·PDF가 모두 빈 값. (로컬 재현 결과 정상 전표에서는 관리자·포털 경로 모두 정상 렌더 확인) |

→ **SCM-9 배포 후 5단계 재검 필요.** 배포 전에는 이 3건을 결함으로 카운트하지 않음.

## 3. 결함 아님 — 문서(기대치) 수정 대상

| 단계 | 테스터 지적 | 실제 동작 | 조치 |
|---|---|---|---|
| 7 | "사용실적 = 계산서 = 원장" 3중 일치가 성립하지 않음 (소급 300원·VAT 차이) | 정상. **사용실적=공급가 기준**, 계산서는 소급분 포함, 원장은 **VAT 포함 미지급금** | 확인 포인트를 3단 비교식으로 정정 (아래) |
| 8 | 과거일자(2023-12-31) 전표가 차단되지 않고 2026-08-31로 자동 변경 | 오두 표준 — 잠긴 기간에는 기표 불가하되 **전표 일자를 잠금 이후로 자동 조정**해 발행 | 기대치를 "차단"→"일자 자동 조정" 으로 정정 |
| 8 | 잠금일 후퇴 시 값 원복되나 **안내·이력 없음** | 표준이 write를 되돌려 실제 변경이 없으므로 이력도 없음(설계상 정상) | 다만 **무언(無言) 원복은 UX 결함** → 개선 과제로 이월 (아래 4항) |
| 1 | 3단 판정 규칙 원본 시트 미제공으로 미검증 | 자료 대기 | 담당자 자료 요청 목록에 유지 |

## 4. 이월 과제 (다음 라운드)

- **BOM 수량 0.5 → 1 반올림** (단계 1): 원인은 라인 단위(UoM)의 반올림 정밀도.
  `escon_uom_standard` 단위 점검이 위반을 잡아주지만, **입력 시점 차단/경고**가 없음.
  → BOM 라인 저장 시 "중량 원재료는 g 단위" 가드 추가 검토.
- **마감 잠금일 후퇴 시 무언 원복** → 명시적 안내 메시지 + 예외 절차 문구 노출.
- **로컬 개발환경 주의(개발 전용)**: `odoo_gh/odoo_plugins/` 아래에 `account_kr_guard`,
  `account_kr_reports`의 **git 미추적 복사본**이 있어 addons_path 우선순위상 wilcoco 정본을
  가립니다. 이번 검증 중 수정이 반영되지 않는 원인이었습니다(미러 동기 후 정상).
  서버 배포에는 영향 없으나 로컬 테스트 오판을 만들므로 **정리 권장**.
