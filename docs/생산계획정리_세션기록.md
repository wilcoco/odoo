# 생산계획 정리 세션 기록 (2026-08-14~18) — 타 세션 인계용

> 작성 세션: IATF/SQ 세션 (생산계획 정리 과업 수행). 이 문서만 읽으면 이어받을 수 있게 정리.

## 1. 브랜치·커밋 (전부 push, 미머지)

| 브랜치 | 내용 |
|---|---|
| `feat/planning-audit-fixes` (wilcoco, base=18.0) | 아래 전부 — 계획 정독 수정 3건 + 테스트수요/소스선택 + escon_uom_standard 신규 + UAT 문서 3종 |
| `feat/iatf-company-forms` @d84376d | PQC `_auto_feed_spc` 잠복결함 수정 (존재하지 않는 필드 참조 — 라인 있는 판정 즉사) |
| (참고) odoo_gh `fix/field-list-injection-flag`, `fix/occupancy-test-determinism` | 여전히 main 미머지 |

## 2. 코드 변경 요약 (feat/planning-audit-fixes)

1. **injection_planning 정독 감사 3건** (`d955420`):
   - UoM 미변환 → `_compute_quantity(round=False)` (BOM 전개+자재소요) — 시연 '마스터배치 0.00' 원인
   - 진행 중 계획 MO 잔량 미차감 → 순수요에 예정입고 가산 (연속 일계획 이중 생산 차단)
   - 원재료 BOM 가진 사출품 직접 수요 소실 → 자체 수요 등록
   - ※ '경제 생산량'은 풀캐퍼+안전재고 **정책**(결함 아님), BR은 조립 MO 전용(사출 계획과 레벨 분리)
   - ※ BOM 저장 자체가 소수 2자리 → **원단위는 g 등록 관례** (테스트에 명문화)
2. **테스트 수요 + 소스 선택** (`3e55143`): production_planning에 위저드(BOM 보유 완제품, source='test'로 원장 기록), planning.run에 demand_source_filter. 메뉴는 의존 방향상 injection_planning 쪽.
3. **escon_uom_standard 신규**: 단위 기준 정책 마스터(4영역 시드: 재고 kg/BOM 원단위 g/실측 g/수량 개) + 정합 점검(위반 드릴다운). "기준은 메뉴, 검증은 기계".
4. 회귀 테스트: injection_planning/tests/test_planning_audit.py (worksite 미설치 가드 포함). CI 그린.

## 3. UAT 준비물

- **DB `uat_plan1`** = cams 복제 (품목 77·BOM 26·capability 11). 중화됨: ERP 오라클 접속정보 삭제·크론 전체 비활성·메일 차단. 계정 `uat/uat1234!` (관리자 아님). 접속은 `?db=uat_plan1` URL 지정 (list_db=False라 선택화면 없음).
- **문서 3종** (docs/ + ~/Downloads docx): 생산계획_UAT_시나리오_매뉴얼(0~6단계+3-B 톤정산, 판정표, 함정 6), 생산계획_UAT_안내문, (타 세션 작성) 발주없는매입_UAT_순서서와 **자매편 정합** — 겹침(입고·IQC·사일로·톤정산)은 순서서 5단계 상호참조.
- **운영 서버 존재**: http://10.10.21.20/odoo (DB odoo18) — 품목 707·BOM 524 적재. 단 이 브랜치 신기능은 **미배포** — 머지+배포 후 환경 A 사용 가능.

## 4. 확정 실무 규칙 (메모리에도 저장됨: cams-purchase-settlement-rules)

- **원재료(수지) = 발주함** (PO→입고→IQC→사일로). 대금은 발주액이 아닌 **적재 실적(톤)×톤단가** 월 정산.
- **부품 = 무발주** (생산 사용실적 적립 정산, 가→정단가 소급). "발주 없는 매입"은 부품 정산만 지칭.

## 5. 미결·다음 작업

- feat/planning-audit-fixes 머지 요청 (PR 링크: github.com/wilcoco/odoo/pull/new/feat/planning-audit-fixes)
- 운영 서버(odoo18) 배포 후 UAT 환경 A 공지 → UAT 실시 (안내문 기한 채워 발송)
- odoo_gh 2브랜치(e943913 결정성·P1-11) 머지 재촉 필요
- UAT 이슈 대응: 코드 동결, 수정은 모아서 일괄 반영+재검
