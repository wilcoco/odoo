# 회사운영 3차 시뮬레이션 (적대적 관점) — 결과

실행: 2026-07-02, DB=cams_sim(1·2차 누적). 대상: **자동채점 신기능(feat/sq-evaluation 8247ace5) + IATF 배점 패치** 반영 후.
관점: **적대적** — 정상확인이 아니라 신기능을 깨뜨리고, 그 -u가 기존 흐름을 회귀시켰는지 공격.

## 반영 확인
- 자동채점: `action_auto_propose`(제안) / `action_apply_proposal`(확정, `not l.status` 가드) / `proposed_status·proposed_reason·auto_scored` 필드 — 반영됨.
- IATF 배점 패치: IATF 30조항 max_score 합 **300**(이전 0) — 반영됨.

## Phase A — 자동채점 신기능 공격 (전부 통과)
| 검증 | 결과 |
|---|---|
| A1 제안≠확정 분리 | ✅ 제안 45라인 생성, **미리 수기확정(우수)한 라인은 자동이 안 덮음** |
| A2 지표기반 제안 | ✅ 증빙0 항목 → "poor(0건)" 근거와 함께 미흡 제안 |
| A3 보강필요 식별 | ✅ **27건** 자동 식별(사장님 핵심 의도) |
| A4 재실행 멱등 | ✅ 라인 47 유지, 확정 보존 |
| A5 일괄확정→점수 재계산 | ✅ status 45 반영, 점수 30→438, 기존 우수 유지 |
| A6 IATF 점수 산출 | ✅ 배점패치 후 **100/300 등급 F** 산출(이전 0/0) |

## Phase B — 회귀 공격 (sq -u가 기존 흐름 깼나? — 무영향 확인)
| 검증 | 결과 |
|---|---|
| B1 생산량 정산 | ✅ 원재료(settle_by_ton) 제외 + 계약단가(333) 적용 유효 |
| B2 대시보드 KPI | ✅ 완제품 1000(모델기반)·NC·채무 정상 |
| B3 톤정산 멱등 | ✅ 중복 매입계산서 차단 유효 |

→ 자동채점/IATF 패치 `-u`가 정산·대시보드·super()체인에 **회귀 없음**.

## Phase C — 오용 공격
| 공격 | 결과 |
|---|---|
| C1 빈 평가서(라인 미로드)에 자동채점 | ✅ 크래시 없음 |
| C2 일괄확정 2회 | ✅ 멱등(확정 상태 불변) |
| **C3 진행/확정 평가서에서 [기준 로드] 재클릭** | ⚠️ **결함** — 아래 |

## 발견 결함 (1건 — IATF/SQ 세션 인계)
**C3 · `action_load_criteria` 무조건 `line_ids.unlink()` → 심사 판정 유실**
- 재현: 평가서 라인 로드→이행상태 판정(45건 확정)→[기준 로드] 재클릭 시 **확정 45건이 전부 unlink→재생성**되어 심사 결과가 날아감.
- 위치: `sq_evaluation.py action_load_criteria` (state 무관 unlink, 이후 state='in_progress').
- 심각도: 중(데이터 유실). 심사 진행 중 실수 클릭 한 번에 판정 손실.
- 권장 수정(그 세션): ① `state=='draft'` 에서만 재로드 허용(아니면 UserError) 또는 ② 기존 status 보존한 채 신규 기준만 추가/삭제(merge), 또는 ③ 재로드 전 확인 위저드.

## 결론
- **자동채점 신기능 견고**: 제안≠확정 분리·보강식별·멱등·IATF 점수산출 전부 적대적 검증 통과. 명세 §5 기준 충족.
- **기존 흐름 회귀 없음**.
- **잔여 결함 C3 1건**(load_criteria 데이터 유실 위험) → feat/sq-evaluation 세션에 인계.
