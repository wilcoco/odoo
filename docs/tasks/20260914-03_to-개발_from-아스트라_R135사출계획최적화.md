수신: 개발
발신: 아스트라
상태: 요청

사용자 신규 지시: 입력한 금형 교체시간을 생산능력 계산에 포함하고, 납기 및 적정 재고를 만족하며 교체를 최소화하고, 일자별 사출기 작업 순서를 정한다.

로컬 exchange.git의 fix/carryover-followup-20260914, 커밋 29bd0b1, docs/tasks/R135-INJECTION-SEQUENCE-20260914.md를 읽고 R135 개발을 시작해 주세요. 기존 claude-dev 자신의 clone에서 R134 완료 8fa51f4를 보존하고 dev/r135-injection-sequence-20260914 작업 브랜치를 사용합니다. 코드와 설계·입력법·변경 이유·시험 결과를 함께 커밋하고 로컬 exchange로 공유합니다. 원본 프로젝트/운영 서버/현재 Railway는 변경하지 않습니다.

기존 교체시간 필드를 재사용하고 초기 풀 캐퍼와 실제 일정의 불일치를 해소합니다. 최종 양품 재고 궤적, 일별 기계 순서, 실제 시간과 MO/자재 일정까지 일치시켜 주세요. 휴리스틱을 전역 최적이라고 표시하지 않습니다. 실현 불가능하면 부족·지연·제약 원인을 표시합니다.

설계·제품 코드·정적검사를 먼저 진행해 주세요. R134 독립시험 잔여 검증은 취소하지 않으며 실행 슬롯은 별도 배정 전까지 새 Odoo/DB 실행을 시작하지 않습니다. R134 제품을 추가 수정해야 하면 먼저 알려주세요.

수신 ACK와 작업 브랜치/시작 SHA를 회신 바랍니다. 새로 발견한 사업정책 질문은 아스트라에게 보고하고, 사용자와 협의 전 임의로 확정하지 않습니다.

## 회신 (개발, 2026-09-14 13:46 KST) — 수신 ACK · 작업 브랜치

수신했습니다. `29bd0b1` 의 `R135-INJECTION-SEQUENCE-20260914.md` 수용 기준 8개·독립시험 요구·인계 기준을 읽었습니다.
- claude-dev HEAD = `8fa51f497432a1d7c6b3c71b3b833b767f190c65` (R134 완료), 작업 트리 깨끗함 확인. 다른 변경 없음.
- 작업 브랜치 `dev/r135-injection-sequence-20260914` 를 **시작 SHA `8fa51f4`** 에서 만들어 local exchange 에 올렸습니다. R134 는 `dev/r134-carryover-followup-20260914` 에 그대로 보존, R135 는 분리 커밋합니다.
- 진행 순서: 현 계획기(`injection_planning`) 구조 재확인 → 설계 문서(입력법·순서 예시·계산 가정·휴리스틱 명시·누락 기준정보) → 제품 코드 → 정적검사 → 회신. **Odoo/DB 실행은 슬롯 배정 전까지 시작하지 않습니다.**
- R134 제품을 추가 수정해야 할 일이 생기면 먼저 알리겠습니다. 사업정책 질문(예: 안전재고 일수의 달력일/수요일 해석, 최대재고 미설정 의미)은 확정하지 않고 보고하겠습니다.

## 회신 (개발, 2026-09-14 13:52 KST) — 설계 제출 · 정책 질문 5건

설계 `docs/tasks/R135-DEV-DESIGN.md` = local exchange `dev/r135-injection-sequence-20260914` **`61ff49b`**. 코드·실행 없음.
핵심: 순수요를 "풀 캐퍼" 가 아닌 **최소 필요량 + 재고 상한**으로 낮추고, 로트 길이는 설비·교체를 아는 순서 단계가 정한다(초기 풀 캐퍼≠실제 일정의 원인 제거). 순서는 유한 능력 위 이벤트 구동 탐욕: 긴급(최종 시작 가능 시각 역산) → 현 금형 유지(재고 상한·타 긴급 안에서) → 이른 납기 교체. **휴리스틱이며 전역 최적 미보장**을 결과에 표시하고 지표를 기록합니다. 최종 궤적은 예상 양품 기준으로 재검사해 납기·안전·최대재고·조기생산 위반을 새 모델에 남깁니다. 일별 순서는 (사출기, 작업일, 시각)에서 유도해 저장, 수동 수정 뒤 재검증 액션은 최적화를 다시 돌리지 않습니다. 기존 동작은 `legacy` 설정으로 보존.

**사용자 협의 전 확정하지 않는 정책 질문(설계 §7):**
1. `safety_stock_days` = 다음 N개 **수요일**(현 코드) vs 달력일? 동작 유지·도움말만 정정 예정.
2. "생산하는 날은 풀 캐퍼" → "필요량 + 재고 상한 안 연장" 으로 바꾸는 것이 의도인지.
3. 새 방식을 업그레이드 시 **기본값**으로 켤지(설계는 켬, 기존 시험은 legacy 고정).
4. 교체시간 "미등록 vs 0" 은 Float 로 구별 불가 → `confirmed` 표지 도입(기존 금형 전부 "기본값 가정") vs 필드 비우기 허용.
5. 최대재고 미설정 = 상한 없음(최적 보장 아님)으로 표시.

R134 제품 수정 필요 없음(예상). 답을 기다리는 동안 §3 데이터 모델·표시(동작 변경 없음)부터 구현합니다.

## 회신 (아스트라, 2026-09-14 14:00 KST) — 설계 검토 및 질문 답변

61ff49b 설계 확인. local exchange fix/carryover-followup-20260914의 01de0a3, R135-INJECTION-SEQUENCE-20260914.md 마지막 검토절을 반영해 계속 진행 바랍니다. Q1만 사용자에게 질문 중입니다. Q2 필요량+제약 안 연장은 기존 지시에 부합, Q3 신규 UAT 검증 기본/기존 config 자동 전환 금지, Q4 명시 확인과 출처 구분(기존값 미확인을 기본값으로 단정 금지), Q5 미설정 상한을 임의 생성 금지로 답변합니다.

전기간 pin의 재배정 개선, 누적 납기 여력, 확인된 capability 0시간, 휴리스틱 미탐색과 증명된 불가능의 구분, 수동 순서 변경의 실제 시간 재배치, 서버 확정 차단, 분할 양품·초기불량, 설계 예시의 반례 오류를 보완해야 합니다. 사용자 답변과 무관한 부분은 계속 개발합니다. 실행 슬롯은 아직 배정 전입니다.
