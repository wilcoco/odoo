# 후속 PR #10 회신 — 스크립트 검토 반영 (2026-09-18, 저자: Claude 개발)

수신: 서버 담당(원도영) · 참조: 홍정수
회신 대상: `20260918-01_to-개발_from-서버담당_R145_운영실측_스크립트검토.md`

## 1. 후속 PR
- https://github.com/escon-odoo/odoo_gh/pull/10 (`release/r145-fix-20260918`, base = 현재 미러 main ce759f5). 머지 후의 40자리 SHA 가 리허설·배포 기준입니다. flushing/20260918 의 로컬 커밋 3개는 섞지 않았습니다.
- 정본 반영: wilcoco/odoo-uat main 29588d0 (같은 스크립트·절차서).

## 2. 지적별 처리
| 지적 | 처리 |
|---|---|
| 5.1 [P1] pg_restore `-` | core·rollback·rehearse 와 절차서 §6.2/§8.2 에서 파일 인자 제거, 표준입력만 사용 |
| 5.2 [P1] 복제본 권한 | config 640·소유 100:101, 코드 a+rX, 파일스토어 소유 100:101. 기동 전 `--user 100:101` 로 config·매니페스트·파일스토어 읽기/쓰기 사전 검증(실패 시 중단) |
| 5.3 [P1] 격리 | 실행별 전용 폴더·네트워크·컨테이너 이름(`<ts>`), `/db/odoo-root` 아래 거부, 이름 충돌 거부, `--internal` 생성 후 검증, 복원 직후 중립화(cron·메일서버 off·대기 메일 취소·base url), 화면용 Odoo 는 127.0.0.1 바인드·cron 0. 7.4 장비 확인은 필요한 cron 만 화면에서 켜서 단계적으로 |
| 5.4 [P2] 범위·판정 | 리허설 3단계 스크립트(rehearse → rehearse_rollback → replica_cleanup). core: DB latest_version = 매니페스트 version 비교, 재기동 후 로그 이상은 종료 코드 3(자동 복원 없음). rollback: 이전 버전 기록과 diff 로 판정, 증적은 폴더 밖으로 복사 후 제거 |
| 5.5 [P2] 입수·증적 | 스크립트는 `/db/release-tools` 별도 clone(README·절차서 §4.5). 매핑 injection_planning 트리 3ca8d3ab… 채움. 절차서 상단 통합 기준 ce759f5 로 정리 |

검증은 가짜 docker 로 정상·업그레이드 오류·버전 불일치·재기동 후 로그 이상·롤백 통과/실패 6경로를 확인한 것이고, 실제 컨테이너 첫 검증은 리허설입니다. 리허설에서 스크립트 문제가 나오면 이 큐로 보내 주시면 바로 고칩니다.

## 3. 범위 변경(축소) — cams_sq_dashboard 제외
새 DB 설치 검증에서 `Couldn't load module cams_sq_dashboard`: 정본·미러에 없는 `iatf_audit.models.sq_readiness`·`iatf_nonconformity.models.closure_guard` 를 import 합니다(격리 브랜치 전용). 운영 `-i` 에서 같은 실패가 나므로 PR #10 에서 모듈을 삭제했고 신규 설치는 `mrp_bom_scan_guard,escon_br_intake` 2개입니다. `release.modules` 도 그에 맞습니다.

## 4. 실측 회신 반영
- PostgreSQL 17.6 / postgres:17, Odoo 18.0-20251008, 컨테이너 사용자 100:101 → 절차서 §2. 복제본은 같은 판으로.
- 사출품 BOM: 현재 15건(활성 6·비활성 9)을 기준으로 게이트 1 완료. 18건은 ID 목록이 없는 구두 집계라 대조하지 않습니다.
- "BOM 변경:" 변경요청 24건 draft: 다음 묶음(확정 BOM 보호)에서 그 자동 생성 훅을 제거하며, 24건은 데이터로만 남깁니다. 운영 반영 때 별도 정리 지시를 드리겠습니다.
- Odoo 밖 구성요소: go_relay_plc·go_server 만 필요하다는 판단 접수. 둘을 정본에 편입하고 UAT(Railway)에 서비스로 올리는 것은 별도 항목으로 잡았습니다(개발 백로그). go_server 가 실행 중이 아닌데 필요한 이유(조립 앱 이종검사 통신)는 다음 큐에서 확인 요청드리겠습니다.

## 5. 다음 순서
PR #10 검토·머지 → 최종 SHA 회신 → 중립화 덤프 1벌 전달(개발 격리 VM 사전 시험, 절차서 §7.5) → 복제본 리허설 3단계 → 결과·증적 회신 → 운영 반영 일정 합의.
