수신: 전체
발신: 아스트라
상태: 요청

# BR 53ea739 후보 접수·독립검증 및 잔여 범위

개발 후보53ea739(제품6825eef)의 BR원장/PLC 회사팀 수정 회신을 접수했고 사용자에게 알렸습니다. 자체206 tests/오류0/skip2는 전체7일 완료가 아닙니다.

- 테스트: 무발주 주간 E2E를 계속하면서 해당 후보를 별도DB에서 독립 검증합니다. source simulation, 중복·개정·취소, MO중복생성, 도착미기록/120분초과, 다회사 PLC를 확인하세요. 선택모듈미설치skip2는 정산모듈을 설치한 합본에서 재시험합니다.
- 개발: 불량 실물완료/원재료소비/격리/처분을 계속합니다. BR→납품 전표→매출근거 자동연결, 개정/취소 때 이미 생성된MO/납품의 잔존수량 처리, 부족경보·seed20260930 조립3%·공정별시간은 계속 미완료 항목으로 관리하고 후속 구현하세요. 구개정 수요만 막고 기존MO를 남겨 중복생산하지 않는지 실제 반례를 작성합니다.
- 차단경보30분은 개발의 합성 기본안이고 회사확정정책이 아닙니다. 사용자확정120분납기와 구분해 문서·설정에 표시합니다.
- 품질 후보fc61e235에는 선행검사14건중 사출PLC1ERROR가 있으므로 이번 회사팀 수정과 합쳐 같은 실패가 없어지는지 확인합니다. 이를 전체 품질기능 완료로 확대하지 않습니다.
- 아스트라 정산 install실측29개중7ERROR는 새 대사선행절차 미셋업으로 막힌 기존청구서시험이며, 정상 대사/별도승인자를 준비해 재시험 중입니다. 아직 정산후보 통과/반입 전입니다.

핵심수정 후 SQ 별도관리항목 목록(09번)도 이어갑니다. 이번 접수에 답하기 위해 실행을 중단할 필요는 없으며 새 실행근거/실패/후속계약이 생길 때 회신하세요.


## 회신 (Claude 개발, 2026-09-11 17:05 KST) — 잔여 3건 처리, 후보 `ff6a1d8`

### ① ⚠ 품질 후보 `fc61e23` 의 사출 ERROR — **제 수정으로 해소됩니다**

지시하신 확인을 했습니다. 그쪽이 남긴 진단이 제 수정과 **정확히 같은 자리**였습니다.

> `"error_cause": "Existing injection_worksite/plc_weight_sync.py selects another
> company quality team globally; no product fix within assigned scope."`
> 실패 시험: `test_actual_injection_unit_requires_process_quality_and_preserves_late_plc_gate`

| | 그쪽 단독 `fc61e23` | 제 수정과 합본 |
|---|---|---|
| 선행검사 14건 | 13 PASS / **1 ERROR** | **14 PASS / 0 ERROR / skip 0** |

```
검증 브랜치 : scratch/qp-verify = dev/br-assembly-e2e(f28def5) + fc61e23  (exchange 에 push 함)
실행 SHA    : f8effe5 / DB cams_night_dev_main
명령        : dev update --modules iatf_quality_precedence,injection_worksite \
                --init-modules iatf_quality_precedence --test-tags '/iatf_quality_precedence'
로그        : artifacts/qp_verify_f8effe5.log
```

**확대하지 않습니다** — 확인된 것은 그 14건이 합본에서 통과한다는 것뿐이고
「전체 품질기능 완료」가 아닙니다. `scratch/qp-verify` 는 **검증 전용 가지**이며 제 후보에
품질 모듈을 반입하지 않았습니다. 통합은 아스트라 몫으로 둡니다.

### ② 구개정 중복생산 반례 — 작성했습니다

「구개정 수요만 막고 기존MO를 남겨 중복생산하지 않는지 실제 반례를 작성합니다」

- 개정이 와도 **이전 개정의 제조오더는 남습니다.** 실물이 이미 만들어지고 있을 수
  있고, 무엇을 취소할지는 업무 결정이라 **자동으로 건드리지 않습니다.**
- 그 상태에서 새 개정이 또 오더를 내면 같은 BR 로 두 번 생산하므로 **막고 사람에게
  넘깁니다**(살아 있는 형제 오더와 상태를 메시지에 적습니다).
- 잔존수량을 처분(취소·완료·수량조정)하면 새 개정이 진행되고, 새 수량이 반영됩니다.
- **취소도 같습니다** — 기존 오더를 자동 취소하지 않고 "잔존수량 처분 필요"만 알립니다.

시험 2건 추가: `test_a_revision_does_not_silently_duplicate_production`(세 단계 모두 확인),
`test_a_cancellation_leaves_the_existing_order_for_disposition`.
같은 BR 로 제조오더가 **하나만** 생기는 것을 수치로 겁니다.

### ③ 차단 경보 30분 표시 — 고쳤습니다

말씀대로 그건 제 잠정 기본안이지 회사 확정 정책이 아닙니다. 필드 라벨을
**「이종검사 차단 경보 한도 (분) [잠정]」** 으로 바꾸고 도움말에 명시했습니다.

> ⚠ 기본값 30분은 **개발이 제시한 잠정 기본안**이며 회사 확정 정책이 아니다.
> 품질·생산 담당이 현장 기준으로 확정해야 한다.
> ⚠ 사용자 확정값인 BR 납기 120분(수신→고객 도착)과는 **다른 값**이다.

### 실행 기록

```
후보 SHA : ff6a1d8 (제품 f28def5) / DB cams_night_dev_main
명령     : dev update --modules escon_br_intake,mrp_bom_scan_guard \
             --test-tags '/escon_br_intake,/mrp_bom_scan_guard'
결과     : 0 failed, 0 error(s) of 27 tests, skip 0   (BR 13 + 이종검사 14)
로그     : artifacts/br_dup_f28def5.log
```

### 계속 미완료로 관리하는 것

지시하신 대로 목록으로 들고 갑니다 — **하나도 완료로 적지 않았습니다.**

- BR → 납품 전표 → **매출 근거 자동연결** (지금은 `picking_id` 자리만 있음)
- **전 구간 공정별 시간기록** (지금은 수신·도착 **두 점**뿐)
- **부족 경보**
- **seed `20260930` / 조립 불량 3%** 재현

다음은 **「불량 실물완료/원재료소비/격리/처분」** 을 계속합니다.
**운영 가능 판정이 아닙니다.** 원본·운영·Railway 미변경, 로컬 exchange 에만 push.


## 회신 (테스트, 2026-09-11 17시대) — B 로직검증 완료 + BR 53ea739 접수 + ⚠pg OOM 인프라 블로커

**B 무발주 주간 E2E (테스트 소유)**: 하네스 구축 완료(`noorder_receive`=PO 없는 입고→검증된 `receive()` IQC+release 재사용, 제품코드 무변경, 코드 `0cf3961`).
- 검증됨: 무발주 입고 3건(clip220/bracket110/label55, **PO 없음·입고 무분개**, IQC 처리) + **첫날 09-24 전 구간**(5조립·정산 component accrual15·수지PO유지·repeat차단·balances).
- **관찰(-07 IQC 병목)**: 현행 MES 양품 스캔이 IQC 승인 LOT 만 허용 → 무발주 입고분도 전건 IQC 거쳐야 사용가능(IQC 생략 시 조립 `Material shortage` 차단 실측). 자동합격 안 함.
- 근거 exchange `test/company-week-br-defects` `19f2e25` §B, 로그 `artifacts/noorder_week_run5/6.log`.

**⚠ pg OOM 인프라 블로커(로직 아님)**: 2~7일 진행 중 `psycopg2 server closed the connection` 반복. 원인 = **pg 컨테이너 `OOMKilled=true`**(데이터 2G tmpfs/RAM, 다수 DB+149모듈 무거운 실행이 메모리 초과). 6회 재개 모두 하루 전후 연결 끊김. → **완주(B) 와 BR 후보 별도DB 검증 모두 메모리 여유 확보 후 가능.** per-step save 라 재개는 됨.

**BR 53ea739 / ff6a1d8 접수**: 독립검증 대상으로 받았습니다(source simulation·중복/개정/취소·MO중복생성·도착미기록/120분초과·다회사 PLC, 정산모듈 합본에서 skip2 재시험). **메모리 확보되면 별도DB에서 즉시 검증**하겠습니다.

**요청**: pg 메모리 여유(불필요 격리 DB 정리/컨테이너 한도 상향)를 총괄이 판단해 주시면 B 완주 + BR 독립검증을 바로 진행합니다. 문서화된 DB(company_week/test_main)는 드롭 가능하나 제가 단독 드롭하지 않고 확인받겠습니다.


## 회신 (Claude 개발, 2026-09-11 17:15 KST) — pg OOM 사실관계 (공통 실행환경은 제 소유가 아니라 보고만)

테스트 세션의 OOM 보고를 보고 **제 쪽이 그 메모리를 먹고 있는지** 확인했습니다.
결론부터: **아닙니다. 컨테이너가 역할별로 분리돼 있습니다.**

```
cams-night-20260910-dev-pg          Mem=768MB  OOMKilled=false   ← 제 것
cams-night-20260910-test-pg         Mem=768MB  OOMKilled=true    ← 테스트
cams-night-20260910-root-pg         Mem=1536MB OOMKilled=true
cams-night-20260910-settlement-pg   Mem=768MB  OOMKilled=true
cams-night-20260910-materials-pg    Mem=768MB  OOMKilled=true
cams-night-20260910-tax-pg          Mem=768MB  OOMKilled=false
```

각 역할 네트워크 안에서 pg 가 **모두 `test-pg` 라는 alias** 로 잡혀서
(`night_runtime.py:85` 의 `--network-alias test-pg`) 한 덩어리처럼 보이지만,
**컨테이너는 별개**입니다. 제 odoo 도 `--db_host=test-pg` 로 붙지만 실제로는 dev-pg 입니다.

### ⚠ 그래서 「불필요 격리 DB 정리」는 이 문제를 풀지 못합니다

- 제 `dev-pg` 에는 DB 가 **`cams_night_dev_main` 119MB 하나뿐**입니다.
  이걸 드롭해도 **테스트 세션의 test-pg 메모리는 1바이트도 늘지 않습니다.**
- 다른 역할 컨테이너의 DB 를 지워도 마찬가지입니다. **메모리는 컨테이너별 한도**입니다.

### 실제 지렛대

`tools/night_runtime.py:85` 의 pg 기동 인자입니다.

```python
'--network', network, '--network-alias', 'test-pg', '--memory', '768m', '--cpus', '1',
```

- 768MB 는 149모듈 + 7일 누적 실행에 좁습니다. root 는 1536MB 로 이미 올렸는데도
  `OOMKilled=true` 입니다 — **한도를 올리는 것만으로 충분한지도 확인이 필요**합니다
  (7일 실행의 실제 사용량을 재 보는 편이 낫겠습니다).
- **이 파일은 공통 실행환경이라 `AGENTS.md` L19 상 아스트라 소유입니다. 제가 고치지
  않았습니다.** 바꾸시면 6개 역할 컨테이너를 모두 재기동해야 하고, 재기동은 기존
  컨테이너 restart 금지 규칙과 맞물리니 총괄 판단이 맞습니다.

### 제 쪽 상태

제 실행은 **영향 없이 계속 가능**합니다(dev-pg OOM 없음). BR·이종검사·PLC 후보
시험은 그대로 돌아갑니다. 다만 **테스트 세션의 독립검증이 막혀 있으므로 제 후보는
아직 독립 인수 전**이라는 점은 그대로입니다.

필요하시면 제 `cams_night_dev_main` 은 언제든 드롭해도 됩니다(재생성 가능).
다만 위 이유로 **그게 도움이 되지는 않습니다.**
