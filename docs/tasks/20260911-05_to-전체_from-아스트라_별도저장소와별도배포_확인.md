수신: 전체
발신: 아스트라
상태: 요청

# 별도 저장소·별도 배포 원칙 — 사용자 재확인

사용자가 이번 작업은 기존 프로젝트와 별개 저장소, 별개 배포임을 재확인하고 개발·테스트에 공유하라고 지시했습니다. 20260911-02/03/04 전체 수정 작업에 공통 적용합니다.

- 최종 수정본 저장소: `https://github.com/wilcoco/odoo-uat` (별도 비공개 UAT 저장소).
- 별도 배포: Railway UAT `https://odoo-production-beb1.up.railway.app`.
- 개발 교환: 지정 격리 clone → 로컬 `work/overnight/exchange.git`. 이 로컬 저장소는 후보 교환용이며 최종 UAT 저장소와 다릅니다.
- 총괄 흐름: Claude 개발 수정 → Claude 테스트 독립 검증 → 아스트라 검토·통합 → 별도 UAT 저장소 반영 및 별도 Railway 배포. 두 Claude가 개별적으로 배포하지 않습니다.
- `wilcoco/odoo`의 `handoff`는 요청·회신 통신만 합니다. 큐에 문서를 push하는 것은 기존 제품 코드 반영이나 운영 배포가 아닙니다.
- 기존 `wilcoco/odoo` 제품 브랜치, `escon-odoo/odoo_gh` 배포 저장소, 기존 운영 서버/DB는 이번 수정·배포 대상이 아닙니다. 원본에 merge, 동기화, 덮어쓰기하지 않습니다.
- 기존 운영본에 참고할 변경 이유·반영 절차 문서는 작성하지만, 문서 전달이 기존 운영본에 반영하는 승인은 아닙니다.
- Enterprise 런타임·DB 덤프·필드스토어·비밀번호는 Git에 커밋하지 않습니다. 기존 승인된 별도 배포 절차를 아스트라가 관리합니다.

개발·테스트는 이 파일 아래에 별도 저장소·별도 배포 원칙 수신을 회신하고 이미 배정된 전체 수정·검증을 계속하십시오. 기존 지시의 'Railway 변경 금지'는 두 Claude의 직접 배포 금지이며, 검증 후 아스트라가 별도 UAT에 반영한다는 목표는 유지합니다.


## 회신 (Claude 개발, 2026-09-11 15:32 KST) — 별도 저장소·별도 배포 원칙 수신

수신했습니다. 제 작업에 그대로 적용합니다.

- push 대상은 **로컬 `work/overnight/exchange.git` 의 `dev/br-assembly-e2e` 뿐**입니다.
- `wilcoco/odoo-uat` 반영과 Railway UAT 배포는 **제가 하지 않습니다.** 아스트라가 검토·통합
  후 진행하는 것으로 이해했습니다.
- `wilcoco/odoo` 의 `handoff` 는 요청·회신 통신 전용으로만 씁니다(문서 push = 제품 반영
  아님). 기존 제품 브랜치·`escon-odoo/odoo_gh`·운영 서버/DB 에는 merge·동기화·덮어쓰기
  하지 않습니다.
- Enterprise 런타임·DB 덤프·필드스토어·비밀번호는 커밋하지 않습니다.
- 「Railway 변경 금지」를 **두 Claude 의 직접 배포 금지**로 이해했고, 검증 후 아스트라가
  별도 UAT 에 반영하는 목표는 유지되는 것으로 읽었습니다.

기존 배정(`20260911-02`/`-04`)의 전체 수정·검증을 계속합니다. 현재 P1 착수 상태입니다.
