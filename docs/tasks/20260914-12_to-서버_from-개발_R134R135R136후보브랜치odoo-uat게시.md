수신: 서버(원도영)
발신: 개발(지휘)
상태: 요청

## 무엇
사용자 지시(2026-09-14)로 야간 격리 개발 결과 3건을 **운영 비교·이식용으로 `wilcoco/odoo-uat` 에 새 브랜치로 게시**했습니다. `main`(`751b6e6`) 은 손대지 않았고 PR 도 열지 않았습니다. Railway 는 main 만 배포하므로 영향 없음(확인 부탁).

| 브랜치 | SHA | 내용 | 변경 규모(vs main) | 검증 상태 |
|---|---|---|---|---|
| `cand/r134-carryover-20260914` | `2dde5ea` | `gh_vendor_settlement` 미결 이월 후속(원 미결→후속 가감→청구 연결, 초안/승인/전기 구분, 자동 종결 없음) | 25 files +3175/−44 | 합성 DB 개발 21/0/0·독립 21/0/0·동시성 probe 통과 |
| `cand/r135-sequencing-20260914` | `1538fa7` | `injection_planning` 교체 인지 순서(setup_aware)·안전재고·R141 — 기존 DB 는 `legacy` 유지, 신규 설치만 setup_aware | 30 files +3764/−129 | 합성 DB 개발 237/0/0·독립 19/0/0·probe 8/8·업그레이드 보존 7/7 |
| `cand/r136-factory-flow-20260914` | `fce625c` | `cams_ops_dashboard` 전사 흐름 서버 어댑터 `cams.factory.flow` + canonical 지도 235노드 | 9 files +11847 | **시험 미실행, 화면 미구현** — 이식 대상 아님, 참고용 |

## 어떻게 보나
- 각 브랜치는 main 위 **커밋 1개**(모듈 디렉터리를 격리 상태로 통째 교체) → `git diff main..cand/r134-carryover-20260914 -- addons/gh_vendor_settlement` 로 바로 비교됩니다.
- 격리 스냅샷(`a141ddb`, 09-10) 은 GitHub 이력과 무관해서, 디렉터리 교체엔 **해당 작업 외 야간 변경도 섞여** 있을 수 있습니다. 작업 범위는 각 브랜치의 `docs/tasks/R134-CHANGE-LEDGER.md` / `R135-CHANGE-LEDGER.md` 가 정본이니 그 파일 목록과 diff 를 대조해 주십시오. 스냅샷 기준과 main 사이 다른 모듈 drift 는 248 files(supplier_portal_purchase 35, injection_planning 23, iatf_incoming_inspection 21, injection_worksite 19, gh_vendor_settlement 19 …).
- 미검증(정직히): 실제 화면 사용, 운영 복제 DB 마이그레이션/복구, 실물 기준정보. 합성 DB 통과만.
- 운영 이식·배포는 **사용자 승인(`승인:` 줄)** 뒤에만. 이 게시는 "보게 하는 것" 까지입니다.

## 요청
1. 이 브랜치가 보이는지, 운영 서버 기준(odoo_gh 미러 / odoo18 DB) 과 비교에 부족한 정보가 무엇인지 회신.
2. 운영 DB 에서 `injection_planning` 의 `sequencing_mode` 가 upgrade 뒤 `legacy` 로 남는지 검토 필요(우리 시험은 합성 DB).

수신 ACK 를 남겨 주십시오.
