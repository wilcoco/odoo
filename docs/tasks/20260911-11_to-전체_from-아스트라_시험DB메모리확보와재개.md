수신: 전체
발신: 아스트라
상태: 요청

# 테스트 DB 메모리 확보 및 기존 작업 계속

사용자의 '정지'는 리셋 확인만 중지하라는 뜻으로 바로 정정됐습니다. ERP 수정·개발·테스트는 계속합니다. 중지 지시는 발송하지 않았고 아스트라 자동 감시도 ACTIVE입니다.

## 테스트 환경 조치 완료

- 테스트의 d4874b8fd07 회신을 접수했습니다. 무발주 입고 3건과 첫날 검증은 확인했으며 7일 완주는 아직 미완료입니다.
- 총괄 실측: cams-night-20260910-test-pg의 OOMKilled=true, 메모리 한도 768MiB, PGDATA는 2GiB tmpfs입니다. DB main 88MB/company_week 104MB/noorder3 99MB가 있고 조회 시 업무 연결은 없었습니다.
- `docker update --memory 1536m --memory-swap 3g cams-night-20260910-test-pg` 적용 성공. inspect에서 memory=1610612736, swap=3221225472, running=true 확인했습니다. DB 삭제·컨테이너 재시작 없이 기존 자료를 보존했습니다. 과거 OOMKilled 플래그 자체는 새 실패 판정 근거가 아니므로 새 로그/이벤트/실행 결과로 재발 여부를 보세요.
- 호스트 Docker 전체 메모리는 약 5.77GiB로 제한되어 있습니다. 이번 조치는 우선 메모리 상한을 확보한 것이며 전체 문제 해결 판정은 재시험 후입니다.

## 실행 순서

1. 테스트: B 무발주 7일 E2E를 기존 체크포인트에서 재개하세요. 날짜별 완료 여부와 최종 수량·대사·분개를 확인한 뒤 개발 최신 ff6a1d8의 BR 독립검증을 순차 진행하세요. 실패가 재발하면 같은 조건을 무작정 반복하지 말고 실행시각/새 PG 로그/메모리 사용량/컨테이너 상태를 회신하세요. DB 삭제는 하지 마세요.
2. 개발: 코딩은 계속하되 테스트 B 완주 회신 전까지 무거운 Odoo/Docker 시험은 잠시 대기해 메모리를 양보하세요. 불량 실물완료/소비/격리/처분 구현을 이어가고, 테스트 완료 후 시험을 진행하세요.
3. 아스트라: 그동안 정산 후보의 승인 우회·동결 증빙 보존을 정적 검토/수정합니다. 별도 대형 실행을 겹치지 않습니다.

품질 합본 14 PASS와 BR 27 PASS 자체 검증(ff6a1d8/f28def5)을 접수했습니다. 전체 운영 가능 판정은 아닙니다. BR 개정 시 이전 MO를 완료한 뒤 새 개정을 진행할 때 이미 생산된 수량을 차감하는지도 독립검증에 포함하세요. '동시에 살아 있는 MO가 하나'만으로 누적 중복 생산을 방지했다고 판정하지 마세요. 취소·개정으로 남는 WIP/완제품은 처분 필요 이력으로 보존하세요.

원본 제품·운영 DB·Railway는 변경하지 않습니다. 실제 IQC 승인 LOT만 조립 가능한 병목은 그대로 결함/정책 검토 항목으로 유지하며 자동합격으로 우회하지 않습니다.


## 회신 (Claude 개발, 2026-09-11 17:35 KST) — 자원 양보 중 코딩 계속, 후보 `a16586c` (**미실행**)

지시대로 **무거운 Odoo/Docker 실행을 멈추고** 코딩만 이어갔습니다.
아래 두 건은 **커밋했으나 아직 실행하지 않았습니다.** 테스트 세션 B 완주 회신 뒤에
돌리고 결과를 보고하겠습니다. 통과했다고 적지 않습니다.

### ① ⚠ 지적하신 누적 중복 생산 — 제 구현의 실제 구멍이었습니다

> 「'동시에 살아 있는 MO가 하나'만으로 누적 중복 생산을 방지했다고 판정하지 마세요.」

맞습니다. 앞 가드는 형제 오더가 **살아 있을 때만** 막습니다. 이전 개정의 오더가
**완료**되면 살아 있는 오더가 없어 가드를 통과하고, 새 개정이 **전량을 다시** 생산합니다.
1개를 만든 뒤 2개짜리 개정이 오면 **합계 3개**입니다.

고친 내용:
- `_produced_qty_for_br()` — 살아 있는지가 아니라 **얼마나 만들어졌는지**를 셉니다
  (형제 오더 `qty_produced` 합).
- 새 개정의 오더는 요청 수량에서 **이미 만든 수량을 뺀 남은 수량**만 지시합니다.
- 더 만들 것이 없으면 오더를 내지 않고, 초과분은 `needs_disposition` +
  `disposition_note` 로 **처분 필요 이력**을 남깁니다.
- 취소도 같습니다 — 남은 WIP/완제품(`qty_produced` 포함)을 처분 필요로 보존합니다.
- 화면에 이전 개정 생산량·처분 필요·사유를 드러냈습니다.

시험 2건 추가(`..._already_produced_is_deducted...`, `..._no_new_order_when_enough...`).
**미실행**입니다.

### ② 불량 처분 — 먼저 읽어 보니 셋은 이미 되고 하나가 빈자리였습니다

| 배정 항목 | 코드 실측 |
|---|---|
| 불량 **실물완료** | **된다** — 게이트가 지연 PLC 판정 전 내부재고 생산을 허용 |
| **원재료소비** | **된다** — MO 가 완료되므로 불량분도 원재료를 소비 |
| **격리** | **된다** — 미검사·불량 LOT 은 하류(생산 투입·고객 출고)에서 막힘 |
| **처분** | **⚠ 빈자리** |

게이트는 목적지가 `production`/`customer` 인 이동만 막습니다. 폐기 위치는
`usage='inventory'` 라 **스크랩은 그냥 통과**했습니다. 즉 **불량 LOT 을 아무나 사유
없이 폐기**할 수 있었고, 어떤 불량 근거였는지도 남지 않았습니다.

→ 사출 원천 LOT 의 스크랩에 **처분 구분(폐기/재작업)·사유·품질관리자 승인**을 요구하고,
승인 시 **불량 근거(사출 단위 MO)** 를 함께 기록합니다.
부품·원재료 폐기는 통제하지 않습니다 — 거기까지 막으면 현장이 멈춥니다.

**품질 기준값을 발명하지 않았습니다.** 처분에 근거를 요구하는 절차 통제이고,
**승인선(누가 승인하는가)은 회사 정책값**이라 표준 품질 관리자 그룹을 쓰되
**확정 필요 값**으로 남깁니다. 확정해 주시면 바꾸겠습니다.

시험 5건 추가. **미실행**입니다.

### 실행 대기 목록 (B 완주 회신 후 한 번에)

```
python3 tools/night_runtime.py dev update \
  --modules escon_br_intake,injection_worksite,mrp_bom_scan_guard \
  --test-tags '/escon_br_intake,/injection_worksite,/mrp_bom_scan_guard'
```
대상: BR 15건(누적 중복 2건 포함) + 불량 처분 5건 + 이종검사 14건 + 기존 회귀.

### pg 관련

제 `dev-pg` 는 `OOMKilled=false` 이고 DB 가 119MB 하나뿐이라 양보할 것이 별로
없습니다만, 지시대로 **실행 자체를 멈춰** 호스트 Docker 메모리(5.77GiB)를 비워 두었습니다.
필요하시면 `cams_night_dev_main` 드롭해도 됩니다(재생성 가능).

**운영 가능 판정이 아닙니다.** 원본·운영·Railway 미변경, 로컬 exchange 에만 push.


## 추가 회신 (아스트라, 2026-09-11) — 테스트 연결 확인 및 정산 자체검증

개발 a16586c 미실행 후보 접수. 누적 생산 차감 및 불량처분 반례는 실행 후에만 통과 판정합니다. 처분 승인 품질 관리자 그룹은 잠정 기준으로 유지하고 실제 품질보류 해제의 품질+생산 2단계와 혼동하지 마세요.

테스트의 11번 재개 회신은 아직 없고 Claude 앱의 해당 원격 테스트 세션에는 Remote Control disconnected가 표시됩니다. 입력 전송을 시도했으나 접수 확인이 안 되므로 재개 성공으로 보고하지 않습니다. Git 큐 지시는 유효합니다.

무거운 Odoo 시험 컨테이너가 없는 것을 확인하고 root 정산 자체회귀 1건을 시작했습니다. 실행 clone astra-settlement @b67e0e2, 후보 eecc929, DB cams_night_settlement_reconciliation, 명령 `python3 tools/night_runtime.py settlement update --modules gh_vendor_settlement --test-tags /gh_vendor_settlement --db-suffix reconciliation`, 로그 runtime/reconciliation-update-3.log. 아직 진행중입니다. settlement-pg도 memory1536MiB/swap3GiB로만 변경(삭제/재시작 없음). 테스트가 복귀하면 이 한 건 종료 후 B를 재개하고, 개발은 그동안 SQ 목록/증빙연결 및 잔여 제품코딩을 계속할 수 있습니다. 같은 호스트에서 무거운 실행을 겹치지 않도록 실제 컨테이너 상태와 큐 회신으로 조정하세요.


## 추가 회신 (아스트라, 2026-09-11 17:18 KST) — 정산 35 PASS, 시험 자원 해제

정산 실제 실행 종료(exit0): 후보 c47d5cb(제품 eecc929), 실행 b67e0e2, DB cams_night_settlement_reconciliation, 위 명령/로그에서 35 post-tests / 0 failed / 0 errors / skip표시없음. 새로운6반례 포함입니다. docs/tasks/N-SUPPLIER-CLAIM-RECONCILIATION.md에 근거를 보존했습니다. 정적검사나 기존29결과를 새 결과로 재사용하지 않았습니다. 실제 동시 트랜잭션 준비·기간마감 연결·독립검증·통합7일은 남아 있습니다.

정산 시험은 종료되어 자원을 비웠습니다. 테스트는 연결 복구 즉시 B 재개 후 최신 BR 후보와 정산 후보 독립검증을 진행하세요. 테스트의 수신/재개가 계속 불가능하면 개발은 Docker에 다른 heavy Odoo 실행이 없는지 확인하고 대기 중인 a16586c 자체시험부터 단독 진행해도 됩니다. 실행 시작/종료를 큐에 공유해 겹침을 피하세요. 개발은 B의 독립시험을 대신 통과 판정하지 않습니다.


## 회신 (Claude 개발, 2026-09-11 17:55 KST) — 실행 완료, 후보 `3e9db21` (215 tests, 0/0, skip 2)

앞 회신의 **미실행 후보를 실행했습니다.** 실행 전 `docker ps` 로 야간 실험 odoo
컨테이너가 하나도 없는 것을 확인했습니다(지시하신 「실제 컨테이너 상태로 조정」).
`cams-uat-preflight-odoo`·`cams-odoo-odoo-1` 은 야간 실험과 무관한 상시 스택입니다.

```
후보 SHA : 3e9db21 (제품 5370c2b) / DB cams_night_dev_main
명령     : dev update --modules injection_worksite,escon_br_intake,mrp_bom_scan_guard \
             --test-tags '/injection_worksite,/escon_br_intake,/mrp_bom_scan_guard'
결과     : 0 failed, 0 error(s) of 215 tests, skip 2
skip     : TestResinPOSettlement 2건 — vendor settlement 선택모듈 부재
로그     : artifacts/worksite_final_5370c2b.log
```

### 돌려 보니 제 시험 3건이 틀렸습니다

「미실행」으로 낸 것이 다행이었습니다. 첫 실행에서 **2 failed / 1 error** 였습니다.

1. `button_mark_done` 이 `to_close` 에서 멈췄습니다 — 부품 재고가 없으면 소비 경고
   마법사가 떠서입니다. **그 상태로는 누적 차감 로직에 도달조차 못 했습니다.**
   부품을 실제로 채우고 소비 경고만 건너뛰도록 고쳤습니다.
2. 취소 메시지 문구를 제가 바꿨는데 시험이 **옛 문구**를 보고 있었습니다.

고친 뒤 BR 15건 + 불량 처분 5건 모두 통과합니다.

### ⚠ 회귀에서 깨진 대시보드 시험 — **제 변경이 아닙니다** (근거 있음)

`TestDashboardData.test_today_payload` 가 깨졌길래 **base `34e375a` 에서도 돌려 봤고
똑같이 실패**했습니다. 선재 결함이고, 원인은 **시각 의존**입니다.

- 대시보드 `today` = **교대 시작부터 24시간** (이 환경 UTC 08:00 시작)
- 시험은 일시중지 로그를 `now - 30분` 에 만듦
- → **교대 경계 직후 30분 동안만** 그 시각이 어제 구간에 떨어져 집계에서 빠짐

실측: **08:24 실패(count 1), 07:45 통과.** 구간 `09-11 08:00~09-12 08:00`,
`now-30분` = 07:54 로 구간 밖이었습니다.

`injection_worksite` 가 제 소유라 **시험만** 구간 안쪽에 고정했습니다.
**제품 동작은 바꾸지 않았습니다.** 범위를 넓힌 것으로 보시면 되돌리겠습니다.

### ⚠ 제가 만든 환경 불일치 — 숨기지 않고 보고합니다

§11 품질 후보 검증 때 `iatf_quality_precedence` 를 이 DB 에 설치했는데, 그 소스는
**검증 전용 가지에만** 있습니다. 그래서 제 브랜치 실행 로그에
`Some modules have inconsistent states: ['iatf_quality_precedence']` 가 남습니다.

- **시험 결과에는 영향 없습니다**(Odoo 가 건너뜀, 215건 통과).
- 정리하려면 uninstall 하거나 DB 재생성이면 되는데, 되돌리다 DB 를 깨뜨리는 위험보다
  기록해 두는 쪽을 골랐습니다. **정리하라고 하시면 하겠습니다.**
- `br.db.model has no table` 은 선재 사항입니다(수동설치 FDW VIEW).

### 처분 승인선 — 혼동 방지 반영

지적하신 대로 **품질 보류 해제의 「품질+생산 2단계」와 다른 절차**임을 코드 주석에
명시했습니다. 보류 해제는 *보류된 물건을 다시 쓰게* 하는 결정이고, 처분 승인은
*불량 판정된 물건을 버리거나 재작업* 하는 결정이라 방향이 반대입니다.
이 1단계는 **잠정 기준**으로 유지합니다.

### 다음

SQ 목록/증빙연결(09번)로 넘어가겠습니다. 계속 미완료: BR→납품→매출근거 자동연결,
전 구간 공정별 시간기록, 부족 경보, seed `20260930`/조립 불량 3%.
**운영 가능 판정이 아닙니다.**
