# 서버 담당(원도영) 2026-09-17 회신 반영 — 재기준화 2라운드(미러 0b36e7f) · 결정 요청 2건

- 발신: Claude 개발 · 2026-09-17 09:xx · 대상: 전체(사용자 결정 2건, 테스트 재검증, 서버 담당 참고)
- 회신 원문: 사용자 채팅 + `~/Downloads/20260916 검토결과 AI 답변 회신용.md`(9개 항목). 큐에는 요지만 적는다.

## 회신 요지와 반영
1. **폴더 일원화 판단 요청**(odoo_plugins=escon-odoo 소유, iatf_plugins=wilcoco addons_custom 대응, 운영은 둘 다 마운트) → **판단: odoo-uat 의 단일 `addons/` 를 정본 배치로 하고, 미러는 당분간 두 폴더 유지**(운영 compose/odoo.conf 를 건드리지 않기 위해). 릴리스 시 모듈→폴더 매핑표(현 배치 그대로, 신규는 odoo_plugins)로 미러에 배치. 기술명 충돌 없음 확인됨. 일원화(미러도 단일 폴더)는 운영 addons_path 변경이 필요하므로 별도 창에서.
2. **병합 대상**: 미러 main **0b36e7f**(09-16 16:20) + 정본 `merge/20260914-final-review`@1098579. 정본 브랜치는 트리 대조 결과 **미러보다 뒤**(injection_planning 1.10<1.11, 나머지 README 확장자 차이) → 추가 대상 없음. 미러 0b36e7f 의 12커밋(30파일: injection_worksite 14, gh_vendor_settlement 9, account_kr_plus_patch 6, escon_br 1)을 2라운드로 병합.
   - **escon_web_trace 는 설치 금지**(웹 이종검사, 공정에서 하는 게 원칙) → UAT 에서 제거(uat_bom_purpose_seed 18.0.1.1.0 migration 이 `button_uninstall` 예약), 운영 반영 제외 목록에 추가. escon_employee 는 설치 유지.
3. **운영 상태**: 미러 HEAD=운영 코드(0b36e7f, 로컬 수정 없음). 설치 모듈 70개·버전 목록 수신 → **odoo-uat 에 70개 전부 존재**. 버전 위반 3건(account_kr_plus_patch 2.5.4<2.5.6, injection_worksite 8.5.0<8.8.0, escon_mo_barcode '0.9' 표기) → 2라운드에서 2.5.7 / 8.9.0 으로 올림(6번 규칙: 항상 운영보다 높게). 디스크 모듈 수 101=마운트 101.
4. **BOM 목적 분포**: vehicle_batch 5016(관리) / odoo_standard 39, **사출품 BOM 18건 전부 odoo_standard** → 운영에서는 사출 계획이 BOM 을 못 찾는 상태(서버 담당: 데이터 정리 후 '사출품 BOM' 으로 관리 예정). UAT 재분류(uat_bom_purpose_seed)는 UAT 전용 유지. 운영 데이터 정리는 서버 담당 몫.
5. 발견 결함 수정 동의. 6. 버전 항상 상승. 7. 미러 iatf_plugins 최상위 잡파일 제거 동의(릴리스 동기화 시 제거). 8. 복제본: 복제본 서버에서 업그레이드 완료 후 DB 덤프(파일스토어 포함)→중립화→전달. 9. 리허설·UAT 검증 후 미러로 받아 운영 반영, 문서 검토.

## 2라운드 병합 결과(후보 `rc/r145-uat-candidate` 갱신, 시험 실행 중)
- 채택: injection_worksite PLC 완료 계약·고아 접수 정합·임시 바코드 복구(controllers, temp_serial, plc 시험 2), account_kr_plus_patch 세금계산서 목록, escon_br 지시표 레이아웃.
- **미채택 1 — gh_vendor_settlement 운영 병행 설계**(line_kind/adjustment_of_id/BOM 스냅샷/event_key unique, migration 2.2.0, `_create_vendor_accrual_lines(target_good_qty, reason)`): 정본 R134(entry_kind/event_key/source_snapshot/부분 정정/대사·이월·마감 게이트)가 상위 집합이고 gh_total_mes·escon_br_intake 가 R134 API(`_record_settlement_good_output`)를 호출. 운영 시험 4건을 R134 API 로 이식해 보장 동작 증명(`test_r145_production_integrity_port`). 버전 4.2.0>2.2.0.
- **미채택 2(결정 요청 A) — injection_worksite 톤 정산 원천 재설계**: 운영은 '입고 이동(receipt/return, kg 환산, 단가 일자, raw_material_settlement_source, FOR UPDATE)' 기준으로 다시 짰고, 정본은 '사일로 투입 로그 + 발주 기준 수지는 PO 로 청구(사용자 정책 #14)'. 둘 다 넣으면 이중 청구 위험. → **사용자·서버 담당 결정 필요**: (a) 정본 유지(사일로 투입+PO 정책) (b) 운영 방식(입고 기준)으로 전환하고 정책 #14 재검토 (c) 운영 방식을 '비PO 수지'에만 적용하는 절충. 개발 권고: (a) 로 두고 서버 담당의 재설계 의도(입고 기준이 필요한 이유)를 먼저 듣는다.
- **의미 차이(결정 요청 B) — 마감 후 정산 조정**: 운영은 마감 뒤 조정을 '오늘 날짜로 자동 전기 + 원생산일 보존'; 정본은 '자동 전기 없음, 승인 대기, 마감 기간 승인 거부'(Q2 이월 종료 시점과 연동). 이식 시험은 정본 의미로 작성. → Q2 답과 함께 결정.
- 제외 목록(운영 반영 시): uat_safety, uat_bom_purpose_seed, escon_web_trace(설치 금지), test_*, 각 모듈 .claude/.vscode, iatf_plugins 최상위 잡파일.

## 회신 (테스트, 2026-09-17 09:4x) — 2라운드 수신 · 재검증 준비 · 이식 시험 1건 표기 지적 · 결정 A/B 는 관여 안 함
- 후보 `rc/r145-uat-candidate` @ 3b0e568 정적 확인: `gh_vendor_settlement` **모델 무변경**(2라운드 diff = 이식 시험 파일 + tests/__init__ 뿐), 미러 병행 설계(line_kind/adjustment_of_id/migration 2.2.0)는 후보에 없음. injection_worksite 채택분(temp_serial·PLC 시험)은 내 R135 fixture 경로 밖. 버전 4.2.0/8.9.0 상승 확인.
- 재검증 브랜치를 3b0e568 + 내 파일로 재구성(제품 diff 0). 슬롯 배정 시 같은 절차(새 DB, R135 19·R134 21·probe 3·업그레이드 보존)에 **이식 시험 `test_r145_production_integrity_port` 4건을 내 DB 에서도 실행**해 개발 자체 결과와 대조.
- **표기 지적(내용 아님)**: `test_closed_period_adjustment_keeps_source_evidence` 의 docstring 첫 줄이 운영 의미("마감 뒤 조정은 오늘 날짜로 전기하고 원생산일 보존")를 그대로 두고 본문은 정본 의미(자동 전기 없음·마감 기간 승인 거부)를 단언함. 결정 B 가 (정본 유지)로 나면 첫 줄을 정본 의미로 고쳐야 "운영이 보장하려던 동작이 정본에서 성립" 이라는 파일 주장이 이 시험엔 해당 없음을 오독하지 않음. (b 결정이 운영 의미로 나면 시험 본문이 바뀌어야 하고 그때 재검증.)
- 결정 A(수지 정산 원천)·B(마감 후 조정)는 사용자·서버 담당 몫 — 테스트는 어느 쪽이든 결정된 의미로 독립 시험을 다시 맞춤(결정 전엔 정본 의미 유지). 운영 사출품 BOM 18건 전부 odoo_standard(서버 담당 회신)는 내가 지난 회신에서 한계로 둔 바로 그 위험이 운영에 실재함을 뜻함 — 데이터 정리 완료 여부가 운영 릴리스 전 확인 항목.
- Odoo 미실행, 슬롯 비어 있음.
