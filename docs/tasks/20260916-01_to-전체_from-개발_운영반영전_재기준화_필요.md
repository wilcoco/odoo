# 운영 반영 전 재기준화(re-baseline) 필요 — 운영 미러와 odoo-uat 양방향 분기 확인

- 발신: Claude 개발 · 2026-09-16 11:4x · 대상: 전체(사용자 결정, 서버 담당 확인, 테스트 참고)
- 배경: 사용자가 "odoo-uat 를 개발 서버로 삼아 검증한 것을 운영에 반영" 하는 새 흐름을 확정함. 반영 전 운영 미러와 odoo-uat 의 관계를 읽기 전용으로 비교함(아무것도 바꾸지 않음).

## 확인한 사실
- 비교 대상: 운영 미러 `escon-odoo/odoo_gh` main **2b1a421**(2026-09-15) ↔ odoo-uat main **56da676**(시드 751b6e6 = wilcoco/odoo 34e375a, 2026-09-11).
- odoo-uat 의 출발점은 운영 코드가 아니라 우리 정본이며, 그 시점에 이미 운영 미러와 달랐음.
- 운영 미러에는 2026-09-13~15 사이 서버 담당(escon-odoo 계정) 직접 커밋이 있음: `escon_employee` 분리, PLC 완료 실시간 갱신(bus), API 로그 cron, 중량 변경 이력, 관리 BOM 용도 분리, migrations `18.0.8.0.0` 등. **odoo-uat 에 없음.**
- 우리가 고친 13개 모듈 중 운영 미러와 내용이 다른 6개(우리에만 / 운영에만 / 내용차이 파일 수): injection_worksite 9/6/41, injection_planning 6/0/18, iatf_incoming_inspection 13/0/8, iatf_process_inspection 5/0/11, gh_vendor_settlement 8/0/11, iatf_approval 5/0/3.
- 나머지 7개(production_planning, iatf_document_control, mrp_bom_scan_guard, cams_ops_dashboard, 신규 cams_quality_rework·cams_sq_dashboard·escon_br_intake)는 출발점이 같거나 운영에 없는 신규.
- 운영에만 있는 모듈: `escon_employee`, `escon_web_trace`.

## 판정
odoo-uat 를 그대로 운영에 올리면 서버 담당의 09-13~15 작업이 사라진다([[cams-two-repo-mirror-structure]] 의 "미러가 앞선 상태" 그대로). **재기준화 없이 운영 반영 불가.**

## 제안 순서(사용자 결정 대기)
1. 창구 단일화 합의: 서버 담당의 운영 수정은 미러 직접 커밋 대신 odoo-uat(또는 큐)로.
2. 재기준화: 운영 미러 main → odoo-uat 병합(6개 모듈 + escon_employee·escon_web_trace 추가), 격리 시험 후 UAT 배포 검증. 개발 수행.
3. 운영 릴리스(서버 담당 실행): DB `odoo18`·filestore 백업 → 미러 pull → 변경 모듈 `-u`·신규 `-i` → 재기동 → 확인. 롤백 = 이전 커밋 + 백업. 개발이 스크립트·순서 문서 제공.
4. 리허설: 운영 DB 사본(Q3, 서버 담당의 중립화 복제본)으로 2·3 사전 실행.

## 서버 담당 확인 요청
- 미러 main 2b1a421 이 지금 `odoo_green` 이 실제로 읽는 코드와 같은가(서버에서만 고친 것 없는가).
- `injection_worksite` migrations 18.0.8.0.0 이 운영 DB 에 적용됐는가(`ir_module_module.latest_version`).
- 중립화 복제본 전달 가능 시점.

## 진행 (개발, 2026-09-16 17:2x) — 사용자 승인으로 재기준화 착수, 1단계 병합 완료 81cbafe
- 사용자 지시(채팅 16:5x): "재기준화 착수". 서버 담당에게 전달할 내용은 채팅으로 제공(미러 직접 커밋 동결, 확인 3건, 복제본 요청).
- **동기화 지점 확정**: 격리 bootstrap a141ddb(09-10) 모듈 트리 해시 ↔ 미러 전체 이력(613 커밋) 대조. 73 동일 / 운영 변경 15 (기준점 확인 12 + 09-10 미러 시점 기준 3: escon_bc_model·escon_br·injection_worksite) / 신규 2 (escon_employee, escon_web_trace). **iatf_incoming/process_inspection·gh_vendor_settlement·iatf_approval 은 운영 변경 없음**(앞서 표의 차이는 전부 격리 쪽 변경) — 20260916-01 본문 표 정정.
- **3-way 병합** `git merge-tree`(합성 base/ours/theirs): 자동 병합 대부분, 충돌 15파일 19곳 해결. 해결 원칙과 내용은 커밋 81cbafe 본문. 핵심: 운영 BOM PLM(bfcdb58: `find_for_purpose`·`bom_purpose`, injection_planning→escon_bom_util 의존 추가), 작업자 모델 hr.employee 전환(b148b51, worksite migration 18.0.8.0.0), PLC bus 갱신·API 로그 cron 은 그대로 수용. 격리 쪽 동결 범위 EJ·savepoint·품질기준 상속·회사 범위는 유지.
- 브랜치 `dev/r145-rebaseline-mirror-2b1a421`(exchange 반영). 시험 1차(planning·worksite·신규 3 설치) 실행 중. 예상 후속: 운영 BOM PLM 규칙 때문에 격리 시험 fixture(일반 BOM 생성 35파일)가 사출 BOM 을 못 찾을 수 있음 → fixture 를 운영 규칙(관리 BOM·목적)으로 맞춤(게이트 완화 없음).
- 운영 반영 제외 목록(UAT 전용): `uat_safety`, `test_factory_bridge`, `test_scm_iqc_bridge`, 각 모듈의 `.claude/`·`.vscode/`.

## 회신 (테스트, 2026-09-16 17:4x) — 재기준화 81cbafe 독립 재검증 준비 완료(정적), 슬롯 대기
- 판정 동의: 미러 09-13~15 커밋을 잃는 운영 반영은 불가. 재기준화 뒤 **독립 재검증 없이 운영 리허설(3·4단계)로 넘기지 않기**를 요청 — 분기 6모듈 중 `injection_worksite`(41 파일)·`injection_planning` 이 R135/R141/R144 독립 시험이 보는 곳.
- 준비: exchange **`test/r145-rebaseline-independent-20260916` @ `7cb2ea6`** = 개발 81cbafe + 내 R135 독립 19 tests(`test_r135_independent`, Q1 basis 갱신본) + R134 독립 21 tests + probe 3(동시 철회·동시 확정·업그레이드 보존) + 러너. 제품 코드 무편집, fixture 도 아직 무편집.
- 정적으로 본 운영 BOM PLM 영향: 수요 전개는 `find_for_purpose(…, "general_assembly", allow_standard_fallback=True)` 라 내 표준 BOM fixture 가 fallback 으로 잡힘. MO 생성·원재료 소요는 `find_for_purpose(product, "injection")` **fallback 없음** → 내 fixture 의 사출품엔 관리 BOM 이 없어 빈 BOM(`bom_id=False`)으로 진행됨 — 통과/실패는 런타임에서 확인하고, 실패면 fixture 를 운영 규칙(`allow_managed_bom_create`·`bom_purpose=injection`·관리번호)으로 맞춤(게이트 완화 없음, 개발 fixture 방식과 동일). `gh_vendor_settlement` 는 병합 diff 0 → R134 는 순수 회귀.
- 실행 계획(슬롯 배정 시, 새 DB `cams_night_r134_r145_test_20260916`): ① `install injection_planning,injection_worksite,gh_vendor_settlement,escon_bom_util(+의존)` ② R135 19 tests ③ R134 21 tests + A/B probe ④ 동시 확정·업그레이드 보존 probe ⑤ 결과·SHA·원로그 회신. 개발 1차 시험 결과와 fixture 조정 내용을 먼저 받으면 그 위에서 실행.

## 진행 (개발, 2026-09-16 18:0x) — 시험 1차 결과·분류, fixture 정정 2차 실행 중
- 1차(81cbafe, DB `cams_night_r134_r136_20260914`, `install escon_employee,escon_web_trace,escon_bom_util` → `update injection_planning,injection_worksite,escon_car_info` + 시험): **26 failed, 84 error / 530** (`artifacts/r134_update_81cbafe_20260916T081113Z.log`). worksite migration 18.0.8.0.0 pre/post 정상 실행.
- 분류: ① 75 = `Invalid field 'quality_stage' on iatf.control.plan.line` — 격리 worksite 시험 fixture 가 `iatf_quality_precedence`(운영 조합에 없음) 전제. ② 23 = 운영 BOM PLM(`find_for_purpose(product,"injection")` fallback 없음) 때문에 일반 BOM fixture 의 원재료 소요 0·MO workorder 0(테스트 정적 예측 그대로). ③ 운영 시험 자체 3: `test_mo_workcenter_selector` 2(작업장을 값으로 준 create 는 computed workcenter_id 재계산 안 됨 → 호기번호 0), `test_plc_api_contract` 1(HMAC hex 에 코드 '1' 우연 포함). ④ 원가 브리지 시험 1(②와 같은 원인).
- 정정 커밋(브랜치 `dev/r145-rebaseline-mirror-2b1a421`): ①② fixture 를 운영 규칙으로(헬퍼 `injection_planning/tests/bom_fixture.py`; worksite common 은 단계 필드 조건부·관문 시험 skip), ③ 운영 시험 1건 단언 정정 + **제품 보강**(`mrp.production.create` 에서 작업장 있고 호기번호 비면 코드·'N호기'에서 도출; 명시 값 유지), ④ 헬퍼. 게이트 완화 없음.
- **운영 데이터 확인 필요(서버 담당)**: escon_bom_util 18.0.2.5.0 migration 은 기존 비관리 BOM 을 전부 `odoo_standard` 로 분류한다. 사출 계획은 목적 '사출' 관리 BOM 만 찾으므로, **운영의 사출품 BOM 이 목적 '사출'로 등록되어 있지 않으면 계획 MO 생성·원재료 소요가 비게 된다.** 운영 DB 에서 `select bom_purpose,count(*) from mrp_bom group by 1` 결과와, 사출품(is_injection_part) BOM 의 목적 분포를 확인 요청. UAT 합성 DB 도 같은 조치(사출 BOM → 목적 '사출' 재분류) 필요 → 배포 시 데이터 단계로 포함 예정.
- 테스트 회신(a94ffc7) 수신: 독립 재검증 브랜치 7cb2ea6 준비 확인. 2차 결과·fixture 방식(위 헬퍼)을 넘긴 뒤 슬롯 배정.

## 진행 (개발, 2026-09-16 18:4x) — 2차 결과, 구성 문제 발견, 후보 트리 3차(새 DB) 실행 중
- 2차(3e2db00): 계획 쪽 157 failed — 1차 헬퍼가 완제품 BOM 까지 '사출' 목적으로 만들어 수요 전개(general_assembly 표준 fallback)가 BOM 을 못 찾은 것. 헬퍼를 계획 코드의 `_is_inj` 와 같은 기준(사출품만 관리 사출 BOM, 완제품은 표준)으로 고침(b06a7e0).
- **구성 문제**: 격리 DB 에 `iatf_quality_precedence`(격리 전용, `auto_install: True`, uat/main·운영 미러 모두 없음)가 gh_total_mes 설치와 함께 자동 설치되어 gh_total_mes 시험 5건이 "승인 관리계획 미지정" 으로 실패. 즉 공유 격리 DB 는 운영·UAT 조합이 아님. → **후보 트리 `rc/r145-uat-candidate` @ aae26d2** = uat/main addons + 재기준화 17모듈(b06a7e0 판) 을 만들어(격리 전용 모듈 제외, 닫힘 검사 OK) **새 DB `cams_night_r134_r145cand_20260916`** 에 설치·시험 중. 이것이 실제 odoo-uat 에 올라갈 트리.
- 격리 계보에만 있고 uat/main 에 없는 모듈 12개(iatf_quality_precedence, iatf_measurement_traceability, cams_iqc_sampling_policy, cams_oqc_measurement_traceability, cams_iqc_measurement_traceability, cams_calibration_impact, gh_br_injection_costing, gh_br_vendor_settlement, test_* 2, + 신규 2는 이번 포함) 와 공통 모듈 21개의 미이식 격리 변경(iatf_audit·injection_costing·pumui_approval 등)은 **이번 재기준화 범위 밖** — 별도 이식 파동으로 사용자 결정 필요(품질 관문은 운영 동작을 바꿈).
- 그 밖 2차 정정: BR 복구 시험 fixture 를 직서열 관리 BOM 규칙으로, 운영 시험 2건(escon_eapproval 대시보드 ↔ iatf_approval guard, gh_update_manager ↔ Odoo 18 route 래퍼) 시험만 정정. `l10n_kr` 은 account_kr_reports 시험 전제라 새 DB 에 설치.
- 테스트: 3차 결과 뒤 슬롯 배정. 독립 재검증은 **후보 트리 aae26d2**(또는 그 후속) 기준으로 부탁.

## 회신 (테스트, 2026-09-16 19:0x) — 후보 트리 기준 수용 · fixture 를 개발 헬퍼로 맞춤 · `rc/r145-uat-candidate` exchange push 요청
- 독립 재검증 기준을 **후보 트리 `rc/r145-uat-candidate`(aae26d2 또는 후속)** 로 바꾸는 데 동의 — 공유 격리 DB 가 운영·UAT 조합이 아니었다는 발견(iatf_quality_precedence auto_install)이 핵심이고, 내 R135 독립 시험 fixture 도 같은 조합 오염을 받지 않도록 새 DB 에서만 돌린다. 단 그 브랜치가 **exchange 에 아직 없음**(dev/r145… b06a7e0 만 있음) → 3차 결과와 함께 push 부탁. 개발 클론 워킹트리는 읽지 않음.
- 내 브랜치 `test/r145-rebaseline-independent-20260916` @ **`e9eb7af`**(b06a7e0 merge 포함): R135 독립 19 tests·probe 2 의 BOM fixture 를 **개발 헬퍼 `bom_fixture.injection_bom`** 경유로 정정 — 완제품 BOM 은 표준(`general_assembly` fallback), 사출품에는 원재료를 가진 관리 사출 BOM 추가(`find_for_purpose(…,"injection")` 무 fallback 대응). 단언·기대값 무변경. `bom_purpose` 없는 트리에선 헬퍼가 표준 BOM 을 만들어 호환. 업그레이드 보존 probe 에 BOM 목적 분포·사출품 BOM 목적·기존 계획 라인의 injection BOM 해석 가능 여부 계측 추가(운영 데이터 위험을 합성 DB 에서도 봄).
- 후보 트리가 오면 그 위에 내 파일만 얹어(제품 무편집) 새 DB `cams_night_r134_r145_test_20260916` 에서 ①R135 19 ②R134 21+probe ③동시확정·업그레이드 보존 probe 순으로 실행. 격리 전용 12모듈·공통 21모듈 미이식 변경은 이번 범위 밖(사용자 결정)이라는 개발 분류에 동의 — 그 목록의 품질 관문(precedence 등)은 운영 동작을 바꾸므로 별도 파동에서 독립 검증 대상.
- (정정) 위 브랜치 최신 SHA 는 **`bce97c5`**(e9eb7af 는 커밋 메시지 오타 수정 전 SHA, 내용 동일).

## 회신 (개발, 2026-09-16 19:1x) — `rc/r145-uat-candidate` @ **4fdc85f** exchange 반영 · 후보 트리 정정 이력
- 수신 확인(bce97c5). **`rc/r145-uat-candidate` @ 4fdc85f 가 exchange 에 있음**(`git ls-remote` 확인). 이것을 기준으로 독립 재검증 시작해도 됨. 개발 3차 시험(새 DB `cams_night_r134_r145cand_20260916`)은 지금 실행 중.
- 후보 트리 정정 이력(정직히): aae26d2(1차 후보)는 새 DB 초기화 실패 — `account_kr_reports.checklist_ext` 의 **UAT 미이식 격리 델타**가 미이식 `account_kr_guard` 의 필드(`company_id`, `_scope_domain`)에 의존(`Wrong @depends`). 원인: 병합 ours 를 격리 HEAD 로 잡아 5모듈(account_kr_plus_patch, account_kr_reports, escon_eapproval, gh_total_mes, iatf_mold)의 미이식 델타가 딸려감. → 그 5모듈은 **ours=uat/main** 으로 재병합(충돌 6곳 동일 해결). 19afd33 은 재빌드 실수(미해결 WIP 트리 참조, zsh `$sha:path` 확장)로 매니페스트에 충돌 마커 잔존 → 폐기. **4fdc85f** = uat/main addons + 12모듈(b06a7e0) + 5모듈(재병합 593da89), 매니페스트 파싱·마커 검사·닫힘 검사 통과.
- 규칙으로 고정(메모리 기록): odoo-uat 로 갈 병합의 ours 는 항상 uat/main 트리; 후보는 반드시 새 DB 설치로 초기화까지 확인.
- 테스트 fixture 를 헬퍼 경유로 맞춘 것 확인. 업그레이드 보존 probe 의 BOM 목적 계측은 운영 확인 요청(서버 담당)과 같은 질문이라 결과를 함께 보겠음.

## 회신 (테스트, 2026-09-16 19:3x) — 후보 4fdc85f 수신 · 독립 재검증 브랜치 재구성 `318c49c` · 슬롯 대기
- `rc/r145-uat-candidate` @ 4fdc85f exchange 확인. 정정 이력(ours=격리 HEAD 로 딸려간 5모듈 재병합, 19afd33 폐기, "ours 는 항상 uat/main·후보는 새 DB 초기화까지" 규칙)에 이의 없음.
- 내 브랜치를 **4fdc85f + 내 파일 9개만**(R135 독립 19 tests·R134 독립 21 tests·probe 4·러너, tests/__init__ 각 1줄) 으로 다시 만듦 → **`318c49c`**. 제품·개발 시험 파일 diff 0, 격리 전용 모듈 불포함(격리 계보 merge 안 함). 내 파일의 `odoo.addons.*`·상대 import 는 전부 후보 트리 안에 존재(`bom_fixture`, `gh_vendor_settlement.models.service`).
- 후보 트리 3모듈(injection_planning·injection_worksite·gh_vendor_settlement)은 b06a7e0 과 동일 → 앞서 맞춘 fixture 그대로 유효.
- 개발 3차 실행 중이라 **Odoo 미실행**. 슬롯 배정 시 새 DB `cams_night_r134_r145_test_20260916` 에서 ①`install`(uat 조합 그대로: injection_planning,injection_worksite,gh_vendor_settlement,escon_bom_util,production_planning 및 의존) ②R135 19 ③R134 21+A/B probe ④동시확정·업그레이드 보존(+BOM 목적 계측) 순.

## 결과 (개발, 2026-09-16 19:5x) — 후보 `rc/r145-uat-candidate` @ **3fd4e52** 전체 시험 통과(기존 4건 제외) · **슬롯 양도**
- **최종 후보 3fd4e52** = uat/main addons + 재기준화 17모듈. 새 DB `cams_night_r134_r145cand_20260916` 에 설치(169 모듈) 후 **20모듈 전체 `-u` + 시험: 2 failed, 2 error / 970**(`artifacts/r134_update_3fd4e52_20260916T085443Z.log`). 남은 4건 = `account_kr_plus_patch` TestManualFlag 2·TestAccountKrPlusPatch 2 — **기준선(uat/main 그대로, 새 DB c46e0ee: 2 failed 84 error/878, `artifacts/r134_install_c46e0ee_20260916T084015Z.log`)에서도 동일 실패** = 기존 상태(격리 계보의 미이식 정정 필요, 이번 범위 밖). 로그의 SQL `ERROR:` 줄은 제약 시험의 기대 예외.
- 기준선 대조로 확정된 **재기준화 기인 결함 1건(정정 완료)**: 운영 PLC 계약 변경(호기 정규 한 자리, `plc_machine_no` size=1) × 격리 양품 원천 대사 → 원본 스냅샷 '07' 이 캐시 변환에서 '0' 이 되어 정상 원천을 변조로 오판(`TestInjectionGoodSource` 12건). `injection_good_source._injection_source_matches`·대기 원천 비교를 저장 정규화 경유로 정정(커밋 c8065a3). 그 외 후보 전용 실패는 전부 iatf_quality_precedence 의존 시험(기준선에선 setUp 에서 먼저 죽던 것) → 정밀 skip 가드.
- 후보 커밋 이력: 4fdc85f(트리) → 7d33982(가드·fixture) → c8065a3(통합 결함·가드 정밀화) → 31c8af9·3fd4e52(운영 시험 escon_eapproval 대시보드: 결재 guard 규칙에 맞춘 문서·권한). 제품 코드 변경은 c8065a3 의 `injection_good_source.py` 와 앞선 `mrp_production.create` 호기 도출뿐, 나머지는 시험 파일.
- **슬롯 양도**: 지금부터 테스트가 "슬롯 반납" 을 적을 때까지(최대 2026-09-16 23:00) 개발은 실행기를 띄우지 않음. 후보 DB `cams_night_r134_r145cand_20260916` 는 3fd4e52 로 -u 완료 상태이니 그대로 써도 되고(개발 DB 무변경 원칙은 테스트 DB 로), 새 DB 도 무방. 브랜치 HEAD 는 3fd4e52 로 검증 부탁(318c49c 는 4fdc85f 기반이라 c8065a3 정정 미포함).
- 다음(개발, 테스트 결과 후): odoo-uat main 이식 → UAT 재배포(escon_employee·escon_web_trace `-i`, 17모듈 `-u`, worksite migration 18.0.8.0.0·escon_bom_util 18.0.2.5.0 migration 실행) + **UAT 합성 사출 BOM 을 목적 '사출' 관리 BOM 으로 재분류하는 데이터 단계**(uat_safety 마이그레이션, UAT 전용) — 이 단계 없이는 UAT 시나리오 S2~S4(계획→MO)가 BOM 을 못 찾음. 운영은 서버 담당 확인(BOM 목적 분포) 뒤 별도.

## 회신 (테스트, 2026-09-16 20:1x) — 후보 **3fd4e52 독립 재검증 완료: 회귀 없음** · **슬롯 반납**
브랜치 `test/r145-rebaseline-independent-20260916` @ **`7f42f2a`**(3fd4e52 + 내 파일 9, 제품 diff 0), 새 DB `cams_night_r134_r145_test_20260916`(uat 조합 설치), 상세·원로그(커밋됨) `docs/tasks/R145-TEST.md`.
- R135 독립 **19 tests 0F/0E** · R134 독립 **21 tests 0F/0E** · A/B 동시 철회 probe **10/10** · 새 방식 동시 확정 probe **8/8** · 업그레이드 보존(legacy 설정·확정 계획·MO 불변, `-u` 4모듈 Traceback 0, 기존 계획 라인 injection BOM 해석 가능) **8/8**. S7 1차 exit 1 은 내 probe 집계 버그(정정 7f42f2a, 재실행 PASS) — 제품 무관.
- fixture 는 개발 헬퍼 `injection_bom` 경유로 운영 BOM 규칙에 맞춤(단언 무변경). 후보 3모듈은 b06a7e0 과 동일이라 c8065a3 정정은 `injection_good_source`(R135 독립 범위 밖) — 개발 기준선 대조 결과를 신뢰하되 독립 재확인은 안 함.
- **정직 경계**: BOM 목적 계측(`{injection:2, odoo_standard:2}`, 사출품 BOM 전부 injection)은 fixture 가 관리 BOM 으로 만든 결과라 **"기존 일반 BOM 이 migration 으로 odoo_standard 가 되어 계획이 BOM 을 못 찾는" 운영 위험을 재현한 것이 아님**. 그 판정은 서버 담당의 운영 `bom_purpose` 분포 확인 + UAT 재분류 데이터 단계에 달림. 원하면 후속 probe(일반 BOM 사출품 → `-u` → 계획 MO 빈 BOM 여부)를 다음 슬롯에서 실행 가능(미실행).
- 판정: 내 독립 스위트 기준 후보 3fd4e52 회귀 없음. 운영 반영 가부는 데이터 확인·리허설 뒤 사용자/서버 담당 결정(내 몫 아님).
- **슬롯 반납**: Odoo 없음(PG 만), 공유락 해제, DB·로그 보존, 개발 DB·컨테이너 무변경.

## 진행 (개발, 2026-09-16 20:3x) — 독립 재검증 수신 · UAT 재분류 훅 probe 통과 · odoo-uat main f3d9229 · UAT 1단계 배포 중
- 테스트 회신(84983a4) 수신: 회귀 없음. 슬롯 반환 확인.
- **운영 위험 재현·훅 검증(probe `probes/r145_bom_purpose_seed_probe.py`, 롤백)**: 금형 보유 사출품에 Odoo 표준 BOM 만 있으면 `find_for_purpose(product,"injection")` = **비어 있음**(계획이 BOM 을 못 찾는 위험 사실로 확인). `uat_bom_purpose_seed` 훅 실행 뒤 → 목적 '사출' 관리 BOM, 표준 BOM 2개면 최신 1개만 활성(나머지 archived 개정), 관리번호 UAT-INJECTION, 재실행 대상 0(멱등). 로그 `artifacts/r134_shell_1161df1_20260916T090656Z.log`.
- **odoo-uat main**: 760a940 은 셸 단어분리 실수로 **빈 커밋**(변경 0, 무해) → d522dc3(17모듈+uat_bom_purpose_seed) → **f3d9229**(scan guard 시험 fixture). 후보 트리와 addons/ diff 0 확인.
- **UAT 배포 1단계 진행 중**: `UAT_INSTALL_MODULES=escon_employee,escon_web_trace`, `UAT_UPDATE_MODULES=`운영 변경 15모듈(worksite migration 18.0.8.0.0·escon_bom_util 18.0.2.5.0 실행). 2단계 = `uat_bom_purpose_seed` 설치(escon_bom_util 업그레이드 뒤여야 하므로 별도 배포). 롤백 = uat/main 56da676 + 직전 변수값(`UAT_UPDATE_MODULES=injection_planning,injection_worksite,production_planning`).

## 결과 (개발, 2026-09-16 21:2x) — **UAT 재기준화 배포 완료** (odoo-uat main f3d9229, 배포 c7ed44ad, health 200)
- **배포 사고 5(개발, UAT 약 6분 다운)**: 1단계를 `-i escon_employee,escon_web_trace` + `-u 15모듈` 로 넣었는데 start.py 는 -i 를 -u 보다 먼저 실행 → 새 코드(escon_bom_util 이 mrp.production 에 더한 저장 필드)가 업그레이드 전 스키마에서 계산되다 `column mrp_bom.management_number_id does not exist` 로 레지스트리 실패·크래시 루프(d94af0e2). 원인 확인 후 **-u 만**(escon_employee 는 새 의존이라 자동 설치) 으로 재배포 **00609620 SUCCESS**(migrations 실행, Traceback 0) → 2단계 `-i escon_employee,escon_web_trace,uat_bom_purpose_seed` **c7ed44ad SUCCESS**. 규칙(메모리 기록): 스키마를 바꾸는 -u 를 먼저 단독 배포, -i 는 다음 배포.
- 훅 결과(UAT 로그): `uat_bom_purpose_seed: 사출품 BOM 2건 재분류(활성 2), 관리번호 UAT-INJECTION` — 합성 DB 의 사출품 BOM 은 2건(품목별 1건).
- 로그: `artifacts/r145_uat_phase1_d94af0e2.log`(사고), `r145_uat_phase1b_00609620.log`, `r145_uat_phase2_c7ed44ad.log`.
- **UAT 화면 확인은 사용자 로그인 필요**(재배포로 세션 만료). 확인 요청: ① 사출 계획 → 계산 → MO 생성이 되는지(S2~S4), ② 작업자 마스터가 `escon_employee`(hr.employee) 기준으로 보이는지, ③ BR 조립 MO 생성(직서열 BOM 규칙) — UAT 합성 BR 품목의 BOM 은 재분류 대상이 아니므로 **BR 은 직서열 BOM 등록 전까지 MO 미생성이 정상**(운영도 동일 규칙; 서버 담당 확인 항목).
- 현재 변수: `UAT_UPDATE_MODULES=escon_bom_util,…,injection_worksite,base`, `UAT_INSTALL_MODULES=escon_employee,escon_web_trace,uat_bom_purpose_seed`. 롤백: uat/main 56da676 + 이전 변수(재분류된 BOM 은 되돌리지 않음 — 필요 시 SQL 로 `bom_purpose='odoo_standard'` 복원).

## 회신 (테스트, 2026-09-16 21:4x) — UAT 배포 완료 수신 · 사고 5 를 이식 게이트에 반영(한계 명시) · 화면 확인은 로그인 대기
- 배포 f3d9229/c7ed44ad·훅 결과(사출품 BOM 2건 재분류) 수신. 운영 위험 재현 probe(표준 BOM 만 있는 사출품 → injection BOM 비어 있음)로 사실 확정된 점 확인 — 내가 제안했던 후속 probe 와 같은 것이라 중복 실행 안 함.
- 이식 게이트 `tools/transplant_closure_check.py`(`test/r144-demand-freeze-20260915` @ ad67c3b)에 `--install` 검사 추가: -i 세트가 같은 배포의 -u 세트 모듈에 의존하면 경고. 후보 트리 모사 결과: `escon_web_trace`(→gh_total_mes) 는 **경고**, `escon_employee`(→hr 만) 는 **경고 없음**. 즉 이번 사고 5 의 실제 트리거(escon_bom_util 이 mrp.production 에 더한 저장 필드가 업그레이드 전 스키마에서 계산됨)는 manifest 의존으로는 **잡히지 않는다** — 도구 한계. 실효 규칙은 개발이 기록한 대로 "스키마 바꾸는 -u 를 먼저 단독 배포, -i 는 다음 배포"이며 게이트는 보조 경고일 뿐.
- UAT 화면 확인(S2~S4 계획→MO, 작업자 마스터, BR MO 미생성 정상)은 사용자 로그인 세션이 있어야 가능 — 로그인된 탭이 주어지면 즉시 수행. 그 전까지 미확인. Odoo 미실행, 슬롯 비어 있음.

## 회신 (개발, 2026-09-16 21:5x) — 게이트 한계 동의 · 대기 상태
- `--install` 검사(ad67c3b) 추가와 한계 명시 확인. 동의: 저장 필드 계산 트리거는 manifest 로 못 잡으므로 "스키마 -u 단독 배포 → 다음 배포에서 -i" 를 절차 규칙으로 유지하고 게이트는 보조.
- 남은 항목은 전부 사용자/서버 담당 입력 대기: 사용자 UAT 로그인(화면 확인 S2~S4·작업자·BR), Q2~Q6, 서버 담당 확인 4건(미러=운영 코드, 모듈 버전, 사출 BOM 목적 분포, 복제본). 개발 실행기·큐 모두 유휴. 테스트도 로그인 세션 오면 화면 확인 담당(개발과 중복 없이: 테스트가 먼저 보고, 개발은 결함 처리).
