# 운영 반영 전 재기준화(re-baseline) 필요 — 운영 미러와 odoo-uat 양방향 분기 확인

- 발신: Claude 개발 · 2026-09-16 11:4x · 대상: 전체(사용자 결정, 서버 담당 확인, 테스트 참고)
- 배경: 사용자가 "odoo-uat 를 개발 서버로 삼아 검증한 것을 운영에 반영" 하는 새 흐름을 확정함. 반영 전 운영 미러와 odoo-uat 의 관계를 읽기 전용으로 비교함(아무것도 바꾸지 않음).

## 확인한 사실
- 비교 대상: 운영 미러 `escon-odoo/odoo_gh` main **2b1a421**(2026-09-15) ↔ odoo-uat main **56da676**(시드 751b6e6 = wilcoco/odoo 34e375a, 2026-09-11).
- odoo-uat 의 출발점은 운영 코드가 아니라 우리 정본이며, 그 시점에 이미 운영 미러와 달랐음.
- 운영 미러에는 2026-09-13~15 사이 서버 담당(escon-odoo 계정) 직접 커밋이 있음: `escon_employee` 분리, PLC 완료 실시간 갱신(bus), API 로그 cron, 중량 변경 이력, 관리 BOM 용도 분리, migrations `18.0.8.0.0` 등. **odoo-uat 에 없음.**
- 우리가 고친 13개 모듈 중 운영 미러와 내용이 다른 6개(우리에만 / 운영에만 / 내용차이 파일 수): injection_worksite 9/6/41, injection_planning 6/0/18, iatf_incoming_inspection 13/0/8, iatf_process_inspection 5/0/11, gh_vendor_settlement 8/0/11, iatf_approval 5/0/3.
- 나머지 7개(production_planning, iatf_document_control, mrp_bom_scan_guard, cams_ops_dashboard, 신규 cams_quality_rework·cams_sq_dashboard·escon_br_intake)는 출발점이 같거나 운영에 없는 신규.
- 운영에만 있는 모듈: `escon_employee`, `escon_web_trace`.

## 판정
odoo-uat 를 그대로 운영에 올리면 서버 담당의 09-13~15 작업이 사라진다([[cams-two-repo-mirror-structure]] 의 "미러가 앞선 상태" 그대로). **재기준화 없이 운영 반영 불가.**

## 제안 순서(사용자 결정 대기)
1. 창구 단일화 합의: 서버 담당의 운영 수정은 미러 직접 커밋 대신 odoo-uat(또는 큐)로.
2. 재기준화: 운영 미러 main → odoo-uat 병합(6개 모듈 + escon_employee·escon_web_trace 추가), 격리 시험 후 UAT 배포 검증. 개발 수행.
3. 운영 릴리스(서버 담당 실행): DB `odoo18`·filestore 백업 → 미러 pull → 변경 모듈 `-u`·신규 `-i` → 재기동 → 확인. 롤백 = 이전 커밋 + 백업. 개발이 스크립트·순서 문서 제공.
4. 리허설: 운영 DB 사본(Q3, 서버 담당의 중립화 복제본)으로 2·3 사전 실행.

## 서버 담당 확인 요청
- 미러 main 2b1a421 이 지금 `odoo_green` 이 실제로 읽는 코드와 같은가(서버에서만 고친 것 없는가).
- `injection_worksite` migrations 18.0.8.0.0 이 운영 DB 에 적용됐는가(`ir_module_module.latest_version`).
- 중립화 복제본 전달 가능 시점.
