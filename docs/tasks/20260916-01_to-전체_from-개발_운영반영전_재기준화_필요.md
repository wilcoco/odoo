# 운영 반영 전 재기준화(re-baseline) 필요 — 운영 미러와 odoo-uat 양방향 분기 확인

- 발신: Claude 개발 · 2026-09-16 11:4x · 대상: 전체(사용자 결정, 서버 담당 확인, 테스트 참고)
- 배경: 사용자가 "odoo-uat 를 개발 서버로 삼아 검증한 것을 운영에 반영" 하는 새 흐름을 확정함. 반영 전 운영 미러와 odoo-uat 의 관계를 읽기 전용으로 비교함(아무것도 바꾸지 않음).

## 확인한 사실
- 비교 대상: 운영 미러 `escon-odoo/odoo_gh` main **2b1a421**(2026-09-15) ↔ odoo-uat main **56da676**(시드 751b6e6 = wilcoco/odoo 34e375a, 2026-09-11).
- odoo-uat 의 출발점은 운영 코드가 아니라 우리 정본이며, 그 시점에 이미 운영 미러와 달랐음.
- 운영 미러에는 2026-09-13~15 사이 서버 담당(escon-odoo 계정) 직접 커밋이 있음: `escon_employee` 분리, PLC 완료 실시간 갱신(bus), API 로그 cron, 중량 변경 이력, 관리 BOM 용도 분리, migrations `18.0.8.0.0` 등. **odoo-uat 에 없음.**
- 우리가 고친 13개 모듈 중 운영 미러와 내용이 다른 6개(우리에만 / 운영에만 / 내용차이 파일 수): injection_worksite 9/6/41, injection_planning 6/0/18, iatf_incoming_inspection 13/0/8, iatf_process_inspection 5/0/11, gh_vendor_settlement 8/0/11, iatf_approval 5/0/3.
- 나머지 7개(production_planning, iatf_document_control, mrp_bom_scan_guard, cams_ops_dashboard, 신규 cams_quality_rework·cams_sq_dashboard·escon_br_intake)는 출발점이 같거나 운영에 없는 신규.
- 운영에만 있는 모듈: `escon_employee`, `escon_web_trace`.

## 판정
odoo-uat 를 그대로 운영에 올리면 서버 담당의 09-13~15 작업이 사라진다([[cams-two-repo-mirror-structure]] 의 "미러가 앞선 상태" 그대로). **재기준화 없이 운영 반영 불가.**

## 제안 순서(사용자 결정 대기)
1. 창구 단일화 합의: 서버 담당의 운영 수정은 미러 직접 커밋 대신 odoo-uat(또는 큐)로.
2. 재기준화: 운영 미러 main → odoo-uat 병합(6개 모듈 + escon_employee·escon_web_trace 추가), 격리 시험 후 UAT 배포 검증. 개발 수행.
3. 운영 릴리스(서버 담당 실행): DB `odoo18`·filestore 백업 → 미러 pull → 변경 모듈 `-u`·신규 `-i` → 재기동 → 확인. 롤백 = 이전 커밋 + 백업. 개발이 스크립트·순서 문서 제공.
4. 리허설: 운영 DB 사본(Q3, 서버 담당의 중립화 복제본)으로 2·3 사전 실행.

## 서버 담당 확인 요청
- 미러 main 2b1a421 이 지금 `odoo_green` 이 실제로 읽는 코드와 같은가(서버에서만 고친 것 없는가).
- `injection_worksite` migrations 18.0.8.0.0 이 운영 DB 에 적용됐는가(`ir_module_module.latest_version`).
- 중립화 복제본 전달 가능 시점.

## 진행 (개발, 2026-09-16 17:2x) — 사용자 승인으로 재기준화 착수, 1단계 병합 완료 81cbafe
- 사용자 지시(채팅 16:5x): "재기준화 착수". 서버 담당에게 전달할 내용은 채팅으로 제공(미러 직접 커밋 동결, 확인 3건, 복제본 요청).
- **동기화 지점 확정**: 격리 bootstrap a141ddb(09-10) 모듈 트리 해시 ↔ 미러 전체 이력(613 커밋) 대조. 73 동일 / 운영 변경 15 (기준점 확인 12 + 09-10 미러 시점 기준 3: escon_bc_model·escon_br·injection_worksite) / 신규 2 (escon_employee, escon_web_trace). **iatf_incoming/process_inspection·gh_vendor_settlement·iatf_approval 은 운영 변경 없음**(앞서 표의 차이는 전부 격리 쪽 변경) — 20260916-01 본문 표 정정.
- **3-way 병합** `git merge-tree`(합성 base/ours/theirs): 자동 병합 대부분, 충돌 15파일 19곳 해결. 해결 원칙과 내용은 커밋 81cbafe 본문. 핵심: 운영 BOM PLM(bfcdb58: `find_for_purpose`·`bom_purpose`, injection_planning→escon_bom_util 의존 추가), 작업자 모델 hr.employee 전환(b148b51, worksite migration 18.0.8.0.0), PLC bus 갱신·API 로그 cron 은 그대로 수용. 격리 쪽 동결 범위 EJ·savepoint·품질기준 상속·회사 범위는 유지.
- 브랜치 `dev/r145-rebaseline-mirror-2b1a421`(exchange 반영). 시험 1차(planning·worksite·신규 3 설치) 실행 중. 예상 후속: 운영 BOM PLM 규칙 때문에 격리 시험 fixture(일반 BOM 생성 35파일)가 사출 BOM 을 못 찾을 수 있음 → fixture 를 운영 규칙(관리 BOM·목적)으로 맞춤(게이트 완화 없음).
- 운영 반영 제외 목록(UAT 전용): `uat_safety`, `test_factory_bridge`, `test_scm_iqc_bridge`, 각 모듈의 `.claude/`·`.vscode/`.

## 회신 (테스트, 2026-09-16 17:4x) — 재기준화 81cbafe 독립 재검증 준비 완료(정적), 슬롯 대기
- 판정 동의: 미러 09-13~15 커밋을 잃는 운영 반영은 불가. 재기준화 뒤 **독립 재검증 없이 운영 리허설(3·4단계)로 넘기지 않기**를 요청 — 분기 6모듈 중 `injection_worksite`(41 파일)·`injection_planning` 이 R135/R141/R144 독립 시험이 보는 곳.
- 준비: exchange **`test/r145-rebaseline-independent-20260916` @ `7cb2ea6`** = 개발 81cbafe + 내 R135 독립 19 tests(`test_r135_independent`, Q1 basis 갱신본) + R134 독립 21 tests + probe 3(동시 철회·동시 확정·업그레이드 보존) + 러너. 제품 코드 무편집, fixture 도 아직 무편집.
- 정적으로 본 운영 BOM PLM 영향: 수요 전개는 `find_for_purpose(…, "general_assembly", allow_standard_fallback=True)` 라 내 표준 BOM fixture 가 fallback 으로 잡힘. MO 생성·원재료 소요는 `find_for_purpose(product, "injection")` **fallback 없음** → 내 fixture 의 사출품엔 관리 BOM 이 없어 빈 BOM(`bom_id=False`)으로 진행됨 — 통과/실패는 런타임에서 확인하고, 실패면 fixture 를 운영 규칙(`allow_managed_bom_create`·`bom_purpose=injection`·관리번호)으로 맞춤(게이트 완화 없음, 개발 fixture 방식과 동일). `gh_vendor_settlement` 는 병합 diff 0 → R134 는 순수 회귀.
- 실행 계획(슬롯 배정 시, 새 DB `cams_night_r134_r145_test_20260916`): ① `install injection_planning,injection_worksite,gh_vendor_settlement,escon_bom_util(+의존)` ② R135 19 tests ③ R134 21 tests + A/B probe ④ 동시 확정·업그레이드 보존 probe ⑤ 결과·SHA·원로그 회신. 개발 1차 시험 결과와 fixture 조정 내용을 먼저 받으면 그 위에서 실행.
