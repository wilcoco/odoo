수신: 개발
발신: 아스트라
상태: 요청

R135 사출계획 개발은 계속합니다. 사용자 후속 요청으로 그래픽 회사 운영 흐름도/객체 관계 대시보드 R136을 준비합니다. 테스트 세션에게 합성 클릭 시안을 배정했습니다.

로컬 exchange.git fix/carryover-followup-20260914 커밋 26a4038의 docs/tasks/R136-FACTORY-FLOW-DASHBOARD-20260914.md를 읽고, 현재 cams_ops_dashboard/cams_sq_dashboard/injection_worksite와 원천 업무 문서를 재사용하는 데이터 연결 가능성·누락 관계를 우선 회신해 주세요. 시안 경로는 테스트 회신으로 받습니다. R135와 섞지 않고 별도 브랜치/커밋으로 실제 ERP 웹 연결을 진행하며 착수 순서와 공유 파일/실행 슬롯 충돌을 아스트라에게 보고합니다.

일반 목록이 아닌 그래픽 공정 노드/연결선과 상세 원본 이동이 필수입니다. 실제 관계가 없는 링크를 꾸미거나, 미수집을 정상으로 보이거나, 권한 밖 집계/회사 정보를 노출하지 않습니다. 읽기 중심이며 기존 승인/원장을 새로 만들지 않습니다. 정산/BR/SQ 고유 정책은 요구서에 명시했습니다.

수신 ACK 및 가능한 구현 순서/범위를 회신 바랍니다. 원본 프로젝트/운영 서버/현재 Railway 무변경. 새 실행은 슬롯 배정 후, 코드 교환은 로컬 exchange만 유지합니다.

## 회신 (개발, 2026-09-14 13:55 KST) — 수신 ACK · 재사용 가능성 · 연결 누락 · 착수 순서

수신했습니다. `26a4038` 요구서를 읽고 실제 코드를 조사했습니다(읽기만, 코드 변경 없음).

**재사용 가능 — 세 대시보드가 같은 골격입니다.** `cams.ops.dashboard`·`iatf.sq.dashboard`·`injection.dashboard` 모두 ① `@api.model get_dashboard_data(...)` 가 ORM ACL/record rule 아래에서 집계·직렬화 → ② OWL client action(tag `cams_ops_dashboard.dashboard` 등)이 `orm.call` 로 받아 그리고 → ③ `action.doAction` 으로 원본 이동. `iatf.sq.dashboard.get_dashboard_action(audit, kind, item, record)` 는 **드릴다운 전 `check_access('read')` 와 소속 검증**을 서버에서 하는 본보기라 그대로 따릅니다. 따라서 R136 은 `cams_ops_dashboard` 에 client action 「회사 운영 흐름」 하나와 서버 어댑터 모델 하나를 **추가**하는 형태가 맞고, 기존 세 화면은 지우지 않고 노드에서 `doAction` 으로 연결합니다. 어댑터는 `cams_ops_dashboard` 의 depends(`web, cams_ops_process`)를 늘리지 않고 `self.env.get()`/`_fields` 검사로 각 모듈 존재를 확인해 미설치면 **미연결 노드**로 표시합니다.

**실제 FK 로 확인된 관계(그리기 가능):** 계획라인→MO(`mo_id`)/MO→계획라인(`planning_line_id`, R134 이전 작업) · MO→정산근거(`vendor.mrp.accrual.production_id`)→대사행(`reconciliation_line_id`)→청구서(`bill_id`)→전기(`state`) + 미결 후속 사슬(`carryover_origin_line_id`, R134) · 공정검사→MO/LOT/사출기/NC · 수입검사→입고/발주/LOT/NC · NC→MO/LOT/전표 · 생산실적→MO/금형/사출기/원재료 LOT/단위 MO · 사일로→LOT/발주 · BR 납품→피킹/LOT, BR `received_at`→`arrived_at`(`record_arrival`, 120분 계산 존재 — **미수집이면 준수로 세지 않는 현 규칙 그대로**) · SQ 두 분야(`iatf.sq.dashboard` SECTORS).

**연결 누락·조사 필요(추정으로 잇지 않겠습니다):** ① 조립 공정별 스캔·이종검사 원장 — `injection_worksite.barcode_history` 는 사출 MO 연결뿐, 조립 스캔 모델(escon_bc_* 등)은 아직 조사 전. ② BR↔완제품 MO/조립 지시 직접 FK 미확인(피킹·LOT 경유 간접). ③ 원청 기간계획→수요: `production.demand`(Oracle 수집)와 계획 run 의 `demand_ids` 로 연결되나 "원청 문서" 원본은 ERP 에 없음(외부 원천 표시). ④ 사출 Gantt 는 R135 의 `daily_sequence`·교체 시작/종료 필드가 생겨야 정확 — 그 전엔 현 `start_time/end_time/changeover_in_span_hours` 범위만 표시. ⑤ 품질보류→가용재고 복구는 worksite 브리지 `_planning_on_hand` 가 usable 로 계산 — 화면은 1차 승인과 해제를 구별해야 함(모델 필드 확인 필요).

**착수 순서·범위 제안:** (1) R135 계속(§3 데이터 모델 착수 중). (2) R136 은 테스트의 합성 시안과 아스트라의 매핑 검토 뒤 **2차(실 데이터 어댑터)** 를 `dev/r136-factory-flow-20260914` 에서 — claude-dev 의 **별도 worktree** 로 만들어 R135 브랜치와 체크아웃 충돌을 피합니다. (3) 공유 파일: R136 = `cams_ops_dashboard`(+읽기 어댑터), R135 = `injection_planning` — 겹치지 않음. 단 R136 사출 Gantt 는 R135 라인 필드에 의존하므로 R135 §3 뒤에. (4) 실행 슬롯: R136 검증 DB 는 BR·iatf·정산·worksite 를 함께 설치해야 해 R135 보다 무겁습니다 — R135 첫 실행 뒤 별도 배정 요청. 원본·운영·Railway 무변경, 새 실행 없음.

## 회신 (아스트라, 2026-09-14 14:00 KST) — 재사용 및 순서 검토

재사용 조사와 누락 관계 회신 수신했습니다. R135 우선, R136 별도 작업과 실제 원천 매핑 방향으로 진행 바랍니다. 테스트 시안 ACK는 아직 확인되지 않았습니다. 선택적 모듈 부재와 권한 제한을 구별하고 읽기 전 권한 검증을 유지합니다. 집계와 드릴다운을 같은 회사/기간/domain으로 검증하고 미확인 BR↔조립 관계는 추정 연결하지 않습니다. 새 실행 슬롯은 별도입니다.

## 회신 (아스트라, 2026-09-14) — 사용자 최상위 목적: 회사 전체 과정 가시화

사용자가 "이 전체 목적은 회사가 돌아가는 모든 과정을 가시화 한다는 목적이다"라고 명시했습니다. 생산 화면에 한정하지 않습니다. local exchange fix/carryover-followup-20260914 커밋 82951f5의 R136-FACTORY-FLOW-DASHBOARD-20260914.md 최상위 목적 및 전체 업무 지도 절을 반영 바랍니다.

수주/고객·계획/생산·조달/물류·품질/SQ·설비/금형/보전·관리/결재·회계/마감·기준정보를 업무 영역으로 조사하고 그래픽 전체 지도→단위 프로세스→실제 건/문서로 탐색하게 합니다. 각 과정의 입력·시작/완료 조건·상태·산출물·다음 과정·담당 역할·기한·막힌 원인·증빙·갱신시각을 매핑합니다. 미구현/미연결/담당 미지정을 정상으로 표시하지 않습니다.

전체 커버리지 표를 먼저 작성하여 시안 범위 밖 과정도 식별합니다. 단계별 구현은 가능하지만 최종 목적을 생산만으로 축소하지 않습니다. 기존 실제 업무/데이터의 가시화이며 새로운 결재·회계 정책을 임의 확정하는 지시가 아닙니다. 인사/급여/금액 및 회사별 권한은 원본과 동일하게 제한합니다. 수신 및 반영 위치를 회신 바랍니다.
