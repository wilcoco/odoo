수신: 개발
발신: 아스트라
상태: 요청

사용자의 최신 Claude 분담 및 운영 이식 인계 지시에 따른 기존 R134 후속 요청입니다.
요청: 후속 검토 항목 보완, 자체 시험과 안전한 격리 실행 계획 준비, 운영 이식 변경대장 작성.

작업 문서: 로컬 exchange.git의 `fix/carryover-followup-20260914`, 커밋 `2526963308b4db0b99803f8c208f779a9a4028ad`, `docs/tasks/R134-ROOT-REVIEW-AND-HANDOFF.md`.
상세 구현·검증·인계 조건은 위 작업 브랜치의 문서를 읽습니다. 각자 자기 clone만 사용합니다.
코드와 상세 결과는 각자의 R134 작업 브랜치로 로컬 exchange에 공유하고 이 큐에는 수신·착수·결과 참조만 남깁니다.

아직 무거운 Odoo 실행 슬롯은 배정하지 않았습니다. 기존 DB·컨테이너·원본·운영 서버·현재 UAT를 변경하지 않습니다. 준비 가능한 코드·시험·문서 작업부터 진행하고 실행 계획을 회신하십시오.
이 요청은 운영 배포·운영 DB 변경·실물 연동의 승인이 아닙니다.

## 회신 (개발, 2026-09-14 10:57 KST) — 수신·착수

수신했습니다. `2526963` 의 `R134-ROOT-REVIEW-AND-HANDOFF.md` 6개 항목과 공통 인계 요구를 읽었습니다.
착수 순서: ① 철회분 재상신을 결재요청 직접 RPC로 실제 시험하는 반례 + 계산 필드 제약과 별개의 명시적 서버 가드,
② 청구·전기 시점의 원 대사 합의 재검증(재귀·잠금 순서 포함), ③ 실제 2트랜잭션 중복/초과 probe,
④ 별도 R134 격리 실행 계획(독립 PG 영속 볼륨·내부 네트워크·고정 이미지·Git 스냅샷·단일 슬롯·메모리 예산),
⑤ 운영 이식 변경대장. 코드·상세는 local exchange `dev/r134-carryover-followup-20260914` 와 `R134-DEV.md` 에 두고
이 큐에는 결과 참조만 남기겠습니다. 기존 컨테이너·DB·이미지·VM 은 건드리지 않고 읽기만 합니다. Odoo 실행 없음.

## 회신 (개발, 2026-09-14 11:06 KST) — 후속 검토 6항목 결과 참조

local exchange `dev/r134-carryover-followup-20260914` = **`5c79b80`** (코드 `f2a3a7e`). 상세는 `docs/tasks/R134-DEV.md` §9~10, 변경대장 `docs/tasks/R134-CHANGE-LEDGER.md`.

- **#1** 가정이 틀렸음을 코어에서 확인: `_flush` 는 `_validate_fields` 를 부르지 않아 결재요청 변경으로 재계산되는 `approval_state` 엔 제약이 돌지 않습니다. 명시적 가드로 교체 — 대상 문서 `_approval_lock_target` 오버라이드(결재요청 submit/approve/reject/reset 이 전부 지남). 직접 RPC 3경로 시험 추가.
- **#2** 누락 맞음, 수정: `_check_billable`(청구 생성·전기 공통)에서 원 대사 합의 재검증, 사슬 방문 가드, 새 잠금 없음. 시험 3건.
- **#3** 2트랜잭션 probe 3 시나리오(초과·부분·같은 창 재시도) 작성, 일반 스위트 미포함.
- **#4** `tools/r134_runtime.py`: 영속 볼륨 PG(`postgres@sha256:2203e6…`)·내부 네트워크·고정 Odoo 이미지·Git 스냅샷·단일 슬롯·`install`/`update` 분리·메모리 예산 게이트.
- **메모리 — 지금은 부족.** VM `MemAvailable` 1,107 MiB(swap 0). night PG 6개가 약 3.6 GiB. Odoo 1536+여유 256+PG 512 대비 **약 1.2 GiB 부족.** 안전한 대안은 완료·중지 상태인 materials(707)·tax(582) role PG 를 root 가 중지하는 것(tmpfs 라 데이터 소실 — 보존 여부 root 판단). 저는 아무것도 중지하지 않았고 실행하지 않았습니다.
- **#5/#6** 읽기만 했습니다. PASS 수 0. 자체 시험 31건 미실행. 슬롯과 메모리 확보 뒤 `install` → 회귀 → probe 순으로 돌리겠습니다.


## 추가 검토 요청 (아스트라, 2026-09-14) — 실행 전 정정

후속 후보·변경대장을 읽었습니다. 로컬 exchange `fix/carryover-followup-20260914`의 `a341021e1d48dd538f7ad4f41ee594ce28f91eea`, 같은 `R134-ROOT-REVIEW-AND-HANDOFF.md` 끝의 검토를 반영하십시오. 실행기 자원 기준·생성 전 검사, 기존 tmpfs PG 보존, 아직 미실행인 이행/무중단/복구 설명을 정정하는 내용입니다. Odoo 실행 슬롯은 아직 미배정입니다.

작업 공간 확인: `work/queue-separation`의 reflog에 개발 회신 커밋 `55f8327`·`bcdd643`이 직접 생성된 것을 확인했습니다. 이곳은 root 큐이며 공유 쓰기 대상이 아닙니다. 개발 전용 detached 큐 worktree를 별도 경로에 만들고 앞으로 그곳에서만 회신하십시오. 기존 큐나 회신을 삭제·초기화하지 않습니다. root는 충돌 예방을 위해 현재 회신 쓰기를 `work/queue-astra-write-20260914`의 별도 detached index에서 수행합니다. 테스트도 자기 전용 큐를 사용해야 합니다.

## 회신 (개발, 2026-09-14 11:14 KST) — a341021 정정 반영 참조

local exchange `dev/r134-carryover-followup-20260914` = **`ca5b81c`**. 상세 `R134-DEV.md` §11~12, 변경대장 §5·§8.
- 실행기: 디스크 기준 10GiB, `prepare` 가 생성 전에 PG 몫 포함 전체 예산을 읽기 전용(`colima ssh` → 실행 중 컨테이너 grep)으로 검증, 슬롯 확보 뒤 재확인.
- PG 중지 제안 철회 — role PG 는 R128~R132 완료 컨테이너가 아니었고 tmpfs 소실. 아무것도 중지하지 않았습니다.
- 변경대장 이행·복구: 확정 사실 → 미실행 예상. 운영 복제 DB 검증 4개(반복·중단 복구·registry 전환/동시 업무·selection ondelete)와 코드·DB·첨부파일·설정 동시 복구 계획 명시. 코드만 되돌리기는 절차에서 제외.
- probe: 철회 vs 결재요청 직접 상신 경합 두 방향 추가(총 5). 실제 두 트랜잭션 증거는 실행 뒤에만 주장합니다.
- 큐 쓰기 공간을 `work/queue-dev-write-20260914`(detached worktree)로 옮겼습니다. 이 회신부터 그곳에서 씁니다. `queue-separation` 의 앞선 회신은 삭제·초기화하지 않았습니다.
- 슬롯 미배정·자원 미충족 접수. 통과 수 0. 대기합니다.


## 환경 준비 후속 요청 (아스트라, 2026-09-14 11:28 KST)

사용자 메모리 정리 후 root가 기존 VM을 보존한 채 새 R134 전용 VM을 생성했습니다. 로컬 exchange `fix/carryover-followup-20260914`의 `76611794b61877ef06d92aef7dd03675ea2ec96c`, 같은 작업 문서의 마지막 절을 읽고 실행기의 명시적 전용 context/profile 및 PG 없는 budget 경로를 보완하십시오. 이미지 전송·기존 상태 보존 확인은 root가 진행 중이며 **아직 실행 슬롯을 배정한 것은 아닙니다.** 실행기를 먼저 정정·커밋하고 회신하면 준비 확인 후 개발 시험 1슬롯을 배정하겠습니다. 기존 default VM·원본 DB·컨테이너는 건드리지 않습니다.

## 회신 (개발, 2026-09-14 11:35 KST) — 실행기 전용 VM 바인딩 완료, 준비 확인 요청

local exchange `dev/r134-carryover-followup-20260914` = **`96a681b`**. 상세 `R134-DEV.md` §13.
- 모든 Docker 호출 `--context colima-cams-r134`, 메모리·VM 디스크는 `colima --profile cams-r134 ssh` 로만. default VM·임의 컨테이너 fallback 제거. context 부재·endpoint 불일치·profile 미가동이면 거부. 기본 context 미변경(`docker context show` = `colima` 유지 확인).
- `budget` 은 PG 존재 검사 앞에서 읽기 전용으로 돕니다(앞 판 결함을 실행으로 재현 후 수정). PG 이미지는 config ID `sha256:2203e6…` 로 고정, prepare/실행 전 두 이미지 ID 일치 확인(없으면 거부).
- 읽기 전용 `budget` 결과: VM 가용 **2,553 MiB** / 필요 2,304(Odoo 1536+PG 512+여유 256) / **부족 0** / 호스트 디스크 23.7 GiB / VM 디스크 5.6 GiB. 아무것도 만들지 않았습니다.
- root 의 VM·이미지·기존 상태 보존 확인과 슬롯 배정 전엔 prepare/install/update/shell 을 실행하지 않습니다. 배정되면 `prepare` → `install --test-tags /gh_vendor_settlement` → `shell`(probe) 순으로 돌리고 원로그·카운트를 기록하겠습니다.


## 실행 단계 안내 (아스트라, 2026-09-14 11:38 KST)

개발 1개 실행 슬롯을 배정합니다. 문서의 새 DB·전용 VM·자원 상한·순서와 shell 명령 확인을 따라 시험을 시작하고 첫 결과를 회신하십시오.
상세: local exchange `fix/carryover-followup-20260914`의 `5191290dc5da82cda433cab7d65e8c74e7dc483b`, `docs/tasks/R134-ROOT-REVIEW-AND-HANDOFF.md` 마지막 절. 기존 원본·운영·현재 UAT는 계속 보존합니다. 각자 전용 큐 worktree에서 수신·결과 참조만 남깁니다.

## 회신 (개발, 2026-09-14 11:49 KST) — 첫 실행 결과 · **실행 슬롯 반환**

local exchange `dev/r134-carryover-followup-20260914` = **`8fa51f4`**. 상세 `R134-DEV.md` §14~18, 변경대장 §6 실제 결과 갱신.
전용 VM `cams-r134` / DB `cams_night_r134_dev_20260914` / Odoo 1536·PG 512. 기본 context·default VM·기존 컨테이너 무변경(읽기 확인).

| 실행 | SHA | 실제 실행 | PASS/FAIL/ERROR/SKIP | OOM | 원로그 |
|---|---|---|---|---|---|
| 빈 DB `install` + `/gh_vendor_settlement` | `b503e09` | **84** | 82/1/1/0 | 없음 | `artifacts/r134_install_b503e09_20260914T024315Z.log` |
| 고정물 수정 뒤 관련 3 클래스 `update` | `94c77fe` | 21 | 21/0/0/0 | 없음 | `artifacts/r134_update_94c77fe_20260914T024554Z.log` |
| 2트랜잭션 probe (`odoo shell`) | `5a8dc65` | 5 | 5/0/0/0 | 없음 | `artifacts/r134_shell_5a8dc65_20260914T024819Z.log` |

- 설치 실패 2건은 **모두 시험 고정물**: ① 마감 기간 시험이 8월 원 근거 미청구 상태로 잠금일을 넣어 기존 회사 마감 가드에 걸림 → 8월 청구·전기 뒤 잠금. ② 재철회 시험은 거부·감사시각 보존 성립, 메시지만 틀림 → 새 워크플로 잠금 가드가 먼저 거부하므로 중복 검사 삭제. 제품 로직 변경 없음. 84건 전체 반복 안 함(82건은 b503e09 통과 그대로).
- shell 디스패치: 지적대로 `shell` 을 이미지 바로 뒤로 옮김(`cli/command.py:54` 확인). 첫 probe 시도(`27b2317`)는 **exit 0 이었으나 결과 행 없음** — 구동부 누락. 성공으로 세지 않고 `5a8dc65` 에서 `run(env)` 구동·실패 시 비정상 종료·실행기의 결과 행 게이트(없으면 exit 3)를 넣고 재실행.
- probe 5/5: 초과·부분·같은 창 재시도·철회↔직접 상신 두 방향. 각 시나리오 실제 `pg_blocking_pids` 잠금 대기 관찰, 패자 40001, 재시도는 각 가드에서 거부, 원 행 불변.
- 미검증: 화면 시험, 운영 복제 DB 이행·복구, 독립 테스트, 아스트라 통합 검토.
- **슬롯 반환합니다.** DB·볼륨·원로그 보존, 전용 daemon 잔여 컨테이너는 PG 하나. 독립 테스트가 별도 DB 에서 이어받으면 됩니다.
