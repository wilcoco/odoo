# PR #10 차단 7건 반영 회신 (2026-09-22, 저자: Claude 개발)

상태: 회신

수신: 서버 담당(원도영) · 참조: 홍정수
회신 대상: `20260921-06_to-개발_from-서버_PR10_수정요청_리허설대기_및_운영연동회신.md`
PR #10 head: **4a4be9a952d9481b09fcdfa68ef6c51b3644ee46** (base 미러 main ce759f5). 정본 반영: odoo-uat main 01e334e.
검증: 가짜 docker 하네스로 확인 모드 기동·업그레이드 오류 중단·재기동 후 로그 이상(코드 3)·업무 재개(cron 복원+재개 명령)·롤백(버전 diff 판정) 5경로 통과, `bash -n` 8개 통과. 실제 컨테이너 첫 검증은 리허설입니다.

| # | 지적 | 처리 | 확인 방법 |
|---|---|---|---|
| 1 | 파일 4개 삭제 | 복구(ecdccb1). 원인은 제 미러 작업 폴더 상태 불량 — `git add -A` 가 사라진 파일을 삭제로 잡음. 이후 커밋 전에 `git diff --cached --name-status origin/main | grep ^D` 로 의도 외 삭제 0건을 확인하는 절차를 넣음 | `git diff --name-status ce759f5 4a4be9a | grep ^D` → cams_sq_dashboard 뿐 |
| 2 | rehearse DB_NAME 미정의·자원 생성 전 검증 | `db_name="${DB_NAME:-odoo18}"`; 자원 생성 전에 SHA 길이·덤프·tgz(안에 `<db>/` 폴더)·이미지·ENTERPRISE_DIR·PROD_CONF·운영 경로 아래 금지·이름 충돌·여유 디스크(MIN_FREE_GB 기본 20) 검증. 실패 시 만든 자원을 `run_<ts>.env` 에 기록하고 종료(정리는 replica_cleanup) | `DB_NAME` 없이 실행 → 기본값 사용; 덤프 없이 실행 → "사전 검증 실패" 종료 2 |
| 3 | 갱신 코드 권한 | `release_core` 가 git ff 구간만 umask 022, 갱신 파일·디렉터리 `chmod a+rX`, 그 뒤 컨테이너 사용자(100:101)로 `docker run --user 100:101 ... test -r` 실제 읽기 검증. 실패 시 업그레이드 전에 중단 | 하네스에서 갱신 매니페스트 644 확인 |
| 4 | 외부 송신 격리 | bridge 연결 제거. 복제본 Odoo 도 internal 네트워크만, 포트 공개 없음(호스트에서 컨테이너 IP 로, SSH 터널 안내). 복원 직후 `pg_foreign_server` 전부 host 127.0.0.1 port 1 로(FDW 가 운영 DB·Oracle 로 못 나감). 새 `replica_isolation_check.sh <ts>`: internal 확인, 다른 네트워크 미연결 확인, 운영 DB·MQTT·운영 API·10.10.21.20·FDW 호스트·인터넷 연결 시도가 **전부 실패해야** 통과 | 스크립트 결과 파일 `r145_replica_isolation_<ts>.txt` |
| 5 | 재기동/업무 재개 통제 | 확인 모드 = (a) 백업 직후 활성 cron 목록 기록 후 전부 비활성, (b) `BLOCK_CMD`(기본 `docker stop proxy-nginx go_relay`)로 사용자·API(미들웨어 포함)·MQTT 차단, 재기동 뒤에도 유지. `release_resume.sh <ts> <승인자>` 가 cron 복원 + `RESUME_CMD` + 기록. **go_relay 만으로 사출 입력이 막힌다는 전제 삭제** — 미들웨어가 프록시를 거치지 않고 Odoo 포트에 직접 붙으면 그 경로를 BLOCK_CMD 에 넣어야 하며, 이는 오늘 실물 확인 항목입니다. 코드 3 메시지는 "확인 모드 유지(운영 업무 미재개)" 로 정정 | 하네스: 재기동 전 `docker stop proxy-nginx go_relay`·`update ir_cron set active=false`, 재개 시 `set active=true where id in (기록)`·`docker start` 확인 |
| 6 | 절차서 정정 | 상단 상태를 "최종 SHA 는 PR #10 머지 후" 로, §7.5 `-i 2`, §7.4 = 실연동(운영 확인 모드에서만) / §7.5 = 복제본 모의(격리, fake_plc·시험 행·크론 수동 실행) 분리, 크론 active 만 켜도 자동 실행 안 됨 주석, 재기동≠업무 재개 | odoo-uat `docs/운영반영-절차서-2026-09.md` |
| 7 | 마스터 추출 개인정보 | 방식 변경: **서버 안에서 중립화 뒤 전달**. `scripts/uat/master_export_neutralized.sh`(운영 DB 는 읽기만) — 마스터 테이블 덤프 → 같은 PG 의 임시 DB 복원 → 개인 파트너 삭제·연락처/주소/세무 제거·임직원 가명화 → 중립화 덤프 재생성 → 임시 DB·원본 덤프 삭제·SHA256. schema 파일은 FDW·user mapping·password 줄 제거. 전달물: `uat_master_neutralized.dump`, `schema_master_tables.sql`, `neutralize_check.txt`, `SHA256SUMS` — 승인된 제한 공유로만 | `neutralize_check.txt` 의 개인 파트너 0·임직원 이메일 0 |

큐 `20260921-02` 의 추출 절차는 이 스크립트로 대체합니다(개인정보 원본 전달 없음).

## 다음
PR #10 최신 head 재검토 → 머지 → 최종 40자리 SHA 회신 → 그 SHA 로 리허설 3단계(+격리 검사). 리허설에서 스크립트 문제가 나오면 이 큐로. 오늘 시사출 관찰·실물 확보(`20260921-03`, `-04`)는 그대로입니다.

---

## 회신 (서버, 2026-09-22 KST)

운영 `odoo18` 원본 백업을 완료했고, 중지 시작부터 nginx 경유 웹 복구 확인까지 약 16초였다. 첨부파일 632개 및 덤프·아카이브·체크섬 검사를 통과했다. PC 사본은 복사 중이며 실제 복원 리허설은 아직 수행하지 않았다.

현재 head `4a4be9a952d9481b09fcdfa68ef6c51b3644ee46`의 중립화 추출 스크립트에서 SQL 전달 `docker exec -i` 누락, 복원 오류 무시, 고정 임시 DB 선삭제, 잔존 정보 검사 실패 시 중단 조건 누락을 확인했다. 중립화 추출·전달과 PR 머지·최종 SHA 확정은 보류한다.

개발자 수신 큐에 [20260922-02 — 운영 백업 완료 및 중립화 추출 수정 요청](20260922-02_to-개발_from-서버_운영백업완료_및_중립화추출_수정요청.md)을 게시했다. 해당 문서의 코드 위치·수정 조건·검증 기준을 확인하고 정본/미러 커밋 및 결과를 회신해 주기 바란다. 원본 덤프·설정·자격증명은 큐에 첨부하지 않았다.
