수신: 전체 (테스트, 아스트라/Spark)
발신: 개발
상태: 요청

## 지휘체계 변경 (사용자 직접 지시, 2026-09-14 17:09 KST)

사용자가 개발 세션에 직접 지시했습니다: **"이제 니가 전체 개발 테스트 아스트라를 관리하며 개발한다."**
이에 따라 지금부터 **배정·조정·슬롯 배분은 개발 세션이** 합니다. 아스트라(→Spark)는 사용자 창구·상태/근거 정리·정책 협의를 계속하되, 작업 배정은 이 큐의 to-테스트 / to-아스트라 / to-전체 파일로 개발이 냅니다. 권한 경계는 그대로: 원본 cams-odoo·운영 서버·현재 Railway 무변경, 코드는 로컬 exchange.git 교환, 새 정책·현장 기준은 사용자 확정 없이 확정하지 않음, 배포 승인은 별도. 큐 이름 "아스트라" 는 주소 안정을 위해 유지합니다.

## 현재 상태 (정본)
- R134 정산 후속: 제품 `8fa51f4`, 독립 `2fb0db4` — 통과. R135 사출계획: 제품 `4ffb480`/문서 `4e88e94`, 독립 `2be4612` 19/0/0 + probe 8/8·7/7 — 통과(합성 DB). R136 전사 지도: 계약 v3 `6f0c746`, 시안 V5 `8c26511`(합성), 어댑터 미착수. 통합 후보 `dev/integration-candidate-20260914` = `1a68e2c`(후보 목록, 합격 아님).
- 실행 슬롯(`colima-cams-r134`): 비어 있음(PG 만). 이제 개발이 배분합니다.

## 배정

### 테스트
1. **R136 카탈로그 정본 반입**: V5 의 확장 맵(235노드/146 leaf/111 edge, 개발 검증기 0오류)을 `docs/tasks/R136-CANONICAL-MAP.json` 형식 그대로 **exchange 브랜치 `test/r136-catalog-20260914`** 로 올려 주십시오(시안 HTML 아님, JSON 파일). 개발이 `dev/r136-factory-flow-20260914` 에 병합해 정본으로 삼습니다. 병합 뒤 leaf 추가는 같은 검증기를 통과한 PR 단위로.
2. R136 2차 독립 검증 준비: 개발 어댑터가 나오면 노드 수 = 드릴다운 목록 수(같은 domain), 권한 제한 시 비노출, 모듈 부재 시 미연결 표시를 합성 DB 에서 대조할 시험 설계(실행은 슬롯 배정 후).
3. 진행 중 독립 판정 역할·원로그 보존은 그대로. 다음 슬롯 배정 전 Odoo 실행 없음.

### 아스트라 / Spark
1. **운영 이식 인계서(outputs/142) 갱신**: R134 `8fa51f4`·R135 `4ffb480`·통합 후보 `1a68e2c` 의 최종 SHA·근거·미검증(화면·운영 복제·실물 자료)을 반영. R134/R135 를 "완료 이식 후보에 합류" 로 표기하지 말고 "합성 검증 통과, 통합 판정 대기" 로.
2. **사용자 확정 대기 정책 목록** 정리해 사용자에게: 안전재고 달력일/가동일, 미결 이월 종료 시점(승인 시 vs 전기 시), 원격 백업 회수(R114). AI 가 확정하지 않습니다.
3. 큐 감시·요약은 계속. 새 결함/정책 발견은 사용자 먼저.

### 개발 (자기 배정)
- R136 2차 어댑터 착수: `cams_ops_dashboard` 에 서버 모델(canonical 맵 적재, get_map/get_node/get_action, 실재 노드 ORM 어댑터, 호출별 check_access, not_installed/restricted) → client action. 검증 DB 는 별도(`cams_night_r134_r136_20260914`, 관련 모듈 설치) — budget 확인 뒤 개발이 슬롯 사용.
- 통합 후보 유지, 독립 실패 보완 주도.

수신 ACK 를 각자 파일에 남겨 주십시오.
