# R145 복제본 리허설 — 검토 결과 및 실행 대기 계획

- 작성: 2026-09-21 15시 KST, 서버 담당 측 검토
- 판정: **BLOCKED / 실제 컨테이너 리허설 미실행**
- PR: https://github.com/escon-odoo/odoo_gh/pull/10
- 검토 의견·브랜치 보호 미설정 결정 댓글: https://github.com/escon-odoo/odoo_gh/pull/10#issuecomment-5756079365 (2026-09-21 15:03 KST 등록)
- 확인한 head: `78b84119f7312ddebe65974584bf4e6f6b142626`
- 확인한 미러 main: `ce759f5da91ac215e2fd8531e8f6a32376264825`
- 승인된 최종 릴리스 SHA: 미확정. PR head를 머지 완료 SHA로 사용하지 않는다.
- 정본 절차서: `wilcoco/odoo-uat main:docs/운영반영-절차서-2026-09.md`, 확인한 파일 blob `184849e8c583f7e4a0b7549236399e7dec838338`.
- 인계 큐 확인 기준: `wilcoco/odoo handoff` 커밋 `a833ffd44dfe938168e92f1679773aabb99f8a54`, 큐 02~05.

## 1. 이번에 한 일과 하지 않은 일

원격 PR·정본 절차서·인계 큐를 읽고, 별도 검토 checkout에서 파일을 대조했다. WSL의 합성 Git 저장소로 권한 문제를 재현하고 셸 문법을 검사했다. 사용자 작업 브랜치는 변경하지 않았다.

PR 머지, 운영 SSH 접속·변경, 운영 배포, DB 추출·전달, 실제 복제본 생성, 자격증명 교체는 하지 않았다. 기존 localhost:8069도 변경하지 않았다. `flushing/20260918`을 미러 main에 병합하지 않았다.

정적 검토·합성 재현의 성공은 Odoo 업그레이드·복원 리허설 통과가 아니다. 실제 컨테이너 업그레이드 로그·화면 검증·롤백 증적은 아직 없다.

## 2. PR 머지 전 해결할 사항

| 항목 | 현재 확인한 근거 | 필요한 수정·검증 |
|---|---|---|
| 필수 모듈 파일 삭제 | head에서 `odoo_plugins/escon_employee/__manifest__.py`, `worker_migration.py`, `iatf_plugins/iatf_menu/__init__.py`가 없음. `injection_worksite/__manifest__.py:24`는 여전히 escon_employee 의존, `escon_employee/hooks.py:23`은 worker_migration import | 삭제 의도 확인 및 필요한 파일 복구. 실제 addon 검색 경로에서 의존성·import 재검증 |
| DB_NAME 미정의 | `rehearse.sh:39`의 `$DB_NAME`, `set -u`, 기본값 선언 없음. 재현 종료 코드 127 | 자원 생성 전 기본값·인자 검증. 실패 시 생성 자원 기록·정리 경로 제공 |
| 코드 갱신 후 권한 | `rehearse.sh:55` 권한 부여 후 `release_core.sh:33`에서 umask 077, `:114`에서 Git 갱신. 합성 Linux 재현에서 변경 파일 600, 신규 디렉터리 700, UID 100 읽기 실패 | 갱신 후 Odoo UID로 전체 대상 addon 접근 재검증. 코드와 비밀 설정의 권한을 구분 |
| 외부 통신 격리 해제 | `rehearse.sh:82`에서 Odoo를 기본 bridge에도 연결 | 외부 egress 차단을 유지하면서 로컬 UI 접근을 제공. 운영 DB·FDW·Oracle·MQTT·프린터/API로 통신 불가를 검증 |
| 업무 재개 통제 불충분 | `release_core.sh:152` 이후 go_relay만 중지하고 일반 설정으로 Odoo 기동. cron·사용자·별도 사출 미들웨어 입력의 강제 차단은 없음 | 실제 입력 경로별 중지/차단·재개 절차와 승인 시점 명시. go_relay만으로 사출 완료 입력을 막는다는 전제 수정 |

사출 완료 흐름은 서버 담당 설명상 `취출로봇 인식 → PLC 업체 푸시 → 미들웨어 → /api/plc/complete`다. go_relay는 이종검사용 MQTT 중계로 설명받았다. 실제 발신 장치/IP·미들웨어 호스트는 별도 실측 대상이며, 위 정보는 이번 로그 분석으로 확인한 결과가 아니다.

원문 절차서에도 다음 정리가 필요하다.

- 상단의 미러 main·PR #9 상태와 마지막 잔여 항목에 과거 상태가 섞여 있다. 최종 SHA 기준으로 통일한다.
- §7.5의 `-i 3`은 현재 `release.modules`의 신규 설치 2개와 다르다.
- §7.4의 운영 MQTT·Oracle·사일로 실연동과 격리 복제본의 모의 검증을 구분한다. 운영 대상에 연결하지 않은 항목을 실연동 통과로 적지 않는다.
- `--max-cron-threads=0` 실행에서는 UI에서 cron active만 바꿔도 자동 실행이 시작되는 것은 아니다. 격리된 수동 시험 방법을 정한다.
- 재기동/업무 재개가 분리됐다는 설명과 런타임 오류 시 “운영은 재개됨”이라는 메시지를 실제 통제 상태에 맞춘다.

기본 bridge 연결은 외부 네트워크 접근을 제공할 수 있으며 localhost 포트 바인딩은 외부 송신 차단과 다르다. [Docker bridge 문서](https://docs.docker.com/engine/network/drivers/bridge/)

## 3. 리허설 환경 준비 상태

2026-09-21 15:01 KST에 이 PC의 Ubuntu-22.04 WSL에서 조회했다.

```text
2026-09-21T15:01:17+09:00
inactive
docker_socket_absent
State  Recv-Q Send-Q Local Address:Port Peer Address:PortProcess
LISTEN 0      48           0.0.0.0:8069      0.0.0.0:*    users:(("python",pid=17556,fd=6),("python",pid=17554,fd=6),("python",pid=17553,fd=6),("python",pid=17552,fd=6),("python",pid=17550,fd=6),("python",pid=17548,fd=6),("python",pid=17540,fd=6))
State Recv-Q Send-Q Local Address:Port Peer Address:PortProcess
```

위 출력은 `date --iso-8601=seconds`, `systemctl is-active docker`, Docker 소켓 존재 검사, `ss -ltnp sport = :8069`, `ss -ltnp sport = :8169` 순서다. Docker 데몬은 이 WSL에서 비활성이고, 8069는 사용 중이며 8169의 수신 소켓은 발견되지 않았다. 이 결과가 PC의 모든 Docker context 부재를 의미하지는 않는다.

계획은 기존 8069를 유지하고 별도 복제본을 127.0.0.1:8169에 두는 것이다. 별도 Linux 경로·PG 컨테이너·파일스토어·네트워크를 사용한다. 기존 테스트베드 DB/파일스토어를 덮어쓰지 않는다.

아직 지정·검증되지 않은 입력:

- 운영과 동등한 Odoo 이미지 및 고정 digest, PostgreSQL 17의 실제 이미지 버전/digest
- Enterprise addon 사본 및 그 버전, 복제본 전용 설정
- 일관된 DB 덤프와 파일스토어 쌍, 덤프 생성 시각·운영 코드 SHA·해시
- 아카이브 내부 경로, 소유권, 빈 파일 여부 및 복원 가능성
- 생산 연동 비활성화/중립화 항목, FDW를 포함한 외부 송신 차단 검증 방법

큐 02의 마스터 일부 테이블 `--data-only` 덤프는 전체 업그레이드·복원 리허설의 입력을 대체하지 않는다. 새 백업을 위해 운영 서비스를 중지해야 한다면 이번 복제본 작업과 별도 승인·일정을 먼저 정한다.

## 4. 수정본 승인 후 실행 순서 — 현재 실행 금지

1. 개발자가 정본에서 수정하여 PR #10에 반영한다. 최신 head 전체 diff, 삭제 파일, 의존성, 실패 경로를 다시 검토한다. CI queued는 통과가 아니다. 실제 테스트 결과 또는 명시적인 대체 검증 근거를 남긴다.
2. 안전성 검토 통과 후 PR만 머지하고 40자리 최종 SHA를 기록한다. `flushing/20260918`은 병합하지 않는다. 운영 checkout은 변경하지 않는다.
3. 해당 SHA의 스크립트를 운영 코드 및 사용자 작업 checkout 밖에 준비한다. 준비한 도구 SHA와 실행할 릴리스 SHA를 각각 기록한다.
4. 위 입력과 격리를 검증한다. 리허설 Docker 런타임을 준비하되 기존 Odoo/PG를 중지·재사용하지 않는다.
5. 수정된 스크립트 계약에 따라 아래 형식으로 실행한다. 실제 값이 채워지고 차단 사항이 해소되기 전에는 복사 실행하지 않는다.

```text
환경: ODOO_IMAGE, PG_IMAGE, ENTERPRISE_DIR, PROD_CONF,
      DB_NAME=odoo18, REPLICA_BASE=<전용 Linux 경로>, REPLICA_PORT=8169
scripts/release/rehearse.sh <승인된 최종 40자리 SHA> <검증된 전체 DB.dump> <대응 filestore.tgz>
```

6. 업그레이드 22개와 신규 설치 `mrp_bom_scan_guard,escon_br_intake`의 종료 코드, 로그, installed 상태와 매니페스트 버전을 확인한다. 금지 모듈이 설치되지 않았는지 확인한다.
7. 복제본에서 로그인·사출 계획/MO·BR·작업자·정산·권한·첨부파일을 확인한다. 합성 PLC 신호의 중복 방지와 사일로 데이터 모의 검증을 수행한다. 시험 대상 URL/DB를 실행 직전 확인한다. 운영 장치/서버에는 시험 요청을 보내지 않는다.
8. 복제본 전용 실행 기록과 대상 경로/컨테이너를 확인한 뒤 `rehearse_rollback.sh <ts>`로 복원을 시험한다. 이전 코드·DB 모듈 버전뿐 아니라 이전 코드의 실제 기동·로그인·첨부 조회도 확인한다. 현재 스크립트 출력만으로 이 항목이 자동 완료됐다고 보지 않는다.
9. 증적을 복제본 경로 밖에 보존한다. 정리는 대상 목록을 재확인한 후 실시한다. 증적 보존 전 `replica_cleanup.sh`를 실행하지 않는다.
10. 통과/실패/미시험을 분리해 보고한 뒤 운영 반영 일정을 별도로 승인받는다. 리허설 성공은 운영 배포 또는 업무 재개 승인이 아니다.

## 5. 이번 검증 출력

실행: `artifacts/r145-pr10-review-20260921/probe.sh`를 WSL root에서 실행. CRLF는 메모리에서 제거하여 bash 문법을 검사했다. 원본 릴리스 파일은 수정하지 않았다. 합성 저장소만 생성했으며 Odoo/DB/Docker 실행은 없다.

```text
PR10 script syntax (CRLF normalized in memory)
PASS rehearse.sh
PASS rehearse_rollback.sh
PASS release_apply.sh
PASS release_core.sh
PASS release_resume.sh
PASS release_rollback.sh
PASS replica_cleanup.sh

DB_NAME reproduction: same release.modules and nounset, no docker executed
bash: line 1: DB_NAME: unbound variable
DB_NAME_unset_exit=127

Native Linux git checkout permission reproduction (synthetic fixture, no Odoo/DB)
Updating ac1c92e..c5d28e6
Fast-forward
 addons/module/model.py     | 2 +-
 addons/new_module/model.py | 1 +
 2 files changed, 2 insertions(+), 1 deletion(-)
 create mode 100644 addons/new_module/model.py
600 0:0 /tmp/r145-pr10-permission.D35KoQ/addons/module/model.py
700 0:0 /tmp/r145-pr10-permission.D35KoQ/addons/new_module
600 0:0 /tmp/r145-pr10-permission.D35KoQ/addons/new_module/model.py
Odoo_UID_100_changed_file_read_exit=1
Odoo_UID_100_new_file_read_exit=1
Synthetic evidence retained at /tmp/r145-pr10-permission.D35KoQ
```

위는 재현 출력 중 문법·DB_NAME·권한 검증 구간의 원문이다. 뒤에 출력된 소스 grep 구간은 §2의 파일·줄 번호로 별도 정리했다. 프로브의 성공은 두 결함을 예상대로 재현했다는 뜻이며 릴리스 성공이 아니다.

## 6. 최종 결과 기록 양식 — 아직 미실행

| 항목 | 이번 상태 |
|---|---|
| PR 머지 SHA | 미확정 |
| CI | tests queued, 성공 아님 |
| 복제본 실행 시각/실행자/ts | 미실행 |
| 이미지 digest·덤프/파일스토어 해시 | 미확정 |
| 업그레이드·설치·로그·모듈 버전 | 미시험 |
| 화면·권한·합성 장비·외부 통신 차단 | 미시험 |
| 복원 및 이전 코드 기동·첨부 조회 | 미시험 |
| 운영 반영/업무 재개 승인 | 없음 |

외부 전달 자료에는 자격증명·개인정보를 포함하지 않는다. 필요한 원문 로그는 제한된 위치에 보관하고 공유본은 가림 처리 사실을 표시한다.
