# 작업 기록

브랜치 하나당 문서 하나를 코드와 같은 브랜치에 둔다.

- 브랜치 `feat/quality-gate` → `docs/tasks/작업/feat/quality-gate.md`
- 브랜치 `security/portal-token` → `docs/tasks/작업/security/portal-token.md`

`TEMPLATE.md`를 복사해 시작하고, PR 전 실제 검증·동기화·배포 정보를 채운다. 문서만을 위한 브랜치는
만들지 않는다. 상세 규칙은 `../AGENT_MERGE_WORKFLOW.md`를 따른다.
