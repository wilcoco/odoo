# 공통 개발 스킬 추가 — 기존 문서 이관 작업에 포함

## 요청과 범위

- 근거: 2026-09-10 사용자가 전달한 원도영 서버 담당의 개발·머지·배포 절차 및 공통 스킬 명시 요청.
- 기존 범위: `9811f5b3af88acfc4de101075f13f277f93baff2`의 문서 이관. 이 추가 작업에서는 기존 이관 문서 내용을 수정하지 않았다.
- 추가 범위: 공통 스킬, 작업 기록 양식, Codex/Claude의 원문 참조 지침 작성과 공유.
- 사용자 추가 확정: 원도영님도 개발할 내용을 Git에 등록하고, 아스트라가 접수해 각 AI에 배정한다. 이에 따라 중앙 접수·배정·결과 회수와 배정 기록 필드를 명시했다. 기존 사용자 선보고·범위 협의 원칙과 원본 저장소 구분은 유지한다.
- 담당: 아스트라. 독립 문구 검토: 별도 검토 에이전트.
- 작업 저장소/브랜치: `wilcoco/odoo`, 기존 `docs/dev-handoff-20260910`.
- PR 대상: `18.0`. 신규 문서 전용 브랜치를 만들지 않고 기존 1회성 이관 작업에 포함한다.

## 변경 결과

- `.agents/skills/cams-odoo-delivery/SKILL.md`: 사용자 접수 협의, 작업 분담, 모듈별 원본 확인, 코드·문서 동반 PR, 검증 근거, 정본 동기화, 운영 인계, 시험 DB, handoff 통신 원칙.
- `.agents/skills/cams-odoo-delivery/assets/task-record.md`: 요청부터 검증·통합·운영 결과까지 연결하는 공통 양식.
- `.agents/skills/cams-odoo-delivery/agents/openai.yaml`: 스킬 표시 정보.
- `AGENTS.md`, `CLAUDE.md`: 같은 스킬 원문 참조. 기존 CLAUDE 기술 설명은 보존했다.
- `.gitignore`: 이 공통 스킬 경로만 추적하도록 예외를 추가했다. 그 밖의 로컬 에이전트 파일은 계속 제외한다.

MES 자체 코드와 ERP 배포 복제본을 구분하고, ERP 원본 SHA와 배포 SHA를 연결하도록 했다. 수정된 로컬 DB를 운영 DB로 대체하지 않는 원칙을 명시했다. 새 수정본이 도착했다는 사실과 본격 검토·수정의 합의를 구분했다.

## 검증 근거

- 기본 형식 검증: `python3 /Users/jhungsoohong/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/cams-odoo-delivery` → `Skill is valid!` (검증 도구는 작성 환경의 시스템 스킬에 포함).
- 독립 검토: 새 수정본만 도착한 경우, 승인한 코드의 시험 후 기록만 추가하는 경우, 복제 경로에만 모듈이 있는 경우를 대조했다. 사용자 선보고, 불필요한 재승인·재시험 방지, 원본 확인 전 삭제 금지가 일관되는 것을 확인했다.
- 지침 참조와 양식 링크 존재, YAML 파싱, 기존 CLAUDE 본문 보존, `git diff --check`를 확인했다.
- 중앙 배정 원칙 추가 후 스킬 형식과 diff를 다시 확인했다. 독립 검토에서 중앙 접수·배정의 일관성, 사용자 선보고·범위 협의 유지, 저장소·운영 권한의 무단 확대가 없음을 확인했다.
- 제품 코드·DB·모듈·실행 설정 변경이 없어 Odoo 실행 시험은 해당 없음. 이 검증은 문서와 스킬 자체에 한정되며, 기존 이관 문서의 기능·운영 준비도 주장을 재검증한 결과가 아니다.

## 적용과 남은 단계

- 이 브랜치에서 지침 참조를 추가했다. 통합 브랜치와 다른 개발자의 작업 공간에 적용되려면 PR 병합 및 해당 변경 수신이 필요하다.
- MES 저장소의 루트 지침은 이번 작업에서 수정하지 않았다. 담당자는 이 공통 원문을 참조하고, 해당 저장소의 다음 관련 작업에 참조 지침을 함께 반영한다.
- PR 병합·배포 정본 동기화·운영 반영은 미실행. 제품 결함의 후속 수정·배정은 별도 합의 범위를 따른다.
- handoff에는 완료 요약과 이 브랜치·커밋·원문 경로만 공유한다.
