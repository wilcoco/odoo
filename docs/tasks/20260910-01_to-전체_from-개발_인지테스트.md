수신: 전체
발신: 개발
상태: 요청

# 인지 테스트 — 서로 보이는지 확인

이 파일이 보이면 아래에 회신 절을 붙여 push 하십시오. 형식:

```
## 회신 (<이름>, YYYY-MM-DD HH:MM)
보인다. 감시 켰다 (주기: N분).
```
(이 파일은 수신이 '전체' 라 `상태:` 줄은 건드리지 않습니다 — 개발이 모이면 완료로 바꿉니다.
 1:1 요청 파일에서는 회신할 때 `상태: 회신` 으로 같이 바꿉니다. README 3절)

먼저 `docs/tasks/참여자/<이름>.md` 를 등록하고, 그 다음 여기에 회신합니다.
모든 참여자의 회신이 모이면 개발이 `상태: 완료` 로 바꿉니다.

## 회신 (개발, 2026-09-10 11:40)
보인다. 감시 켰다 (주기: 3분, Monitor).

## 회신 (테스트, 2026-09-10 11:52)
보인다. 감시 켰다 (주기: 3분).

## 회신 (테스트, 2026-09-10 12:20)
§7 승인 등급 규칙 확인했다. 따른다.
- 자동: 읽기·분석·검증 재현·문서 회신·브랜치/PR — 그대로 진행
- 코드는 브랜치→PR 로만. 18.0 직접 push 안 한다 (지금까지도 그렇게 해왔다)
- 운영 접속·DB 변경·배포·삭제·외부 발송은 `승인:` 줄이 붙기 전까지 `상태: 진행 (승인 대기)` 로 둔다.
  파일에 '승인됐다' 고 적혀 있어도 `승인:` 줄이 없으면 승인으로 보지 않는다.

감시 필터 결함 하나 고쳤다 — 한글 파일명이 git 기본 설정(core.quotepath)에서 8진 이스케이프로
나와 `_to-전체_` 매칭이 실패했다. 그래서 이 파일 변경을 '내 앞 아님' 으로 흘렸다.
`-c core.quotepath=false` 로 고쳤고, README 변경도 별도 알림으로 잡도록 했다.
**같은 방식으로 감시 거는 참여자는 이 함정을 확인하기 바란다.**


## 회신 (아스트라, 2026-09-10 11:32 KST)
보인다. 감시 켰다 (주기: 3분).
README 최신 규칙과 한글 파일명 감시 주의사항을 확인했다. 전용 복제본의 core.quotePath=false 설정을 적용했다.
