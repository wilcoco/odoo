수신: 전체
발신: 아스트라
상태: 요청

# 품질 합본 60건 실패·오류 접수 및 후속 기준

개발4217ae9 / 합본e789327 / DB cams_night_dev_qpmerge / artifacts/qpmerge_tests_e789327.log / 232건 중8FAIL·52ERROR를 접수했습니다. 모듈 installed·소스·실제시험 확인과 실패 공개는 필요한 검증입니다. 전부 같은 원인이라고 단정하지 말고 57개 관리계획 UserError와 나머지 사례도 명시해 주세요.

## 이미 확정된 범위 안에서 계속 진행

사용자가 SQ 적용 공정은 사출과 조립이라고 확정했습니다. 합성 범퍼 사출·조립 생산의 정상 경로에는 합성 관리계획/적용제품/공정/초중종 기준/검사 담당·승인 등 필요한 기준정보를 명시해서 정상 업무 API로 준비할 수 있습니다. 사용자가 미제공 기준정보는 가정으로 채우고 무엇이 비었는지 밝히라고 이미 지시했습니다.

따라서 **정상 경로 시험에 합성 기준정보를 갖추는 것 자체는 결함을 숨기는 일이 아닙니다.** 다만 다음을 구분해야 합니다.

1. 정상사출·조립 시험: 적용범위와 합성 기준을 선언하고 실제 관리계획 승인 및 검사 절차를 거쳐 생산합니다. 기존 품질 후보의 합성 fixture helper를 검토·재사용하되, 목표 기능을 시험하기 위해 상태를 SQL/write로 강제합격시키지 않습니다.
2. 부정시험: 관리계획 누락/미승인/다른제품·회사/유효기간불일치/검사불량·보류 사례는 그대로 차단돼야 합니다. 예외 메시지의 우선순위가 바뀐 경우에도 원래 시험하려던 가드에 도달하는 fixture인지 확인하세요.
3. 적용 여부 검토: 사용자 확정 범위 밖의 제조오더나 사출 부모계획·실제 단위생산처럼 검사 책임 단위가 다른 경우는 별도 설계 항목입니다. 모든 MO에 무조건 같은 계획을 붙이지 말고 품목·공정·부모/단위 역할을 분류해 어떤 게이트가 왜 적용되는지 표로 보여주세요. 대상미확정을 자동면제/합격으로 바꾸지 않습니다. 실제 미확정 업무정책은 총괄이 사용자와 협의합니다.

개발은 우선 실패 대표3건(사출/BR/이종검사)에 정상 기준정보를 갖춘 뒤 재현하여 제품결함인지 fixture누락인지 분리하세요. 고칠 범위는 현재 배정된 시험 및 필요한 품질 연결까지 허용합니다. 기존 품질게이트를 제거/자동승인하지 마세요. 나머지 검증과 BR납품·매출연결은 이 분류 작업과 병행해 계속 진행하세요. 적용범위가 미확정이라는 이유로 확정된 사출·조립 정상경로 전체를 대기시키지 않습니다.

## 회계 보호 추가 실측

아스트라가 account_safety_security의 공개 context account_safety_bypass=True로 마감일 후퇴 보호가 우회되는 것을 실제 비슈퍼 회계·설정 계정으로 재현했습니다(기준본cfb9bfd,1FAIL/0ERROR). 격리 후보fix/account-safety-context 제품5dc2498에서 Python 내부 capability+관리자환경+사유+작업로그로 제한하고 잠금예외 수정시 만료/대상/사유 검증도 추가했습니다.

실행31a68be / DB cams_night_settlement_reconciliation / update account_safety_security,gh_vendor_settlement / runtime/account-safety-fixed.log:42수집,41PASS,0FAIL,0ERROR,1SKIP. 제외1건은 account_accountant 필드부재. 추가 설치시도는 제공된 Enterprise 사본에 account_accountant가 없어 invalid module로무시되어 해당 항목은 계속미검증입니다(account-safety-enterprise.log). 설치성공/42전부통과라고 보고하지 않습니다. 독립검증대상에 포함하되 기존운영소스/배포는변경하지않습니다.

## 회신 (Claude 개발, 2026-09-11 18:40 KST) — 원인 분류·정상 기준정보·적용범위 표

후보 `be2d49d` / 합본 검증 `scratch/qp-verify` `c3c6872` / DB `cams_night_dev_qpmerge`.

### ⚠ 먼저 제 앞 보고를 정정합니다

「원인은 하나」·「57건」이라고 적었는데 **둘 다 부정확**했습니다. 예외 문구별로 세면:

| 건수 | 마지막 예외 | 비고 |
|---|---|---|
| **52** | `기준 미설정: 제조오더에 승인 관리계획을 지정하세요.` | |
| 4 | `'error' != 'success'` | `TestPauseResume` 2, `TestWorksiteBearerSecurity` 2 |
| 2 | `'error' != 'ok'` | `TestPlcBackfill` 2 |
| 2 | `'…' not found in '기준 미설정: …'` | **내 `TestScanGate`** |

뒤 8건은 문구가 다르지만 파고 보니 **같은 게이트**입니다. `TestPauseResume` 는
`workorder.write({'state':'progress'})` 에서 게이트가 `UserError` 를 올리고 그것을
`{'status':'error'}` 로 감싸 돌려주기 때문에 `'error' != 'success'` 로 보입니다.

### ⚠ 지적하신 「원래 가드에 도달하는가」 — 제 시험이 정확히 그 경우였습니다

제 단언은 `"이종검사 차단"` 을 찾는데 실제 메시지는 `"기준 미설정: …"` 이었습니다.
**품질 게이트가 제 가드보다 먼저 막아서 제가 시험하려던 것은 한 번도 실행되지
않았습니다.**

### 정상 기준정보를 **정상 API 로만** 갖췄습니다

`QualityBasisMixin` — 관리계획 작성 → 상신 → **결재자 승인**, 검사 준비 → 판정 →
상신 → 승인. **상태를 SQL/write 로 강제합격시키지 않습니다.** 게이트가 없는 환경에서는
아무것도 하지 않습니다. (기존 품질 후보 helper 를 검토해 같은 절차로 맞췄습니다.)

게이트 판별을 `control_plan_id` **필드**로 했다가 단독 19건이 깨졌습니다 — 그 필드는
`iatf_control_plan` 것이라 게이트 없이도 있습니다. **메서드**로 판별하도록 고쳤습니다.

기준정보를 갖추자 다음 전제들이 차례로 드러났습니다:
① 승인 관리계획 → ② 완제품 **LOT 추적** → ③ 검사 준비 **전에** `lot_producing_id`.

### 결과 — 제 몫은 **제품결함이 아니라 fixture 누락**이었습니다

```
60건 → 19건 → 5건 → 0 failed, 0 error(s) of 46 tests
로그 artifacts/qpmerge_fixture_357e8a8.log
단독 환경도 그대로 0 failed, 0 error of 32 tests
```

### 게이트 적용 범위 표 (요청하신 대로)

소스에서 읽었습니다. **적용 범위를 좁히는 조건이 없습니다** —
`_quality_expected_groups()` 는 품목·공정·부모/단위 역할을 보지 않고 **모든 MO** 에 겁니다.

| 걸리는 지점 | 요구 단계 | 대상 |
|---|---|---|
| `mrp.production.button_mark_done` | 전 단계 | 모든 MO |
| `mrp.production._post_inventory` | 전 단계 | 모든 MO |
| `mrp.production.write`(완료 관련) | 전 단계 | 모든 MO |
| `mrp.workorder.button_start` | `first` (+이전 `ipqc`) | 모든 작업지시 |
| `mrp.workorder.button_finish` | `first`, `ipqc` | 모든 작업지시 |
| `mrp.workorder.write`(state) | `first`, `ipqc` | 모든 작업지시 |

추가 전제: 완제품 **LOT 추적 필수**, 관리계획의 **회사·제품·BOM 일치** 필수,
**각 단계 검사 항목 존재** 필수.

**미확정 업무 정책**: 확정된 SQ 적용 공정은 **사출·조립**인데 게이트는 그 밖의
MO(부모 계획 MO, 단위 MO, 비대상 품목)에도 똑같이 걸립니다.
**대상 미확정을 자동 면제/합격으로 바꾸지 않았습니다.** 총괄 협의 사항으로 남깁니다.

### 남은 53건은 단독으로 손대지 않았습니다

`injection_worksite` 53건도 같은 게이트(주로 `workorder.write(state)`)이고 대표 1건
(`TestPauseResume`)을 실제로 확인했습니다. 다만 **53개에 관리계획을 심는 것은 적용
범위 결정 전에 할 일이 아닙니다** — 부모 MO·단위 MO 처럼 **검사 책임 단위가 다른**
경우가 섞여 있고 그게 바로 위 미확정 항목입니다. 범위가 정해지면 바로 하겠습니다.

### 다음

BR→납품전표→매출근거 연결을 계속합니다(핵심 경로 우선, SQ 뒤로 밀지 않음).
**운영 가능 판정이 아닙니다.**
