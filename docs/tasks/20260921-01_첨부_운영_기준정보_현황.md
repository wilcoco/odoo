# 운영 기준정보 현황 — 읽기 전용 조회 원문 (2026-09-21)

수신: 개발

작성: 서버 담당 측 조회 지원

상태: 운영 조회 완료 / handoff 회신 첨부

## 1. 조회 기준과 범위

- 요청: [20260921-01 운영 기준정보 현황 조회 요청](https://github.com/wilcoco/odoo/blob/30964e4cb8ccf3f85ea8fb41a66f6878e6e90a46/docs/tasks/20260921-01_to-서버담당_from-개발_운영_기준정보_현황조회_요청.md)
- 요청 저장소/브랜치: `wilcoco/odoo` / `handoff`
- 확인한 handoff SHA: `30964e4cb8ccf3f85ea8fb41a66f6878e6e90a46`
- 요청 문서 blob SHA: `1f9abe03a28aee682807fc1cb3eb4c3bd7760ba4`
- 대상: 운영 `escon-server` (`10.10.21.20`), `postgresdb` / `odoo18` / DB 사용자 `odoo`.
- 조회 시간: 2026-09-21 10:32:24~10:34:36 KST. SQL 출력 시간은 UTC(`+00`).
- 실측 운영 코드 SHA: `0b36e7f103149d23251ac3e6bedf2dedc0c2874c`. PR 머지 상태와 운영 배포 상태는 별개이며 PR #10 작업은 수행하지 않았다.
- 모든 DB 세션: `default_transaction_read_only=on`, `statement_timeout=15000`, `lock_timeout=3000`, `psql -X`.
- 주 조회와 보완 조회는 각각 `REPEATABLE READ READ ONLY` 트랜잭션에서 수행. 두 조회가 동일 시점 스냅샷은 아니다.
- 조회 외 데이터 변경·설치·업그레이드·서비스 중지/재기동·서버 파일 생성·Git 변경은 수행하지 않았다.
- 아래 명령의 `COMMIT`은 읽기 전용 트랜잭션 종료이며 데이터 변경을 뜻하지 않는다.
- 결과에는 요청된 운영 품목 코드·카테고리명·작업장 코드·사일로 이름과 업무 건수가 포함된다. 최초에는 공개 여부 확인을 위해 로컬 보관했으며, 2026-09-21 서버 담당 사용자가 본 대화에서 이 파일을 `wilcoco/odoo`의 `handoff`에 올리고 푸시하도록 명시 요청하여 해당 범위로 게시한다. 이는 추가 데이터 공개나 운영 변경에 대한 승인이 아니다.

## 2. 요청 SQL과 다른 점 / 해석 주의

요청 1절의 42개 SELECT 분기 중 사전 스키마 검사에서 확인된 아래 3개를 제외한 39개를 실행했다. 테이블 생성이나 컬럼 추가를 통한 우회는 하지 않았다. 요청 2절의 코드 예시 4개 조회는 조건을 유지하고 15초 제한을 추가했다. 원문 전체 UNION을 실패시켜 반복 실행하지 않고 스키마 존재 여부를 먼저 확인했다.

| 요청 항목 | 실측 | 처리 |
|---|---|---|
| 계약 단가 `vendor_part_price.state` | `state` 컬럼 없음. `price_status` 컬럼 존재 | 원래 상태 분기는 제외. 별도 보완 조회에서 전체 3건, `price_status=firm` 1건 / `provisional` 2건을 기록. 서로 다른 컬럼이므로 원래 `state` 결과인 것처럼 대체하지 않았다. |
| BR 접수 `br_intake` | 테이블 없음, `escon_br_intake` 모듈 등록 행 0건 | 조회 불가(MISSING)로 기록. 접수 업무 건수 0건이라는 의미가 아니며 다른 접수/외부 테이블로 대체하지 않았다. |
| BR 지시 `br_work_model` | 요청한 테이블 없음. 운영 소스 모델 `escon.br.work`, 실제 테이블 `escon_br_work` 확인 | 실제 테이블의 건수 61건을 보완 조회. `escon_br`는 설치 상태이므로 단순 '모듈 미설치'가 아니다. |

추가 주의:

- `GROUP BY` 쿼리는 원본 테이블이 비어 있으면 결과 행 자체가 없다. 관리계획은 별도 전체 COUNT로 0건임을 확인했다.
- 원문의 `active` 필터는 그대로 유지했다. 항목마다 활성/비활성 포함 범위가 다르므로 모든 건수를 '활성 건수'로 해석하지 않는다.
- BOM은 전체 분류 5,052건 중 active=true 516건(일반 조립 1 + 사출 6 + 직서열 509)이다. 비활성 직서열 4,507건 및 비활성 표준 29건도 원문에 포함된다.
- 사출품 코드 예시 SQL에는 `active`와 `ORDER BY`가 없다. 원문대로 실행했으므로 활성 사출품 15건의 대표 표본 또는 재현 가능한 정렬 표본으로 해석하지 않는다.
- `string_agg`는 NULL을 제외하고, 작업장 코드 중복을 제거하지 않는다. 코드 나열 개수와 활성 작업장 82건은 일치할 필요가 없다.
- 예시에 `TEST-INJ-TPO`, 사일로에 `TEST-SILO`가 관찰되었다. 이름만으로 테스트 데이터라고 확정하지 않았으며, UAT 이관 전에 실제 용도와 포함/제외 여부를 업무 담당자가 확인해야 한다.
- 공급사·고객은 각각 `supplier_rank>0`, `customer_rank>0` 집계이다. 한 거래처가 양쪽에 포함될 수 있으므로 단순 합산하지 않는다.
- 코드·명칭 및 건수만 조회했고, 직원명·거래처명·연락처·비밀번호·토큰·계약 금액은 조회하지 않았다.
- 아래 출력은 성공한 실행의 원문이다. 오류 출력을 새로 만들어 넣지 않았고, 실행하지 않은 항목을 0건으로 기재하지 않았다.

## 3. 사전 확인 — 실행 명령

운영 SSH 세션에서 아래 읽기 전용 명령을 표준입력으로 실행했다. 스키마 출력의 `name`은 컬럼명/자료형 메타데이터이며 개인 이름의 값이 아니다.

```bash
set -e
hostname
date --iso-8601=seconds
git -C /db/odoo-root/odoo/git/odoo_gh rev-parse HEAD
docker exec -i -e PGOPTIONS='-c default_transaction_read_only=on -c statement_timeout=15000 -c lock_timeout=3000' postgresdb psql -X -U odoo -d odoo18 -P pager=off -At -v ON_ERROR_STOP=1 <<'SQL'
select current_database(), current_user, current_setting('transaction_read_only'), current_setting('statement_timeout'), current_setting('lock_timeout'), version();
with requested(name) as (values ('product_template'),('product_product'),('product_category'),('mrp_bom'),('escon_bom_management_number'),('escon_bom_version'),('mrp_bom_line'),('car_info_model'),('escon_color_code'),('res_partner'),('vendor_part_price'),('material_ton_price'),('mrp_workcenter'),('injection_mold'),('injection_machine_mold_capability'),('injection_stock_silo'),('stock_warehouse'),('stock_location'),('stock_quant'),('stock_lot'),('hr_employee'),('hr_department'),('res_users'),('iatf_control_plan'),('iatf_incoming_inspection'),('iatf_process_inspection'),('iatf_nonconformity'),('mrp_production'),('injection_planning_run'),('br_intake'),('br_work_model'),('vendor_mrp_accrual'),('account_move'),('iatf_approval_template'),('iatf_approval_request'))
select r.name, coalesce(to_regclass('public.'||r.name)::text,'MISSING'), coalesce(string_agg(c.column_name||':'||c.data_type, ', ' order by c.ordinal_position) filter (where c.column_name in ('active','is_injection_part','complete_name','categ_id','bom_purpose','bom_state','department_id','state','alc','supplier_rank','customer_rank','x_is_injection','product_id','product_tmpl_id','usage','quantity','share','is_injection_mo','move_type','default_code','code','name')), '-') from requested r left join information_schema.columns c on c.table_schema='public' and c.table_name=r.name group by r.name order by r.name;
select name,state,latest_version from ir_module_module where name in ('escon_br','escon_br_intake','injection_planning','injection_worksite','gh_vendor_settlement','escon_bom_util') order by name;
SQL
```

### 출력 원문

```text
escon-server
2026-09-21T10:32:24+09:00
0b36e7f103149d23251ac3e6bedf2dedc0c2874c
odoo18|odoo|on|15s|3s|PostgreSQL 17.6 (Debian 17.6-2.pgdg13+1) on x86_64-pc-linux-gnu, compiled by gcc (Debian 14.2.0-19) 14.2.0, 64-bit
account_move|account_move|name:character varying, state:character varying, move_type:character varying
br_intake|MISSING|-
br_work_model|MISSING|-
car_info_model|car_info_model|alc:character varying
escon_bom_management_number|escon_bom_management_number|department_id:integer, name:character varying, active:boolean
escon_bom_version|escon_bom_version|name:character varying, state:character varying
escon_color_code|escon_color_code|code:character varying, name:character varying
hr_department|hr_department|complete_name:character varying, name:jsonb, active:boolean
hr_employee|hr_employee|department_id:integer, name:character varying, active:boolean
iatf_approval_request|iatf_approval_request|state:character varying
iatf_approval_template|iatf_approval_template|department_id:integer, name:character varying, active:boolean
iatf_control_plan|iatf_control_plan|product_id:integer, name:character varying, state:character varying
iatf_incoming_inspection|iatf_incoming_inspection|product_id:integer, name:character varying, state:character varying
iatf_nonconformity|iatf_nonconformity|product_id:integer, name:character varying, state:character varying
iatf_process_inspection|iatf_process_inspection|product_id:integer, name:character varying, state:character varying
injection_machine_mold_capability|injection_machine_mold_capability|product_id:integer, active:boolean
injection_mold|injection_mold|product_id:integer, name:character varying, code:character varying, state:character varying, active:boolean
injection_planning_run|injection_planning_run|name:character varying, state:character varying
injection_stock_silo|injection_stock_silo|product_id:integer, name:character varying
material_ton_price|material_ton_price|product_id:integer, active:boolean
mrp_bom|mrp_bom|product_tmpl_id:integer, product_id:integer, code:character varying, active:boolean, alc:character varying, bom_state:character varying, bom_purpose:character varying
mrp_bom_line|mrp_bom_line|product_id:integer, product_tmpl_id:integer
mrp_production|mrp_production|product_id:integer, name:character varying, state:character varying, is_injection_mo:boolean, bom_purpose:character varying
mrp_workcenter|mrp_workcenter|name:character varying, code:character varying, active:boolean, x_is_injection:boolean
product_category|product_category|name:character varying, complete_name:character varying
product_product|product_product|product_tmpl_id:integer, default_code:character varying, active:boolean
product_template|product_template|categ_id:integer, default_code:character varying, name:jsonb, active:boolean, is_injection_part:boolean
res_partner|res_partner|name:character varying, complete_name:character varying, active:boolean, supplier_rank:integer, customer_rank:integer
res_users|res_users|active:boolean, share:boolean
stock_location|stock_location|name:character varying, complete_name:character varying, usage:character varying, active:boolean
stock_lot|stock_lot|product_id:integer, name:character varying
stock_quant|stock_quant|product_id:integer, quantity:numeric
stock_warehouse|stock_warehouse|name:character varying, code:character varying, active:boolean
vendor_mrp_accrual|vendor_mrp_accrual|product_id:integer, state:character varying
vendor_part_price|vendor_part_price|product_id:integer, active:boolean
escon_bom_util|installed|18.0.2.5.0
escon_br|installed|18.0.1.6
gh_vendor_settlement|installed|18.0.2.2.0
injection_planning|installed|18.0.1.11.0
injection_worksite|installed|18.0.8.8.0
```

## 4. 주 조회 및 실제 BR 테이블/단가 컬럼 확인 — 실행 명령

```bash
set -e
docker exec -i -e PGOPTIONS='-c default_transaction_read_only=on -c statement_timeout=15000 -c lock_timeout=3000' postgresdb psql -X -U odoo -d odoo18 -P pager=off -At -v ON_ERROR_STOP=1 <<'SQL'
BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY;
select '조회시각', now(), current_database(), current_setting('transaction_read_only'), current_setting('statement_timeout'), current_setting('lock_timeout');
select '품목 전체', count(*) from product_template where active
union all select '품목 사출품(is_injection_part)', count(*) from product_template where active and is_injection_part
union all select '품목 카테고리별|'||coalesce(c.complete_name,'-'), count(*) from product_template t left join product_category c on c.id=t.categ_id where t.active group by c.complete_name
union all select 'BOM 분류|'||coalesce(bom_purpose,'-')||'|'||coalesce(bom_state,'-')||'|active='||active, count(*) from mrp_bom group by bom_purpose,bom_state,active
union all select 'BOM 관리번호 수', count(*) from escon_bom_management_number
union all select 'BOM 관리번호|부서 지정', count(*) from escon_bom_management_number where department_id is not null
union all select 'BOM 배치(Batch)|'||coalesce(state,'-'), count(*) from escon_bom_version group by state
union all select 'BOM 라인 수', count(*) from mrp_bom_line
union all select '차종연결(car.info.model)', count(*) from car_info_model
union all select 'ALC 목록', count(*) from (select distinct alc from mrp_bom where alc is not null) a
union all select '컬러코드', count(*) from escon_color_code
union all select '협력사(공급사)', count(*) from res_partner where supplier_rank>0 and active
union all select '고객(원청)', count(*) from res_partner where customer_rank>0 and active
union all select '톤 단가(material.ton.price)', count(*) from material_ton_price
union all select '작업장 전체', count(*) from mrp_workcenter where active
union all select '작업장 사출기(x_is_injection)', count(*) from mrp_workcenter where active and x_is_injection
union all select '금형', count(*) from injection_mold
union all select '설비능력(사출기×금형×품목)', count(*) from injection_machine_mold_capability
union all select '사일로', count(*) from injection_stock_silo
union all select '원재료(수지) 품목', count(*) from product_template t where active and exists (select 1 from injection_stock_silo s where s.product_id in (select id from product_product where product_tmpl_id=t.id))
union all select '창고', count(*) from stock_warehouse where active
union all select '재고 위치(내부)', count(*) from stock_location where usage='internal' and active
union all select '재고 수량 행(quant, qty>0)', count(*) from stock_quant where quantity>0
union all select 'LOT/시리얼', count(*) from stock_lot
union all select '임직원(활성)', count(*) from hr_employee where active
union all select '부서', count(*) from hr_department
union all select '사용자(내부 활성)', count(*) from res_users where active and not share
union all select '관리계획|'||coalesce(state,'-'), count(*) from iatf_control_plan group by state
union all select '입고검사', count(*) from iatf_incoming_inspection
union all select '공정검사', count(*) from iatf_process_inspection
union all select '부적합', count(*) from iatf_nonconformity
union all select 'MO 전체|'||state, count(*) from mrp_production group by state
union all select 'MO 사출(is_injection_mo)', count(*) from mrp_production where is_injection_mo
union all select '사출 계획 실행', count(*) from injection_planning_run
union all select '정산 원장(accrual)', count(*) from vendor_mrp_accrual
union all select '청구서(공급사 인보이스)|'||state, count(*) from account_move where move_type='in_invoice' group by state
union all select '전표(전기)', count(*) from account_move where state='posted'
union all select '결재 템플릿', count(*) from iatf_approval_template
union all select '결재 요청|'||state, count(*) from iatf_approval_request group by state
order by 1;
select '품목 코드 예시(사출품 5개)', string_agg(default_code, ', ') from (select p.default_code from product_product p join product_template t on t.id=p.product_tmpl_id where t.is_injection_part limit 5) x
union all select '직서열 BOM 품목 코드 예시 5개', string_agg(default_code, ', ') from (select distinct p.default_code from mrp_bom b join product_product p on p.product_tmpl_id=b.product_tmpl_id where b.bom_purpose='vehicle_batch' and b.active limit 5) y
union all select '작업장 코드', string_agg(code, ', ') from mrp_workcenter where active
union all select '사일로 이름', string_agg(name, ', ') from injection_stock_silo;
COMMIT;
SQL
find /db/odoo-root/odoo/git/odoo_gh/odoo_plugins/escon_br/models -maxdepth 1 -type f -name '*.py' -exec grep -HnE '^[[:space:]]*_(name|table)[[:space:]]*=' {} +
docker exec -i -e PGOPTIONS='-c default_transaction_read_only=on -c statement_timeout=15000 -c lock_timeout=3000' postgresdb psql -X -U odoo -d odoo18 -P pager=off -At -v ON_ERROR_STOP=1 <<'SQL'
select 'BR model',model from ir_model where model like '%br%work%' or model like '%br%intake%' order by model;
select 'BR table',table_name from information_schema.tables where table_schema='public' and (table_name like '%br%work%' or table_name like '%br%intake%') order by table_name;
select 'vendor_part_price columns',column_name,data_type from information_schema.columns where table_schema='public' and table_name='vendor_part_price' order by ordinal_position;
SQL
```

### 출력 원문

```text
BEGIN
조회시각|2026-09-21 01:33:51.775396+00|odoo18|on|15s|3s
ALC 목록|767
BOM 관리번호 수|3
BOM 관리번호|부서 지정|3
BOM 라인 수|99105
BOM 배치(Batch)|committed|4
BOM 분류|general_assembly|active|active=true|1
BOM 분류|injection|active|active=true|6
BOM 분류|odoo_standard|pending|active=false|29
BOM 분류|vehicle_batch|active|active=true|509
BOM 분류|vehicle_batch|archived|active=false|4507
LOT/시리얼|531
MO 사출(is_injection_mo)|646
MO 전체|cancel|341
MO 전체|confirmed|11
MO 전체|done|350
MO 전체|progress|8
MO 전체|to_close|1
결재 요청|draft|52
결재 요청|in_progress|1
결재 템플릿|0
고객(원청)|14
공정검사|90
금형|18
부서|8
부적합|0
사용자(내부 활성)|3
사일로|4
사출 계획 실행|6
설비능력(사출기×금형×품목)|18
원재료(수지) 품목|2
임직원(활성)|32
입고검사|3
작업장 사출기(x_is_injection)|4
작업장 전체|82
재고 수량 행(quant, qty>0)|384
재고 위치(내부)|12
전표(전기)|1597
정산 원장(accrual)|5
차종연결(car.info.model)|482
창고|1
청구서(공급사 인보이스)|posted|351
컬러코드|4
톤 단가(material.ton.price)|2
품목 사출품(is_injection_part)|15
품목 전체|731
품목 카테고리별|All|210
품목 카테고리별|All / 사출 완제품|8
품목 카테고리별|All / 사출 원재료|3
품목 카테고리별|All / 캠스별도생산품목|1
품목 카테고리별|직서열 조립품|509
협력사(공급사)|257
품목 코드 예시(사출품 5개)|TEST-INJ-TPO, 865D1-5S200, INJ-BF-001, INJ-BR-001, INJ-BK-F01
직서열 BOM 품목 코드 예시 5개|865A0-5S000CA, 865A0-5S100PSR, 865A0-5S110PSR, 865A0-5S120PSR, 865A0-5S130PSR
작업장 코드|frt, frt, rr, frt, 4, frt, frt, rr, rr, 3, rr, frt, frt, 2, 1, rr, frt, frt, frt, rr, rr, rr, rr, frt, frt, rr, rr, rr
사일로 이름|TEST-SILO, 003SILO, 001SILO, 002SILO
COMMIT
/db/odoo-root/odoo/git/odoo_gh/odoo_plugins/escon_br/models/br_sample_model.py:12:    _name = 'escon.br.sample'
/db/odoo-root/odoo/git/odoo_gh/odoo_plugins/escon_br/models/br_print_printer.py:4:    _name = "br.print.printer"
/db/odoo-root/odoo/git/odoo_gh/odoo_plugins/escon_br/models/br_data_model.py:13:    _name = 'br.db.model'
/db/odoo-root/odoo/git/odoo_gh/odoo_plugins/escon_br/models/br_data_model.py:20:    _table = 'br_foreign_table_v'
/db/odoo-root/odoo/git/odoo_gh/odoo_plugins/escon_br/models/br_work_model.py:11:    _name = 'escon.br.work'
BR model|escon.br.work
BR table|escon_br_work
vendor_part_price columns|id|integer
vendor_part_price columns|product_id|integer
vendor_part_price columns|partner_id|integer
vendor_part_price columns|currency_id|integer
vendor_part_price columns|create_uid|integer
vendor_part_price columns|write_uid|integer
vendor_part_price columns|date_from|date
vendor_part_price columns|date_to|date
vendor_part_price columns|price|numeric
vendor_part_price columns|active|boolean
vendor_part_price columns|create_date|timestamp without time zone
vendor_part_price columns|write_date|timestamp without time zone
vendor_part_price columns|company_id|integer
vendor_part_price columns|superseded_by_id|integer
vendor_part_price columns|price_status|character varying
vendor_part_price columns|agreement_ref|character varying
vendor_part_price columns|confirm_date|date
```

## 5. 누락 항목 보완 — 실행 명령

```bash
set -e
docker exec -i -e PGOPTIONS='-c default_transaction_read_only=on -c statement_timeout=15000 -c lock_timeout=3000' postgresdb psql -X -U odoo -d odoo18 -P pager=off -At -v ON_ERROR_STOP=1 <<'SQL'
BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY;
select '보완조회시각', now(), current_database(), current_setting('transaction_read_only'), current_setting('statement_timeout'), current_setting('lock_timeout');
select 'BR 지시(escon_br_work)',count(*) from escon_br_work;
select 'BR 접수 테이블(br_intake)',coalesce(to_regclass('public.br_intake')::text,'MISSING');
select 'escon_br_intake 모듈 등록 행',count(*) from ir_module_module where name='escon_br_intake';
select '계약 단가(vendor_part_price)|전체',count(*) from vendor_part_price;
select '계약 단가(vendor_part_price)|price_status='||coalesce(price_status,'-'),count(*) from vendor_part_price group by price_status order by 1;
select '관리계획 전체',count(*) from iatf_control_plan;
COMMIT;
SQL
date --iso-8601=seconds
```

### 출력 원문

```text
BEGIN
보완조회시각|2026-09-21 01:34:36.833018+00|odoo18|on|15s|3s
BR 지시(escon_br_work)|61
BR 접수 테이블(br_intake)|MISSING
escon_br_intake 모듈 등록 행|0
계약 단가(vendor_part_price)|전체|3
계약 단가(vendor_part_price)|price_status=firm|1
계약 단가(vendor_part_price)|price_status=provisional|2
관리계획 전체|0
COMMIT
2026-09-21T10:34:36+09:00
```

## 6. 개발자 전달 코멘트

운영 기준정보를 2026-09-21 오전 읽기 전용으로 조회했습니다. 첨부에 실행 명령과 출력 원문을 보존했습니다. 요청 SQL의 BR 테이블명 2개와 단가 상태 컬럼 1개가 운영 스키마와 달라 차이를 명시하고, 실제 확인 가능한 항목만 별도로 보완했습니다. 운영 변경이나 PR #10 작업은 하지 않았습니다. 공개 저장소라는 점을 안내한 뒤 서버 담당 사용자의 명시적 게시 요청에 따라 지정한 handoff 경로에 원문을 첨부합니다. 조회 시각은 본문에 기록된 2026-09-21 오전이며, 게시를 위해 운영 데이터를 다시 조회하거나 변경하지 않았습니다.

## 7. UAT 범위 선정 시 해석

- 이 결과는 조회 당시 운영 코드 `0b36e7f`와 설치된 모듈의 현황이다. 릴리스 이후의 스키마나 동작을 검증한 결과가 아니다.
- 요청 SQL의 가정과 운영 스키마가 다른 3개 항목은 '조회 불가', '실제 테이블명 보완', '다른 상태 컬럼 분포'로 구분했다. 모든 차이를 미배포 또는 모듈 미설치 탓으로 단정하지 않는다.
- 사출품 15개와 활성 사출 BOM 6개는 서로 다른 대상을 센 값이다. 차이 9개를 BOM 누락으로 단정할 수 없으며 품목별 연결 검증이 필요하다.
- 관리계획 및 결재 템플릿 0건은 해당 테이블에 행이 없다는 의미이다. 실제 업무 미수행, 다른 관리 방식, 데이터 부족 중 무엇 때문인지는 이번 조회로 판정하지 않았다.
- 작업장 82개 중 사출기 표시가 있는 항목은 4개이다. 실제 물리 사출기가 82대라는 의미가 아니다.
- UAT에는 어떤 활성 마스터와 관계 데이터를 사용할지, 비활성 이력/시험성 자료를 제외할지, 빈 업무 시나리오를 합성 데이터로 보충할지 개발 측이 제안하고 업무 담당자가 결정한다. 이번 건수 조회만으로 마스터의 완전성이나 정상 여부가 검증된 것은 아니다.
