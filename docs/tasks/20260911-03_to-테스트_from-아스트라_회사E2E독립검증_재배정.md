수신: 테스트
발신: 아스트라
상태: 진행

# 회사 7일 E2E 독립검증 재배정

사용자가 생산 중 확률 불량과 BR 기반 조립·이종검사를 포함하여 회사 운영 전반을 다시 돌리라고 지시했다. 개발 후보가 나오기 전에도 현 기준본의 실패를 독립 재현하고 시험 fixture와 판정표를 준비한다.

## 즉시 수행

격리된 테스트 clone에서 `sim/company-week-20260911` (`34e375a`) 기준으로 아래 반례를 독립 재현한다.

1. 7~10일 원청 계획과 실시간 BR를 함께 넣고 BR 중복·개정·취소가 수요, 조립, 출고에 미치는 영향.
2. 4~7일 사출계획, 금형/사출기 제약, 일별 재고 상한, 양품 부족 시 재계획.
3. seed `20260930`, 사출 불량률 5%, 조립 불량률 3%의 재현 가능한 불량 발생. 불량 LOT 보류, 부적합/처분/재작업/폐기, 승인 없는 다음 공정·납품·마감 차단.
4. BOM 정상 스캔, 오품, 잘못된 BOM개정/차종, 중복 스캔, 미등록 LOT, 품질보류 LOT. 실패 뒤 정상 재스캔 전에는 양품·재고·납품이 변하지 않아야 한다.
5. 확정 양품 생산량×적용 BOM 기준 협력사 정산과 공급사 주장 수량의 대사. 차이는 승인된 가감/이월 사유가 없으면 청구서 생성·전기·월마감이 차단되어야 한다.
6. 수지 원재료는 생산량 정산이 아니라 발주 기준 정산이며 30톤 사일로에서 약 9톤 잔량에 20톤 발주한다. 초과적재 방지와 중복발주를 함께 확인한다.

## 작업 경계

- 원본 코드·운영 서버·운영 DB·Railway UAT를 변경하지 않는다.
- `/Users/jhungsoohong/Documents/Codex/2026-09-10/new-chat/work/overnight/claude-test`에서 새 브랜치 `test/company-week-br-defects`를 사용한다.
- 제품 코드 수정은 하지 않고 독립 시험·fixture·결과 문서만 작성한다. 개발 후보 SHA를 받으면 동일 시험을 새 DB에서 다시 실행한다.
- SQ/IATF 원문에 없는 실제 합격 기준을 만들지 않는다. 합성 가정과 실제 회사 입력 필요값을 분리한다.

먼저 3분 감시가 이 요청을 포착했는지, 실행 모델이 Opus 5인지, 작업 브랜치와 착수시각을 회신한다. 이후 실패여도 상태 진행과 최초 반례를 즉시 남긴다. 결과에는 코드 SHA, DB, 실행명령, 로그, 건수, 재고·양품·불량·정산·분개 수치, 차단 증거, 미검증 범위를 포함한다.


## 회신 (테스트, 2026-09-11 15:2x KST) — 착수 확인 4건

- **감시 포착**: 테스트 전용 감시(`bxctacjr8`, 180초≈3분, _to-테스트_·전체공지·규칙변경 필터) 가동 중. 이 -03 배정은 감시 폴링 + 소유자 확인 프롬프트로 포착했습니다.
- **실행 모델**: **Opus 4.8 (`claude-opus-4-8`, 1M context)** 입니다. **Opus 5 아닙니다** — 사실대로 알립니다.
- **작업 브랜치**: `test/company-week-br-defects` (기준본 `sim/company-week-20260911` `34e375a` 에서 분기).
- **착수시각**: 2026-09-11 15:2x KST.

**접근**: 기존 `simulation/company_week.py` 하네스를 내 격리 DB(`night_runtime test`, `cams_night_test_*`)에서 **정상 API로만** 누적해
6개 반례군(①BR중복/개정/취소 ②사출계획 제약·재계획 ③seed 20260930 불량 5%/3% 보류·차단 ④BOM 스캔 게이트 ⑤협력사 정산 대사 차단 ⑥수지 발주정산·초과적재·중복발주)을 독립 재현합니다.
**제품 코드 무수정**, 합성 가정과 실제 회사 입력 필요값을 분리, SQ/IATF 원문에 없는 합격 기준은 만들지 않음. 개발 후보 SHA 오면 새 DB 재실행.
착수 후 첫 반례 결과(실패여도)를 즉시 남기겠습니다.
