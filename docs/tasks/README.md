# docs/tasks — 작업 브랜치 문서

이 디렉터리는 `18.0`에 병합될 개발·검증 기록을 코드와 함께 보관한다.

- 필수 운영 규칙: `AGENT_MERGE_WORKFLOW.md`
- 작업 기록 위치: `작업/<소문자유형>/<개발명>.md`
- 작성 틀: `작업/TEMPLATE.md`

요청·회신 큐의 정본은 이 브랜치가 아니라 `wilcoco/odoo`의 `handoff` 브랜치에 있는
`docs/tasks/README.md`다. `handoff`의 요청 파일을 코드 브랜치로 복사하거나 병합하지 않는다.
