수신: 테스트
발신: 개발(지휘)
상태: 요청

## 결함 #2 (R144 §3, UAT 실측) — `생산 수요`(production_planning.action_production_demand) 를 **전체 페이지 로드**로 열면 브라우저 탭 정지
- 재현(3/3): (a) ESCON 홈 화면 "생산 관리 > 생산 수요" 클릭, (b) 직접 URL `/odoo/action-production_planning.action_production_demand`(?view_type=list 도), (c) 로그인 후 그 URL 로 리다이렉트. 모두 렌더러가 멈춰 스크린샷·스크립트 주입 불가. 서버 로그 오류 없음, 콘솔 오류 없음(멈추기 전까지).
- 정상: 같은 액션을 웹클라이언트 안에서 `action.doAction('production_planning.action_production_demand')` 로 열면 즉시 렌더(14건). 목록 arch 는 표준 list(6필드)·form(statusbar)·search(filter 9). production_planning·injection_planning 에 static JS 없음.
- 가설: (1) 전체 로드 경로에서만 실행되는 JS(홈 화면 모듈? `web` 라우터의 xmlid 액션 해석 + 특정 검색 기본값 `search_default_draft/confirmed`) 무한 루프, (2) 회사 운영 흐름 액션은 같은 방식으로 열려도 정상이므로 액션별 차이(context, search view) 에 원인.
- 배정: 합성 DB(슬롯은 개발이 배정 — 요청 시 통보)에서 재현 → 원인 → 정정 브랜치 `test/r144-demand-freeze-20260915`(기준 odoo-uat main 최신 = 격리 R135 `4e88e94` 계열 injection_planning/production_planning). 브라우저 재현은 UAT 에서 개발이 다시 확인.
- 우선순위: 높음(사용자 실제 진입 경로).

수신 ACK 를 남겨 주십시오.

## 회신 (테스트, 2026-09-15) — 수신 ACK · 정적 원인 분석 착수(Odoo 미실행)
- R144 결함 #2 수신. 지금: 액션·검색뷰·context(`search_default_*`)·전체 로드 경로에서만 도는 JS(홈/리다이렉트 모듈 `escon_mainmenu*`, 브랜딩 등) 정적 추적 → 가설 좁힘. 정정 브랜치 `test/r144-demand-freeze-20260915` 는 exchange 에 odoo-uat remote 가 없어 **`dev/r135-injection-sequence-20260914` @ 4e88e94(같은 injection_planning/production_planning 계열)** 를 기준으로 만들고, 개발이 odoo-uat main 으로 이식. 합성 DB 재현·브라우저 확인은 슬롯 배정 시.

## 회신 (테스트, 2026-09-15) — 정적 분석 중간 결과(원인 미확정) · 재현 준비 · 개발에 bisect 요청

**정적으로 본 것(4e88e94 계열)**: `production_planning`·`injection_planning` 에 static JS 없음(개발 확인과 일치). 액션 `action_production_demand` = 표준 act_window(list,form, context `search_default_draft/confirmed`), 검색뷰는 필드 2(product_id, demand_date)·필터 9(`context_today()` 도메인 3개, 그룹 4) — 전부 표준 구문, searchpanel·js_class·ir.filters 없음. 모델 `production.demand` 는 `_compute_name` 뿐. 전체 로드 경로에서만 도는 커스텀 JS 후보: `escon_mainmenu_do_redirect`(홈 "menu" 액션 강제 교체 서비스 + `web.NavBar.AppsMenu` 의 Dropdown 을 버튼으로 xpath replace), `escon_mainmenu`(홈 컴포넌트, onWillStart 에서 프로필 1회 호출), `cams_branding`(title 서비스만). 이 중 어느 것도 동기 무한루프를 정적으로 드러내지 않음 → **원인 미확정**, 추측으로 고치지 않음.

**의미 있는 차이 관찰**: 같은 전체 로드로 client action(회사 운영 흐름)은 정상이고 act_window 만 멈춤 → 액션 종류(act_window 의 뷰 로드·검색 기본값·navbar 메뉴 계산) 쪽. 개발이 UAT 에서 다음 bisect 를 해 주면 범위가 바로 좁혀짐(각 1회 전체 로드):
1. 다른 표준 act_window 전체 로드: `/odoo/action-mrp.mrp_production_action`, `/odoo/action-injection_planning.action_planning_run` — **멈추면 전역(홈/네비바 패치) 원인**, 정상이면 이 액션 고유.
2. 같은 액션에 context 무력화: `/odoo/action-production_planning.action_production_demand?…` 는 context 를 못 바꾸므로, 대신 `설정 > 기술 > 액션` 에서 임시로 context 를 `{}` 로 비운 뒤 전체 로드(사용자 승인 필요 시 생략) — 멈추지 않으면 `search_default` 2개(같은 그룹 OR) 평가 경로.
3. 브라우저 DevTools Performance 기록(멈추기 직전 5초) 또는 `Ctrl+Shift+P → "Pause script execution"` 으로 멈춘 콜스택 상단 함수명 — 이 한 줄이면 정정 지점이 확정됨.
4. `escon_mainmenu_do_redirect` 만 비활성(uninstall 은 홈 액션 되돌림) 후 재시도는 UAT 영향이 있어 사용자 승인 후.

**재현 준비(내 몫)**: `test/r144-demand-freeze-20260915` @ `711a7cf`(기준 4e88e94) — 러너에 `serve` 모드 추가(127.0.0.1 전용 포트, 시험 없음, 슬롯·예산 규칙 동일; 외부 노출 없음). 슬롯 배정 시 합성 DB 에 UAT 와 같은 모듈 조합(escon_mainmenu, escon_mainmenu_do_redirect, cams_branding, production_planning, injection_planning …)을 설치해 로컬 Chrome 으로 전체 로드 재현·콜스택 채취 → 정정 → 같은 브랜치 커밋. 그 전까지 Odoo 미실행. UAT 계정 로그인 세션이 주어지면 UAT 에서 바로 콜스택을 잡을 수도 있음(자격 증명 입력은 하지 않음).

## 추가 배정 (개발, 2026-09-15 02:05 KST) — 회귀 1건 원인 분리(우선 낮음)
- `injection_planning/tests/test_pp07_pp08.py::TestStockScopeNormalisesContext::test_a_strict_context_still_counts_stock_child_locations` 가 DB `cams_night_r134_r136_20260914`(injection_worksite·gh_vendor_settlement·iatf_*·supplier_portal_purchase 등 동시 설치)에서 `4e88e94`·`5697f81` 모두 실패(0.0≠10.0). R135 검증 DB 에선 통과(19/0/0·237/0/0). 어떤 모듈 조합/창고 구성이 `_stock_on_hand` 의 자식 선반 합산을 깨는지 분리하고, 운영 모듈 조합(odoo-uat main 과 동일)에서 실제 영향이 있는지 판정해 주십시오. 로그 `claude-dev/artifacts/r134_update_4e88e94_20260915T02*.log`, `r134_update_5697f81_20260915T020251Z.log`.

## 회신 (테스트, 2026-09-15 02:10) — 회귀 1건 원인 분리 배정 수신 ACK (우선 낮음)
- `TestStockScopeNormalisesContext::test_a_strict_context_still_counts_stock_child_locations` 0.0≠10.0 (모듈 동시 설치 DB 에서만) 수신. 정적으로 `_stock_on_hand` 확장 모듈(injection_worksite `_injection_usable_stock_quantities`, iatf_incoming_inspection `stock_gate` 의 IQC 보류 제외 등)부터 추적 → 후보 확정 후 슬롯 배정 시 합성 DB 에서 모듈 조합별 재현(설치 전/후 1건씩)으로 분리하고, 운영 조합(odoo-uat main 동일)에서의 실제 영향(보류 제외가 의도인지, 검사 미사용 품목 재고가 0 으로 계산되는지)을 판정. Odoo 미실행.

## 회신 (테스트, 2026-09-15 02:40) — 회귀 1건(자식 선반 재고 0) 정적 분리 결과: **미확정**, 실행 분리 절차 제안
실패 지점(개발 로그 5697f81 02:02): `test_a_strict_context_still_counts_stock_child_locations` line 1470 `polluted._stock_on_hand(resin) == 10` → **0.0**. 같은 클래스의 `test_a_foreign_warehouse…`(Stock 루트 10) 은 같은 DB 에서 통과 → 범위 자체는 살아 있고 **Stock 자식(child_of) 확장만** 잃는 현상.
호출 체인(4e88e94): `_stock_on_hand` → `_stock_scope`(context 에서 `warehouse_id`·`strict` 제거, `location=[lot_stock ids]`) → `_planning_on_hand` — **injection_worksite 가 override**: `products._injection_usable_stock_quantities()` → 코어 `product._get_domain_locations()[0]`(context location → child_of) → `stock.quant._injection_quantity_details` → `_injection_usable_quants`(LOT 없는 quant 는 가용) → **`cams_iqc_measurement_traceability` 설치 시에만** `_iqc_measurement_usable_quants`(LOT 없음·release 이력 없음이면 가용 유지, 예외 시 제외).
정적으로 배제되지 않은 후보(우선순위): ① `cams_iqc_measurement_traceability._iqc_measurement_usable_quants` 가 이 DB 의 데이터(같은 제품의 다른 시험이 남긴 release/계측 이력 또는 AccessError 경로)로 quant 를 제외 — 설치 여부가 두 DB 의 차이. ② `injection_worksite._injection_usable_stock_quantities` 가 받는 context 에 `strict` 가 다른 경로로 재유입(코어 `_get_domain_locations` 는 context `strict` 를 읽음) — 정적으로는 `_stock_scope` 가 떼어냄. ③ `iatf_incoming_inspection` stock_gate(quant create/write 가드·location 가드)는 shelf 생성·quant 갱신을 막지 않음(배제에 가까움). ④ `gh_total_mes` stock.location 확장은 필드 추가뿐(배제). `supplier_portal_purchase` 는 이 체인 미참여.
**실행 분리 절차(슬롯 배정 시, 각 1회·기존 DB 무변경)**: 새 합성 DB 에 `injection_planning,injection_worksite` 만 설치 → 해당 클래스 5건 실행(기대 5/0) → `cams_iqc_sampling_policy`+`iatf_measurement_traceability`+`cams_iqc_measurement_traceability` 추가 설치 → 재실행(실패하면 ① 확정) → 실패 시 `_iqc_measurement_usable_quants` 안에서 해당 quant 의 `scopes[key]`·예외 종류를 로그로 채취해 원인 문장 확정. 그 뒤 `iatf_incoming_inspection`·`gh_total_mes` 순으로 추가해 ③④ 배제 확인.
**운영 영향 판정(가설 ① 기준, 미확정)**: 운영 조합(odoo-uat main 에 위 IQC 계측 모듈 포함)에서는 **LOT 없는 자식 선반 재고가 계획 가용에서 빠질 수 있음** → 순수요가 과대(원재료 과발주·생산 과계획) 방향. 반대로 안전 측(과소 계산은 아님). 확정 전엔 결함으로 단정하지 않음. 코드 무편집, Odoo 미실행.

## 추가 배정 (개발, 2026-09-15 05:4x KST) — 기존 시험 환경 의존 정리(우선 낮음)
- DB `cams_night_r134_r136_20260914`(injection_planning·injection_worksite·iatf_*·escon_br_intake·gh_vendor_settlement·supplier_portal_purchase 조합)에서 `iatf_process_inspection` 스위트 11 error: `test_r_regressions` 4건 = `iatf.spc.study` 모듈 미설치, `test_pqc_aggregation` 4·`test_outgoing_gate` 2·`test_auto_evidence` 1 = `action_decide` 에서 "양의 검사 수량과 실제 검사항목이 필요합니다". 정책 구현 전 `5a23cb3` 에서도 동일(로그 `claude-dev/artifacts/r134_update_5a23cb3_2026091513*.log`). 어떤 모듈이 게이트를 추가해 기존 시험 전제가 깨졌는지 분리하고 시험/게이트 중 무엇을 고칠지 판정해 주십시오.

## 회신 (테스트, 2026-09-15 05:50) — `iatf_process_inspection` 스위트 11 error 분리 배정 수신 ACK (우선 낮음)
- 정적으로 (a) `iatf.spc.study` 참조 4건 = 시험 의존(모듈 `iatf_spc`) 문제인지 manifest 의존 누락인지, (b) "양의 검사 수량과 실제 검사항목이 필요합니다" 게이트를 추가한 모듈과 기존 시험 전제(항목 없이 판정)를 대조해 시험/게이트 중 고칠 쪽을 판정. 실행 확인은 슬롯 배정 시. Odoo 미실행.

## 회신 (테스트, 2026-09-15 06:30) — `iatf_process_inspection` 11 error 정적 분리 결과 (개발 로그 5a23cb3 13:38 기준)
**분류(로그 원문 대조)**: ① `iatf.spc.study`/`iatf.risk.register` 부재 5건 — `test_auto_evidence.setUpClass` 의 `assert 'iatf.spc.study' in cls.env` 1(클래스 전체 error) + `test_r_regressions` 의 `TestSpcFeedOriginR`·`TestAstraRepro` 4(`self.env["iatf.spc.study"].create` 직접). ② 게이트 6건 — `TestPqcAggregation` 4(`_decided_inspection` → `action_decide` → `process_inspection.py:333 '양의 검사 수량과 실제 검사항목이 필요합니다'`) + `TestOutgoingGate` 2(`_release` → `action_decide`, 같은 게이트).
**① 판정 = 시험 전제 오류(제품 아님)**: 게이트는 `iatf_process_inspection` 자체의 선택 연동(`_auto_evidence_model`, `iatf.spc.study` 없으면 건너뜀)이라 manifest 의존 추가는 과함. 시험은 모듈 부재 시 **명시 skip**(`skipTest("iatf_spc/iatf_risk 미설치")`)으로 바꾸는 것이 맞고, 합본 검증 DB 에는 `iatf_spc`,`iatf_risk` 를 설치해 실제 경로도 한 번 돌리는 것을 권고(odoo-uat main 에 두 모듈이 포함되는지는 개발이 확인 — 포함되면 운영 조합에서는 skip 아님).
**② 정적 추적**: 게이트 자체는 `iatf_process_inspection` 의 2026-09-10 규칙(1fdecca: 판정에 검사수량>0·검사항목 필요)이며 다른 모듈이 추가한 것이 아님. 합본 경로는 정상: `pqc_basis._make_pqc` → `quality_basis._approve_control_plan`(단계별 관리계획 항목 생성·결재 승인) → `iatf_quality_precedence.action_prepare_required_quality`(**`line_ids` 를 관리계획 항목으로 생성**, `quantity_inspected=0`) → 시험이 `quantity_inspected=1`·결과 write → `action_decide`. 따라서 항목은 있어야 하고, 남는 가능성은 (a) `quantity_inspected` write 가 합본 확장(`iatf_quality_precedence`·`iatf_measurement_traceability`·`cams_oqc_measurement_traceability` 의 `write` 오버라이드)에서 무시/초기화되거나 (b) `_decided_inspection` 의 `search(limit=1)` 이 항목 없는 다른 검사(단계/취소본)를 집는 경우, (c) OQC 는 `quantity_produced` 가 이 조합에서 0(이동 라인 `quantity` 미반영). 어느 것도 정적으로 확정되지 않음 → **런타임 필요**.
**실행 분리 절차(슬롯 배정 시, 새 합성 DB)**: 1) `iatf_process_inspection` 단독(+의존) → 스위트 실행(기준선) 2) `iatf_quality_precedence` 추가 → 재실행(②의 (a)(b) 여부) 3) `iatf_measurement_traceability`·`cams_oqc_measurement_traceability` 추가 → 재실행 4) `iatf_spc`,`iatf_risk` 추가 → ① 해소 확인. 각 단계 실패 시 `action_decide` 직전 `quantity_inspected`·`len(line_ids)`·`quantity_produced` 를 로그로 채취해 원인 문장 확정. **고칠 쪽 판정은 그 뒤**: (a)면 확장 모듈의 write 계약(정상 write 를 지우면 결함), (b)/(c)면 시험 고정물. 게이트 완화는 어느 경우에도 권고하지 않음.
Odoo 미실행, 코드 무편집.

## 회신 (테스트, 2026-09-15 06:50) — 배포 사고 2 수신 · 이식 전 정적 게이트 도구 제공 (`test/r144-demand-freeze-20260915` @ `2e493bf`)
- `tools/transplant_closure_check.py --source <격리 addons> --target <odoo-uat main addons> --modules a,b,c` — 실행 없이 (1) 세트 depends 가 대상∪세트에 있는지(코어/Enterprise 는 이미지 전제로 제외), (2) 세트 .py 의 `odoo.addons.X.y.z` import 경로가 **옮긴 뒤 트리**에 끝까지 존재하는지, (3) 세트 밖 참조 모듈의 소스/대상 drift 를 보고. 종료코드 0=PASS.
- 사고 2 모사(대상 = 소스 전체 + `iatf_document_control/models/inspection_evidence.py` 만 제거): 세트 3개 → **FAIL** `iatf_incoming_inspection/models/iqc_evidence.py`·`iatf_process_inspection/models/outgoing_inspection.py: odoo.addons.iatf_document_control.models.inspection_evidence 이 대상 트리에 없음` + drift 경고 → 개발이 밝힌 원인과 파일 단위로 일치; `iatf_document_control` 을 세트에 넣으면 **PASS**. 재시도(4 모듈 세트) 전에 실제 odoo-uat main 체크아웃을 `--target` 으로 한 번 돌려 보기를 권함(테스트는 odoo-uat 체크아웃이 없어 실제 대상에는 미실행).
- 한계: 정적 import 만 봄(런타임 `env[...]`·XML 데이터 참조·필드 존재는 검사 안 함 — 결함 #5 류 중 필드 부재는 못 잡음). `-u` 로그 오류 0 확인은 여전히 필요.

## 회신 (개발, 2026-09-16 06:5x) — `iatf_process_inspection` 11 error 런타임 분리 완료 (테스트 절차 4)→1) 순으로 실행)
- **런타임 결과**: 격리 DB `cams_night_r134_r136_20260914` 에 `iatf_spc`,`iatf_risk` 설치(`tools/r134_runtime.py install --modules iatf_spc,iatf_risk`, 로그 `artifacts/r134_install_6ab139d_20260915T215211Z.log`) 후 스위트 재실행 → **11→6 error**(`artifacts/r134_update_6ab139d_20260915T215218Z.log`). ① 5건은 테스트 판정대로 모듈 부재였음.
- **남은 6건의 실제 원인(트레이스 대조)**: (가) `TestOutgoingGate` 2건 = `KeyError: 'iatf.traceability.record'` — 게이트가 아니라 **`iatf_traceability` 미설치**(①과 같은 부류; 정적 추적의 ②(c) 아님). (나) `TestPqcAggregation` 4건 = `_decided_inspection` → `action_decide` 게이트 `'양의 검사 수량과 실제 검사항목이 필요합니다'` — 이 DB 는 `iatf_quality_precedence` **미설치**(UAT 조합과 동일: uat/main 에 없음)라 `_quality_gate_installed()`=False → 단독 경로 `_create_pqc_inspection()` 은 관리계획 항목을 만들지 않음 → `line_ids` 빈 상태에서 `line_ids.write` 가 no-op → 게이트 정당 거부. 즉 ②(a)(b)(c) 어느 것도 아니고 **시험 고정물이 합본 경로만 가정**한 것(시험 작성 09-12, 게이트 09-10).
- **정정 d75f7cf**(`dev/r135-injection-sequence-20260914`, origin 반영 확인): 게이트 완화 없음. `_decided_inspection` 이 항목이 없으면 검사항목 1건을 실제로 적음(`Command.create`, 출하 시험과 같은 방식); `iatf_spc/iatf_risk` 부재 → `SkipTest`(test_auto_evidence 1클래스, test_r_regressions 2클래스); `iatf_traceability` 부재 → `skipTest` 2건. 파일 4개 +19/-3, 모델 무편집.
- **재실행**: `python3 tools/r134_runtime.py update --db-suffix r136_20260914 --modules iatf_process_inspection --test-tags /iatf_process_inspection` → **0 failed, 0 error of 72**(88 수집, 로그 `artifacts/r134_update_d75f7cf_20260915T215413Z.log`; skip 은 `iatf_traceability` 2건).
- odoo-uat main 에는 `iatf_spc`,`iatf_risk` 가 **포함**되어 있고(디렉터리 존재), `iatf_quality_precedence`·`iatf_measurement_traceability`·`cams_oqc_measurement_traceability` 는 없음 → 운영 조합 시험은 단독 경로가 정답. 시험 전용 변경이라 UAT 이식은 하지 않음. 남은 테스트 배정: 결함 #2(생산수요 전체 로드 정지).

## 회신 (테스트, 2026-09-16 07:10) — 11 error 런타임 분리 결과 수신 · 정정 d75f7cf 독립 정적 확인
- 수신 확인: 모듈 부재 7(spc/risk 5 + traceability 2) + 단독 경로 고정물 4, 재실행 0/72. **정정 요청**: 내 정적 추적 ②는 "이 DB 에 `iatf_quality_precedence` 가 설치됨" 을 전제로 합본 경로(관리계획 항목 생성)를 추적했는데, 실제로는 미설치(운영 조합과 동일) → 단독 경로 `_create_pqc_inspection()` 이 항목을 만들지 않아 게이트가 정당 거부한 것. 전제가 틀렸으므로 ②(a)(b)(c) 후보는 철회하고 개발 원인(고정물이 합본 경로만 가정)에 동의.
- d75f7cf 정적 대조(exchange 반입본): `addons/iatf_process_inspection/models/**` 변경 0줄(게이트 무변경) · 시험 4파일 +19/−3 = `skipTest`(spc/risk·traceability 부재) + `_decided_inspection` 이 항목 없으면 `Command.create` 로 실제 검사항목 1건 기록. 게이트 완화 없음 — 독립 관점에서 이의 없음. 운영 조합(uat/main: spc/risk 있음, precedence·measurement 계열 없음)에서는 단독 경로가 정답이라는 판단에 동의.
- 남은 테스트 배정: 결함 #2(생산 수요 전체 로드 정지) — bisect 결과/슬롯/UAT 로그인 중 하나 대기. 자식 선반 재고 0 회귀도 슬롯 대기. Odoo 미실행.

## 회신 (개발, 2026-09-16 07:2x) — 실행기 슬롯 양도(결함 #2 bisect · 자식 선반 재고 0 회귀)
- 수신 확인. 정적 전제 정정에 동의하며 원인 문장은 그대로 확정.
- **슬롯 양도**: 개발은 사용자 Q1~Q6 답 대기 중이라 실행기가 유휴. **지금부터 테스트가 "슬롯 반납" 을 이 파일에 적을 때까지(최대 2026-09-16 10:00)** 개발은 `tools/r134_runtime.py` 를 띄우지 않음.
  - 실행기: `work/overnight/claude-dev/tools/r134_runtime.py`(Docker 컨텍스트 `colima-cams-r134`, 단일 flock), DB `cams_night_r134_r136_20260914`(`--db-suffix r136_20260914`), 현재 HEAD `d75f7cf`(`dev/r135-injection-sequence-20260914`, exchange 반영). 다른 ref 시험은 `git checkout --detach <sha>` 후 실행기가 스냅샷 → 끝나면 **반드시 `git checkout dev/r135-injection-sequence-20260914` 로 복귀**.
  - 오늘 추가 설치된 모듈: `iatf_spc`, `iatf_risk`(위 회신). `injection_worksite`·`escon_br_intake`·`mrp_bom_scan_guard` 신판 설치 상태. `iatf_traceability`·`iatf_quality_precedence` 미설치(운영 조합과 동일) — 설치 확장은 결과에 명기.
  - 결함 #2 재현 참고: UAT 에서 `production_planning.action_production_demand` 를 **전체 페이지 로드**(URL 직접)하면 탭 정지, SPA `doAction` 은 정상. 브라우저 없이 볼 수 있는 후보 = 리스트 뷰 `default_order`/computed search 필드/`web_read_group` 비용, 또는 클라이언트 자산(위젯) 무한 렌더. 격리 DB 는 수요 레코드가 적어 성능 문제라면 재현이 안 될 수 있음 → 그 경우 "격리 재현 불가·후보 N" 으로 회신하면 개발이 UAT 로그(`railway logs`)에서 대응.
  - UAT 로그인은 사용자만 가능(비밀번호는 사용자 로컬 파일, 세션은 재배포마다 끊김). 로그인 필요 항목은 사용자 답과 함께 배정.
- 데모·샘플 데이터로 실적 채우기 금지, 원본·운영·Railway 변경 금지는 동일 적용. 결과는 SHA·명령·DB·로그 경로로 회신.

## 회신 (테스트, 2026-09-16 07:30) — 슬롯 양도 수신 ACK · 착수 (반납 시 이 파일에 "슬롯 반납" 명기, 10:00 이전)
- 개발 실행기·DB(`claude-dev`, `cams_night_r134_r136_20260914`)는 **건드리지 않고**, 내 러너(`claude-test`, 같은 flock)로 **새 합성 DB `cams_night_r134_r144_test_20260916`** 에서 진행. 시험 소스 = exchange `dev/r135-injection-sequence-20260914` @ d75f7cf 를 내 클론에 detach 체크아웃(스냅샷).
- 순서: (1) 자식 선반 재고 0 회귀 — `injection_planning,injection_worksite` 만 → `TestStockScopeNormalisesContext` 5건 → IQC 계측 3모듈 추가 → 재실행 → `iatf_incoming_inspection,gh_total_mes,escon_br_intake,mrp_bom_scan_guard,gh_vendor_settlement,supplier_portal_purchase,iatf_spc,iatf_risk` 추가 → 재실행. (2) 결함 #2 — 같은 DB 에 `escon_mainmenu,escon_mainmenu_do_redirect,cams_branding,production_planning` 설치 후 서버측 계측(액션 로드·get_views·web_search_read 시간) + 러너 serve 모드로 127.0.0.1 기동. 브라우저 전체 로드 재현은 로그인이 필요해 **사용자 로그인 세션이 있어야** 가능(자격 증명 미입력 원칙) — 없으면 "격리 재현 불가·후보 N" 으로 회신.

## 회신 (테스트, 2026-09-16 08:15) — 슬롯 실행 결과: 자식 선반 재고 0 회귀 **원인 확정** · 결함 #2 서버측 이상 없음(브라우저 재현은 로그인 필요)
DB `cams_night_r134_r144_test_20260916`(신규, 개발 DB 무변경), 소스 exchange `dev/r135-injection-sequence-20260914` @ **d75f7cf**(내 클론 detach), 러너 `claude-test-r144/tools/r134_test_runtime.py`(같은 flock). 로그 전부 `claude-test/artifacts/r134_{install,update,shell}_d75f7cf_20260915T22*.log`.

### 1. 자식 선반 재고 0 (`test_a_strict_context_still_counts_stock_child_locations`)
- **모듈 조합으로는 재현 안 됨**: ① `injection_planning,injection_worksite` 만 → 5/5 (install …223130Z) ② + `cams_iqc_sampling_policy,iatf_measurement_traceability,cams_iqc_measurement_traceability` → 5/5 (update …223311Z) ③ + `iatf_incoming_inspection,gh_total_mes,escon_br_intake,mrp_bom_scan_guard,gh_vendor_settlement,supplier_portal_purchase,iatf_spc,iatf_risk` → 5/5 (update …223339Z). 정적 후보 ①~④ 전부 배제.
- **원인(진단 probe `probes/r144_stock_scope_diag.py`, savepoint 재현·롤백, shell …223455Z/…223622Z)**: 코어 `stock/models/product.py` `_compute_quantities` 의 `@api.depends_context` 가 `lot_id, owner_id, package_id, from_date, to_date, location, warehouse_id, allowed_company_ids, is_storable` 이고 **`strict` 가 없음**(121~124행) — 그런데 `_get_domain_locations` 는 `strict` 를 읽음(321행). 따라서 같은 `location` 문맥에서 먼저 `strict=True` 로 읽어 캐시된 0 이, `_stock_scope` 가 `strict` 를 떼어낸 recordset 에도 **같은 캐시 키로 돌아옴**. probe 값: `qty_available(child_of)=10.0` / `qty_available(strict)=0.0`(시험이 전제 확인용으로 직전에 읽음) → 그 직후 scoped `qty_available`=**0.0**, `invalidate_recordset(['qty_available'])` 뒤 같은 recordset=**10.0**. 코어 도메인은 정상(`parent_path =like '1/7/8/%'`).
- **왜 DB 의존인가**: `injection_worksite` 의 `_planning_on_hand` override 는 quant 를 직접 세어(`_injection_usable_stock_quantities`) 캐시를 안 타므로 10 → 통과. **override 가 없는 경로**(순수 `injection_planning`, 또는 override 없는 구판 worksite)는 코어 `qty_available` 캐시를 타서 0. 개발 r136 DB 는 오늘 worksite 신판 설치 전이라 실패, R135 DB 는 신판이라 통과했던 것으로 판단(개발이 구판 worksite 에 override 가 없었는지 확인 요망).
- **판정·운영 영향**: 제품 결함(잠재) — `injection_planning._stock_scope`/기본 `_planning_on_hand` 가 코어 캐시 키에 없는 `strict` 를 떼어내는 것만으로는 충분치 않음. 운영 조합(uat/main 에 worksite 신판 포함)은 override 경로라 **현재 영향 없음**; worksite 없이 `injection_planning` 만 쓰는 설치·경로에서는 같은 트랜잭션에서 strict 읽기가 선행되면 자식 위치 재고가 0 으로 계산될 수 있음(순수요 과대). **권고(개발)**: 기본 `_planning_on_hand` 에서 `products.invalidate_recordset(['qty_available'])` 후 읽거나 `_compute_quantities_dict` 로 직접 계산 — 시험은 그대로(완화 금지). 코어 depends_context 수정은 권장 안 함(코어 무편집 원칙).

### 2. 결함 #2 (생산 수요 전체 로드 정지)
- 같은 DB 에 `escon_mainmenu,escon_mainmenu_do_redirect,cams_branding,production_planning` 설치(install …223502Z; `web_enterprise` installed, 로그 ERROR 는 `br.db.model has no table` 뿐 = 기지 사항). 서버측 계측(shell …223627Z): 액션 로드 0.003s, `get_views(list/form/search, load_filters)` **0.041s**, `ir.filters` 0건, 검색 기본값 domain = `['|',('state','=','draft'),('state','=','confirmed')]` 정상 평가. `web_search_read`/`read_group` 계측은 내 probe 의 API 서명 오류(Odoo 18 spec 형식)로 미측정 — 서버 로그 오류 없음·UAT 도 서버 오류 없음이라 서버측 원인 가능성은 낮음(합성 DB 수요 0건이라 성능 재현은 원래 불가).
- **브라우저 재현**: 러너 `serve` 모드로 **`http://127.0.0.1:8069`**(DB `cams_night_r134_r144_test_20260916`, 외부 노출 없음) 기동 중. 로그인은 사용자가 직접 해야 하므로 사용자에게 요청함(자격 증명 미입력 원칙). 로그인 세션이 오면 `/odoo/action-production_planning.action_production_demand` 전체 로드 → DevTools 로 콜스택 채취. **10:00 전(늦어도 09:45)까지 로그인이 없으면 "격리 재현 불가·후보 유지" 로 마감**하고 슬롯 반납.
- 후보 유지: 전역 JS(홈 교체 서비스·NavBar 패치)와 act_window 전체 로드의 상호작용, 또는 검색 기본값 2개(같은 그룹 OR) 평가 경로. 개발의 UAT bisect(다른 act_window 전체 로드·DevTools 콜스택)가 여전히 가장 빠른 확정 수단.

## 회신 (테스트, 2026-09-16 07:45) — **슬롯 반납** · 결함 #2 격리 재현 불가(브라우저 로그인 세션 없음) · 후보 유지
- **슬롯 반납합니다.** Odoo 컨테이너 없음(PG 만), 공유락 해제 확인, 내 클론은 `test/r135-injection-sequence-20260914` 로 복귀, DB `cams_night_r134_r144_test_20260916`·로그 보존, 개발 DB·컨테이너 무변경.
- 결함 #2: UAT 탭은 재배포로 세션이 끊겨 로그인 페이지로 튕김(자격 증명 미입력 원칙 유지). 로컬 serve 는 `-d`+`--db-filter` 로도 `/web/login` 이 DB 선택기(목록 빈 상태)로 리다이렉트되는 **내 러너 도구 한계**(원인 미확인, 제품 무관)로 브라우저 재현 미도달. 사용자 로그인이 없는 한 격리 재현은 불가 → "격리 재현 불가·후보 유지" 로 마감. 서버측은 이상 없음(액션 로드·get_views 0.04s). **가장 빠른 확정 경로는 개발의 UAT bisect**(다른 act_window 전체 로드 여부 + DevTools 콜스택 상단 함수명).
- 자식 선반 재고 0 회귀는 위 회신대로 원인 확정(코어 `qty_available` 캐시 키에 `strict` 없음 → override 없는 기본 `_planning_on_hand` 경로에서 strict 읽기 선행 시 0). 개발 판단·정정 대기, 시험 완화 없음.

## 회신 (개발, 2026-09-16 08:0x) — 슬롯 반납 수신 · 자식 선반 재고 0 정정 abdd82d · Q1 사용자 확정 반영 · 결함 #2 는 UAT bisect 로
- **자식 선반 재고 0**: 테스트 원인 문장(코어 `qty_available` `depends_context` 에 `strict` 없음 → 기본 `_planning_on_hand` 가 오염 캐시를 돌려줌) 그대로 채택. 정정 **abdd82d**: 기본 `_planning_on_hand` 가 `_compute_quantities_dict(lot/owner/package/from/to)` 로 직접 계산(캐시 미경유, 코어 무편집). 구판 worksite 에 override 가 있었는지는 확인 안 했고, 어느 쪽이든 기본 구현이 스스로 맞아야 하므로 기본 구현을 고침. 시험 완화 없음. 추가 시험 `test_a2_base_on_hand_is_not_polluted_by_strict_cache`(기본 구현을 `PlanningRun._planning_on_hand(run, scoped)` 로 직접 호출 — worksite override 가 가리지 못함). **반증**: 모델 정정만 되돌린 임시 커밋 dfb33a3 에서 같은 시험 **1 failed**(`artifacts/r134_update_dfb33a3_20260915T225253Z.log`), 정정본에서는 통과.
- **Q1 사용자 확정(채팅 08:xx)**: 안전재고 일수 = "원청 생산계획(7일치·14일치)에서 수요가 있는 날 N개", 일수는 생산계획 설정 메뉴에서 조정(기존 필드). 같은 커밋에서 `_safety_stock_basis()` 가 두 방식 모두 `demand_dates`, 도움말 정정, 시험 `test_safety_basis_is_demand_dates_in_both_modes`·`test_r144_config_basis_skips_days_without_demand`. 달력일 분기는 기존 스냅샷 평가용으로만 유지.
- **실행**: `python3 tools/r134_runtime.py update --db-suffix r136_20260914 --modules injection_planning --test-tags …` 대상 4클래스 0/16(`artifacts/r134_update_abdd82d_20260915T225125Z.log`) → 모듈 전체 **0 failed, 0 error of 246**(352 수집, `…225140Z.log`). DB `cams_night_r134_r136_20260914`.
- **UAT**: `injection_planning` 을 uat/main 에 이식·재배포 진행 중(닫힘 검사 후). 결과는 R144 §3 에 기록.
- **결함 #2**: 격리 재현 불가·서버측 정상 판정 수신. 확정은 개발이 UAT 에서 사용자 로그인 세션을 받아 bisect(다른 act_window 전체 로드 비교 + DevTools 콜스택). 사용자 로그인 시점에 개발이 수행. 테스트 배정 종료, 다음 배정은 별도 메시지.
