수신: 테스트
발신: 개발(지휘)
상태: 요청

## 배경 (사용자 지시, 2026-09-14 17:3x KST)
사용자: 전사 가시화가 "최상단 → 모듈(영역) 클릭 → 세부 프로세스 → 클릭 → 세부 내역" 로 드릴다운되는 대시보드가 잘 안 되고 있다. 설계를 개발이 만들고 **테스트 세션이 화면 구현을 수행**한다(사용자 지시). 독립 판정 역할은 유지하되, 이 건의 UI 는 테스트가 만들고 개발이 서버 시험과 함께 실행 검증, Spark 가 정적 검토한다.

## 정본
- 설계: exchange `dev/r136-factory-flow-20260914` @ **`b7fa70b`** `docs/tasks/R136-CASCADE-DESIGN.md` (4단 캐스케이드·표시 규칙 7·서버 계약·클라이언트 지시 §4).
- 서버 어댑터: 같은 브랜치 `addons/cams_ops_dashboard/models/factory_flow.py` (`cams.factory.flow`: `get_map/get_node/get_action`, 어댑터 10노드, 상태 linked/not_wired/not_installed/restricted). 시험 6건 `tests/test_factory_flow.py` — **미실행**(슬롯 대기).
- 카탈로그: 테스트 `448325f` 를 `361364b` 에 병합 완료(235노드/146 leaf/111 edge, 검증기 0오류) — 모듈 적재 사본 `data/r136_canonical_map.json` 동기화.

## 배정
1. **클라이언트 구현** — 브랜치 `test/r136-cascade-ui-20260914`(기준 `b7fa70b`), 파일·액션·메뉴·매니페스트는 설계 §4 그대로. ORM 호출은 3 메서드만. 표시 규칙 §2 (퍼센트 없음, 4종 연결 상태 구분, late 별도 배지, restricted 는 숫자·제목 없음, 건수는 서버 total 그대로).
2. **web_tour** 4단 클릭 시나리오 1개(회사 전체 → 생산 → 사출 계획 → 문서 폼) — 실행은 슬롯 후.
3. 정적 완료 시 이 파일에 SHA·파일 목록·설계 대비 미구현 항목 회신. Odoo 실행 없음(슬롯은 개발이 배정). 기존 `R136-TEST-2ND.md` A1~A10 은 그대로 유지, UI 시험(H4~H6)은 서버 시험과 분리 기록.
4. 설계에 이견이 있으면 구현 전에 이 파일에 먼저 — 임의 변경 금지.

수신 ACK 를 남겨 주십시오.

## 회신 (테스트, 2026-09-14 17:40) — 수신 ACK · 착수

- 10 배정 수신. 브랜치 `test/r136-cascade-ui-20260914`(기준 `b7fa70b`)에서 설계 §4 그대로 클라이언트 구현 착수 — ORM 호출은 `get_map/get_node/get_action` 3개만, 표시 규칙 §2 준수(퍼센트 없음·연결 상태 4종·late 별도 배지·restricted 는 숫자/제목 없음·건수 = 서버 total). web_tour 4단 시나리오 1개 포함. Odoo 실행 없음(정적 구현 → 구문·QWeb XML 검사만).
- 설계 이견이 생기면 구현 전에 이 파일에 먼저 적음. 완료 시 SHA·파일 목록·미구현 항목 회신. 독립 판정 역할·`R136-TEST-2ND.md` A1~A10 유지, UI 시험(H4~H6)은 서버 시험과 분리 기록.

## 회신 (테스트, 2026-09-14 18:05) — 배정 1·2 정적 완료: `test/r136-cascade-ui-20260914` @ **`39e3422`** (기준 b7fa70b, 2커밋) — **미실행**

**파일**(`addons/cams_ops_dashboard/`):
- `static/src/factory_flow/factory_flow.js`(396줄) — OWL `FactoryFlow`(루트) + `AreaRibbon`/`ProcessCards`/`NodeDetail`, 상태 `{level, path[], map, catalog, node, page, filters, error, …}`, `useService("orm"/"action"/"notification")`, 60s 폴링·숨김 탭 정지(기존 ops_dashboard 패턴), ORM 호출은 **`cams.factory.flow` 의 `get_map`(level 2; L2 상세 진입 시 level 4 를 하위 트리 이름·상태용으로 1회 지연 적재) / `get_node` / `get_action`** 뿐(정적 grep: get_map 2·get_node 1·get_action 2, `rpc`/`sudo` 없음). URL hash `#ff/CO.PROD/CO.PROD.INJ_PLAN` + `hashchange` → 브라우저 뒤로가기 = 한 단 위. 서버 예외는 상단 배너, 노드 단위 상태는 배지.
- `static/src/factory_flow/factory_flow.xml`(179줄) — 4 템플릿. 표시 규칙 §2: 퍼센트 없음(안내문 "단계별 건수 합(고유 사건 중복 제거 안 함)"), 연결 4종 서로 다르게(`linked` 실선·건수 / `not_wired` 점선 "어댑터 없음" / `not_installed` 회색 "모듈 없음" / `restricted` 자물쇠 "권한 없음"·숫자·내용 없음), `late` 별도 "기한경과" 배지, 상위 타일에 "권한 제한 n 노드 제외", 세부 내역 건수 = 서버 `status.total` 그대로(+`truncated` 시 "상태 분류는 n건까지"), `as_of`·`updated_at` 표시, 관계선은 맵 edge 를 L2 조상으로 접어 표시(FK 없는 선 없음), "데모" 라벨 없음.
- `static/src/factory_flow/factory_flow.scss`(116줄) — `.ops-*` 토큰 재사용, `.ff-*` 전용.
- `views/factory_flow_views.xml` — `ir.actions.client` tag `cams_ops_dashboard.factory_flow`, 메뉴 "회사 운영 흐름" `cams_ops_process.menu_ops_root` 아래 sequence 6.
- `__manifest__.py` — 18.0.3.0.0, data·`web.assets_backend`·`web.assets_tests` 추가.
- `static/tests/tours/factory_flow_tour.js` — `cams_ops_dashboard_factory_flow_tour`: `.ff-area[data-pid='CO.PROD']` 클릭 → `.ff-process[data-pid='CO.PROD.INJ_PLAN']` 클릭 → breadcrumb 3단 확인 → 첫 행 `.ff-row__open` 클릭 → `.o_form_view`. `tests/test_factory_flow_tour.py`(HttpCase, 태그 `r136_ui`): `injection.planning.run` 없으면 **명시 skip**, 있으면 계획 1건 생성 후 `start_tour`. `tests/__init__.py` 에 import 1줄.
**정적 검사**: py_compile·XML 파싱·JS 구문·템플릿 이름=컴포넌트 이름 대조 통과. 실행(모듈 -u, tour, 브라우저)은 슬롯 배정 후 — 미실행이므로 완료 아님.
**설계 대비 미구현/차이(이견 아님, 확인 요청)**: (1) L2 왼쪽 하위 트리는 `get_map(level=4)` 를 추가 호출해 채움(설계는 `get_node.children` 이 id 만 주므로 이름·상태가 필요) — 3 메서드 안이지만 호출 1회 추가. (2) L3/L4 하위 업무 클릭은 어댑터가 `linked` 인 경우만 그 노드로 이동, 아니면 안내 알림(숨기지 않음). (3) 카드 사이 관계선은 SVG 선이 아니라 "A → B ×n" 배지 목록(정적 단계에서는 레이아웃 측정 없이 안전한 형태; 실행 확인 뒤 SVG 로 바꿀 수 있음). (4) 회사 선택 UI 없음 — `env.companies` 범위를 "회사 n개 범위" 로 표시만(설계 공통 필터 "회사(현재 env.companies)" 해석). (5) `get_node` 의 `rows` 는 `order="id desc"` 서버 기준 그대로, 정렬 UI 없음.
독립 판정 역할 유지: 이 UI 의 검증은 개발 실행 + Spark 정적 검토(설계 §5). `R136-TEST-2ND.md` A1~A10 은 그대로.

## 회신 (테스트, 2026-09-14 19:55) — 기준 갱신: `test/r136-cascade-ui-20260914` @ **`cb0d7c8`** (41b2c1b 위로 rebase, 내용 39e3422 와 동일 2커밋, 정적 검사 재통과) — 미실행

## 회신 (개발, 2026-09-14 20:0x KST) — cb0d7c8 실행 결과 · dev/r136 병합 · main 반영
- 실행(개발 슬롯, VM `colima-cams-r134`, DB `cams_night_r134_r136_20260914`): `tools/r134_runtime.py update --db-suffix r136_20260914 --modules cams_ops_dashboard --test-tags /cams_ops_dashboard` @ `cb0d7c8` → **0 failed, 0 error / 10**, `web.assets_web.min.js` 번들 재생성(JS 번들 오류 없음), `factory_flow_views.xml` 적재. 로그 `artifacts/r134_update_cb0d7c8_20260914T102946Z.log`.
- **tour 는 skip**: `websocket-client module is not installed` — 고정 Odoo 이미지(sha256:876e3d…)에 브라우저 시험 의존성이 없음. 즉 **브라우저에서 4단 클릭은 아직 아무도 확인하지 않음**(정직히). 확인 경로는 (a) UAT 배포 뒤 사용자·서버가 직접, (b) 개발이 별도 이미지로 tour 실행(이미지 재빌드는 금지 항목이라 사용자 승인 필요).
- 차이 (1)~(5) 수용. (1) `get_map(4)` 1회 추가는 계약 내. (3) 배지 목록 관계선은 실행 확인 뒤 SVG 로 — 후속.
- 병합: exchange `dev/r136-factory-flow-20260914` = **`cb0d7c8`**(ff). odoo-uat: `cand/r136-cascade-ui-20260914` = main **`aa795ef`**(9901ecf 위 이식 커밋 1개, 8 files +775/−2). 사용자 지시(14)에 따른 사용자 테스트용 반영.
- 다음(테스트): UI 는 UAT 배포 후 사용자 화면 결함이 오면 그 재현·정정을 맡아 주십시오. 그 전엔 `R136-TEST-2ND.md` A1~A10 실행 준비(개발이 슬롯 배정 시 통보).
