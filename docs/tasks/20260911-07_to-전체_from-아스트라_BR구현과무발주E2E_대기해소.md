수신: 전체
발신: 아스트라
상태: 요청

# 대기 해소 — 기존 승인 범위 후속 배정

최신 개발 e478dd6/테스트22e27c8 회신을 접수했고, 사용자에게 부분 완료이며 총괄 결정 대기였다고 알렸다. 사용자는 이미 가상 BR 공정 시험과 전체 결함 수정을 승인했다. 아래 기술·소유 경계는 총괄이 확정하므로 재승인 대기하지 않는다.

## 개발 — BR 구현 우선

- 격리 UAT에서 BR→조립 MO→LOT→납품 추적을 구현한다. 기존 비활성 스텁을 완료로 취급하지 않는다. 실제 운영 FDW/원청에는 접속하지 않는다.
- FDW 모델 아래 임의 테이블을 만들어 운영 연결로 위장하지 않는다. 별도 내부 BR 수신 원장/입력 어댑터에 합성 BR을 넣고 source=simulation으로 식별한다. 중복키·개정·취소·수신시각·납기·MO/납품 연결을 보존한다. 외부 FDW 원본은 미래 연계 경계로 남긴다.
- 사용자 지정 2시간은 BR 수신부터 고객 도착이다. 차단 지속시간120분과 혼동하지 않는다. 가상 운송시간 가정을 명시하고 도착시각을 실제 기록하지 않은 건은 납품완료/2시간PASS로 표시하지 않는다. 정상·지연·중복·개정·취소를 시험한다.
- 이 작업에 필요한 BR/MES 모듈 소유권을 개발에 명시적으로 부여한다. 별도 adapter 모듈 방식 가능. 기존 소유 규칙 때문에 중단하지 않는다. 다른 담당 작업공간은 수정하지 않는다.
- 계획 후보 fix/planning-inventory-week=3bb4cf3은 145PASS/3SKIP 보고가 있으나 부분 부모MO 예정입고 중복 등 잔여 한계가 있어 완료로 보지 않는다. 품질 후보 fix/quality-precedence-gates=fc61e235는13PASS/1ERROR로 배포 후보 아님. 두 후보를 재구현하지 말고 필요한 연계를 보고한다.
- BR 뒤 후속 순서: 사출 PLC 품질팀 회사필터 오류(injection_worksite/models/plc_weight_sync.py:176), 불량 실물완료/원재료소비/격리/처분, 입고검사 운영정책 설정 경계. 해당 소유권도 개발에 부여하되 실제 품질기준을 발명하지 않는다. 각 단계 후보를 제출하면 테스트가 독립 시험한다.

## 테스트 — 무발주 주간 E2E 즉시 착수

- B 무발주 정상 E2E 하네스는 테스트 소유로 확정한다. 제품코드 변경 없이 부품PO를 제거한 주간 시나리오를 지금 수행한다. 개발의 BR 구현을 기다리지 않는다.
- 공유 원청계획→납입예정/ASN→무발주 입고·재고/LOT→양품생산→BOM 정산대사까지 실행한다. 원재료 수지PO는 유지한다. 입고무분개가 manual_periodic 설정 때문임을 검증표에 명시한다.
- 설정이 없는 품질판정을 자동합격하지 말고, 현행 전건IQC 병목을 별도 관찰결과로 남긴다. 전사 기준 확정 없이 일률적으로 gate를 끄지 않는다.
- 개발 e478dd6 이종검사 후보의 정상·누락·다품목·미등록LOT·보류LOT·중복·해소 권한·BOM 개정 증빙을 독립 재시험한다. 개정 증빙과 구개정부품 차단을 구분한다.
- BR 후보가 오면 수신→고객 도착2시간 판정을 합본에 추가한다. 계획/품질 후보는 정확 SHA와 설치모듈을 고정한 별도 DB에서 시험하고 실패를 숨기지 않는다.

## 아스트라

협력사 대사·회계마감 후보 검토/통합과 전체 통합 판정을 맡는다. 후보 수치만으로 회사 전체 완료를 선언하지 않는다. 새로운 사업정책 결정이 필요한 항목은 사용자에게 먼저 알린다.

별도 UAT 저장소/별도 Railway 배포 원칙 유지. 기존 제품/운영DB 무변경. 수신 후 현재 수행항목과 첫 실행 근거를 회신하고 배정 범위 작업을 계속한다.


## 회신 (테스트, 2026-09-11 16시대) — 수신·B 무발주 주간 E2E 즉시 착수

수신했습니다. **B(무발주 정상 주간 E2E) 테스트 소유 확정**을 받고 지금 착수합니다(개발 BR 대기 안 함).
- 현재 수행: 격리 DB(신규 suffix)에 **부품 PO 제거·무발주 납입** 주간 변형 구축 — 공유 원청계획→납입예정/ASN→무발주 입고·재고/LOT→양품생산→BOM 정산대사. **수지 PO 유지**, 제품코드 무변경(하네스만).
- 검증표에 **입고무분개=manual_periodic 설정 근거** 명시, **현행 전건 IQC 병목을 별도 관찰결과**로 남김(일률 gate 끄지 않음, 설정없는 품질판정 자동합격 안 함).
- 개발 이종검사 후보(e478dd6) 오면 정상/누락/다품목/미등록LOT/보류LOT/중복/해소권한/BOM개정 **독립 재시험**(개정증빙 vs 구개정부품 차단 구분).
- BR 후보 오면 수신→도착 2시간 판정 합본 추가. 계획/품질 후보는 정확 SHA·설치모듈 고정 별도 DB에서 시험, 실패 안 숨김.

첫 실행 근거는 착수 직후 회신하겠습니다. 지정 격리 clone·개별 DB·local exchange 만 사용, 원본·운영·Railway 무변경.


## 회신 (Claude 개발, 2026-09-11 16:50 KST) — BR 구현 + PLC 회사경계, 후보 `53ea739`

Opus 5 / `dev/br-assembly-e2e` / DB `cams_night_dev_main`.
지시 순서대로 **BR 우선** 후 **PLC 회사필터**까지 했습니다.

### ① BR 수신 원장 — FDW 를 흉내 내지 않았습니다

못박으신 대로 `br.db.model`(FDW VIEW) 아래에 임의 테이블을 만들지 않았습니다.
**별도 내부 원장 `br.intake`** 를 새 모듈 `escon_br_intake` 에 두었습니다.

- 합성 BR 은 **`source = 'simulation'`** 으로 식별되고, 화면에도
  「이 BR 은 합성(시험)입니다. 원청 실수신이 아닙니다.」가 뜹니다.
- 실제 연계가 붙으면 같은 원장에 `source = 'fdw'` 로 들어옵니다.
- **외부 FDW 원본은 미래 연계 경계로 남겼습니다.** `escon_br` 은 건드리지 않았습니다.

| 요구 | 구현 |
|---|---|
| 중복키 | `(company, br_no, revision)` **DB 유니크**. 파이썬 `constrains`+`search` 는 동시 수신에서 뚫리므로 DB 가 막게 하고, **검사 우회로** 제약이 실제로 걸렸는지 시험 |
| 개정 | 새 개정이 이전 것을 `superseded` 로 내림. **대체된 개정으로 수요를 또 만들지 않음**. 더 낮은 개정은 거부 |
| 취소 | 취소된 BR 은 수요가 되지 않음. **이미 납품된 건은 취소로 덮지 않음** |
| 수신시각 | 없으면 받지 않음 — **2시간의 기점**이 없으면 납기를 셀 수 없음 |
| MO 연결 | `action_create_production` — **기존 비활성 스텁이 비워 둔 자리**. 한 BR = 한 MO |

### ⚠ 2시간을 넘겨짚지 않게 두 가지를 필드로 강제했습니다

1. **도착 미기록은 PASS 가 아닙니다.** `arrived_at` 이 실제로 기록되지 않으면
   `delivery_verdict = 'not_recorded'` 이고 `state` 도 `delivered` 가 되지 않습니다.
2. **가정 운송시간과 실측을 구분**했습니다. `transit_minutes`(합성 가정) ≠
   `lead_minutes`(기록된 두 시각의 차이).

### ⚠ 120분이 두 개였습니다 — 지적하신 혼동, 실제로 제 코드에 있었습니다

`mrp_bom_scan_guard` 의 `br_delivery_deadline_minutes`(기본 120분)가 실은
**이종검사 차단 경보 한도**였는데 이름이 납기처럼 읽혔습니다.

- 차단 경보 한도 → **`scan_block_alert_minutes`, 기본 30분** (2시간 납기 안에서
  처리돼야 하므로 그보다 짧아야 합니다. 실제 값은 현장 기준이라 발명하지 않고 설정으로 열었습니다)
- BR 납기 한도 → **`br_delivery_deadline_minutes`, 기본 120분**(사용자 확정 2시간),
  `escon_br_intake` 소유

### ② PLC 품질팀 회사필터 (`plc_weight_sync.py:176`)

지목하신 자리 그대로였고, 문제가 **둘**이었습니다.

1. 회사 필터 없음 → 아무 회사 품질팀이 잡힘
2. **반복문 밖에서 한 번만 고름** → 여러 회사 기록을 함께 처리하면 전부 같은 팀.
   도메인만 고쳐도 이건 남습니다.

품질검사·품질경보 **둘 다** 그 `team_id` 를 씁니다. 회사별로 고르도록 반복문 안으로
옮겼습니다. `record.company_id` 로 짰다가 **생산기록에 회사 필드가 없는 것**을 확인하고
제조오더(`mo_id`, required)를 출처로 고쳤습니다.

**회사가 하나면 이 결함은 보이지 않아서**, 시험이 두 번째 회사와 그 회사 품질팀을
만들어 겁니다. 소스에 `search([], limit=1)` 이 남아 있지 않은 것도 고정해 되돌아가는
것을 막습니다.

### 실행 기록

```
후보 SHA : 53ea739 (제품 6825eef) / DB cams_night_dev_main
명령     : python3 tools/night_runtime.py dev update \
             --modules injection_worksite,escon_br_intake,mrp_bom_scan_guard \
             --test-tags '/injection_worksite,/escon_br_intake,/mrp_bom_scan_guard'
결과     : 0 failed, 0 error(s) of 206 tests, skip 2
skip     : TestResinPOSettlement 2건 — vendor settlement 선택모듈 부재
로그     : artifacts/full_regression_6825eef.log
```

BR 시험 11건은 `Starting` 기록으로 **실제 실행 확인**(정상·지연·도착미기록·중복·
DB유니크·개정·구개정거부·취소·납품후취소거부·1BR1MO·수신시각없음).

### 아직 아닌 것 — 넘겨짚지 말아 주십시오

- **BR → 납품 전표 자동 생성은 없습니다.** `picking_id` 를 이어 붙일 자리만 있고
  전표는 밖에서 만듭니다. **매출 근거 연결도 아직**입니다.
- **전 구간 시간기록**(수신→자재가용→조립→이종검사→포장→상차→운송→도착)은 아직입니다.
  지금 재는 것은 **수신·도착 두 점**뿐입니다. 「2시간 전 구간 검증」이 아닙니다.
- **부족 대응·경보**, **조립 불량 3%/seed `20260930` 재현**은 아직입니다.
- 계획(`3bb4cf3`)·품질(`fc61e235`) 후보는 지시대로 **재구현하지 않았습니다.**
  필요한 연계는 다음 회신에 정리하겠습니다.

다음은 「불량 실물완료/원재료소비/격리/처분」으로 넘어갑니다. **운영 가능 판정이
아닙니다.** 원본·운영·Railway 미변경, 로컬 exchange 에만 push.
