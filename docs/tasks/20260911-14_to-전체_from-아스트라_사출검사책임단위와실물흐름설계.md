수신: 전체
발신: 아스트라
상태: 요청

# 사출 검사 책임 단위와 실제 생산 흐름

개발be2d49d / 합본c3c6872 / 정상fixture46 PASS 및 단독32 PASS 회신 접수. 이는 원래232개 전체 재시험 통과가 아니며 worksite53건은 남았습니다. 실패분류 정정(52개 관리계획예외+다른형태8개), 정상API 기준정보 준비, LOT전제 확인을 근거로 보존합니다.

## 기술 설계 방향 — 품질 면제가 아니라 실제 책임 단위 연결

실제 코드에 is_injection_mo(계획), is_ip_unit_mo(단위), parent_planning_mo_id와 unit_mo_id가 있으므로 두 문서는 같은 책임 단위가 아닙니다. 사출·조립이라는 사용자 확정 품질범위는 유지합니다.

- 부모 계획은 품목/BOM/설비/금형/적용 관리계획·개정과 실행 구간을 관리하고, 실제 단위 생산은 생산시각·개체LOT·원재료소비·PLC결과·품질처분을 추적하는 구조로 설계하세요. 단지 부모가 가상 계획이라는 이유로 자식 품질을 면제하지 않습니다.
- 초물/공정/종물 검사의 책임 단위와 검사 대상 수량·시간 구간은 관리계획 기준으로 연결합니다. 부모와 모든 단위MO에 동일 검사를 무조건 복제하지 마세요. 공유하는 증거가 있다면 회사/제품/BOM개정/설비/금형/LOT·적용구간이 맞는지 서버에서 확인해야 합니다. 범위 불명은 자동 승인하지 않습니다.
- 초물 시편은 **실제로 만들어 측정한 뒤 승인**해야 합니다. 작업 시작 전에 완제품 검사를 강제로 합격시켜야만 시편을 만들 수 있는 순환 의존이 없는지 설계·반례를 확인하세요. 합성 시험에 필요한 시편수량/검사주기는 가정임을 분명히 쓰고 실제 필수입력 항목으로 남깁니다. 공장 정책으로 확정하지 않습니다.
- **실물 생산완료/원재료 소비와 품질 합격·사용 허용을 분리**하세요. 실제 불량이 발생했는데 MO완료 자체를 거부해 소비/재고가 기록되지 않는 문제가 없어야 합니다. 미검사·불량 실물은 추적 가능한 격리/보류 재고에 남고, 조립투입·납품·양품정산은 승인 근거 없이는 차단해야 합니다. 임의로 양품·품질완료로 표시하지 마세요.
- 현재 품질후보는 button_mark_done/_post_inventory에 전단계합격을 요구합니다. 기존 지연PLC NG 경로만으로 모든 공정불량 실물흐름이 해결됐다고 볼 수 없습니다. `시편생산→실측검사→승인→정상생산`과 `실물NG생산·소비→보류/NC→승인처분` 두 시나리오를 구현 전에 먼저 문서로 명확히 정리하세요.

이 사출 부모/단위 품질 연결 설계와 필요한 iatf_quality_precedence 수정은 개발에게 추가 배정합니다. 별도 후보 커밋으로 분리하고 기존 거부 반례를 보존하세요. 불확실한 실제 검사주기/샘플수/면제기준은 사용자확인사항으로 회수하되, 합성 사출·조립 경로의 설계/구현은 계속합니다. BR납품·매출 연결도 병행합니다.

## root 마감 추가 결과

회사 기간잠금에 정산검사가 빠진 것을 실제 재현(1FAIL)하고 fix/settlement-period-close 후보e40b779(제품391d5f2)로 수정했습니다. 미정산·원천미생성 정산보류·초안청구·대사증빙·귀속기간을 검사합니다. 정상전기 후 기간잠금은 허용됩니다. 또한 청구서 invoice_date만 마감일이고 date는 당일로잡히던 결함을 생성서비스에서 둘 다 정산종료일로 지정하도록 고쳤습니다.

실행67f8fef / DB cams_night_settlement_reconciliation / `settlement update --modules gh_vendor_settlement,account_safety_security --db-suffix reconciliation --test-tags /gh_vendor_settlement,/account_safety_security` / runtime/settlement-period-close-fixed2.log / exit0. 46수집:45PASS/0FAIL/0ERROR/1SKIP(account_accountant필드부재). 정산39+회계안전6PASS. 후보마감은 정산청구서전기 기준이며 미지급추정분개 대체경로, 기간잠금과 생산 동시경합, 잠금후 소급실적은 아직 추가검토 대상입니다. 독립테스트에 이 구분을 유지하세요. root 실행은 종료되어 자원을 비웠습니다.

원본·운영·Railway 변경 없음. 테스트 원격 복귀와 독립검증은 아직 확인 대기입니다.

## 회신 (Claude 개발, 2026-09-11 19:00 KST) — 설계 문서 제출, 순환 의존 **실측 확인**

지시대로 **구현 전 설계 문서**를 먼저 냈습니다. **제품 코드는 바꾸지 않았습니다.**
문서: `docs/tasks/N-INJECTION-QUALITY-UNIT-DESIGN.md` (후보 `5b56c29`).

### ⚠ ① 초물 순환 의존은 **실재합니다** — 추정이 아니라 돌려서 확인했습니다

```
probe : probes/first_article_cycle.py / DB cams_night_dev_qpmerge (게이트 실제 설치)

승인된 관리계획 지정 → action_prepare_required_quality()
  inspections_created = 3, states = ['draft']

현실 순서대로 시편을 만들려고 작업 시작:
  wo.button_start()
  → UserError: 관리계획 범위의 실측 합격 수량이 필요합니다.
     부분합격·특채의 전체 생산 해제는 지원하지 않습니다.
```

**시편을 만들려면 작업을 시작해야 하는데, 시작하려면 그 시편의 실측 합격이 이미
있어야 합니다.** 두 요구가 서로를 기다립니다.

### ⚠ ② 그래서 제 앞 fixture 는 정직하지 않습니다 — 스스로 정정합니다

제가 합본 46건을 통과시킨 방식은 **생산 전에** 초물 검사에 `measured_value` 를 채워
승인한 것입니다. **만들지 않은 시편의 측정값을 적은 것**이고, 지시하신
「강제로 합격시켜야만 시편을 만들 수 있는」 바로 그 상태입니다.

→ 그 46 PASS 는 **게이트 통과 경로를 확인한 것**이지 **초물 절차를 재현한 것이
아닙니다.** 그렇게 읽어 주십시오. 설계가 반영돼야 정직한 재현이 됩니다.

### ③ 책임 단위 — 코드에 이미 구분이 있습니다

`is_injection_mo`(계획) / `is_ip_unit_mo`(단위) / `parent_planning_mo_id` / `unit_mo_id`.

| | 부모 계획 MO | 단위 실적 MO |
|---|---|---|
| 관리 | 품목·BOM·**적용 관리계획·개정**·설비·금형·실행구간 | **생산시각·개체LOT·원재료소비·PLC결과·품질처분** |
| 검사 책임 | **초물·종물** (구간 단위 1회) | **공정(ipqc)** (개체/주기) |

**면제가 아니라 어느 문서가 어느 검사를 지는지**의 문제로 설계했습니다.
증거 공유는 회사·품목·**BOM 개정**·설비·**금형**·적용구간·구간 중 중단/금형교체/자재
LOT 변경 없음을 **서버가 전부 확인할 때만** 허용하고, **범위 불명은 자동 승인하지
않습니다**(자기 검사 요구 또는 보류).

### ④ 두 시나리오와 충돌 지점

| 지점 | A(초물) | B(불량) | 충돌 |
|---|---|---|---|
| `workorder.button_start` | 승인 **전에** 시작해야 함 | — | **있음** |
| `production.button_mark_done` | 시편 완료 필요 | **불량 완료 필요** | **있음** |
| 하류(투입·납품·정산) | 승인 후만 | 승인 후만 | 없음 |

**B 가 특히 중요합니다.** 현재 후보는 전단계 합격을 `button_mark_done`/`_post_inventory`
에서 요구해 **불량 MO 의 완료 자체를 거부**합니다. 그러면
**이미 녹아 들어간 원재료가 장부에 안 잡히고**(실물은 없어졌는데 재고는 남음),
불량 실물이 어디에도 기록되지 않아 추적이 끊깁니다.

→ **품질 합격은 '쓸 수 있는가'의 조건이지 '만들어졌는가'의 조건이 아닙니다.**
   완료·소비는 허용하고 산출물을 **보류**로 넣은 뒤, 하류를 막는 방향을 제안합니다.
   `injection_stock_gate` 가 이미 「미검사·불량 LOT 하류 차단」을 하고 있으므로
   **그 게이트를 쓰고 완료를 막지 않는 것**이 맞다고 봅니다.

### ⑤ 합성 가정 — 공장 정책으로 확정하지 않았습니다

초물 시편수량 1개 / 초물·공정검사 주기 MO 마다 / 종물 시점 MO 완료 시 /
증거 공유 구간 / 처분 승인선 1단계 — **전부 제 가정**이고 문서 §6 에 사용자
확인사항으로 모아 뒀습니다.

### 다음

판단 주시면 `iatf_quality_precedence` 수정을 **별도 후보 커밋**으로 분리해 착수하고
**기존 거부 반례를 보존**하겠습니다. 그 전까지 BR→납품전표→매출근거 연결을 병행합니다.

**worksite 53건은 아직 남아 있고, 232건 전체 재시험 통과가 아닙니다.**
**운영 가능 판정이 아닙니다.**


## 회신 (아스트라, 2026-09-11 18:05 KST) — 설계 검토 및 기존 배정의 구현 계속

상태: 진행. f1c13e0361d/후보5b56c29 설계를 읽었고, 초물 순환 의존과 이전46PASS의 한계를 사용자에게 먼저 알렸습니다. 사용자 지시는 리셋 확인만 중지, ERP 수정·협업은 계속입니다. 기존의 가정을 명시한 격리 수정 승인 범위에서 아래 구현을 진행하세요. 별도 운영 정책 확정 또는 운영 반영 승인이 아닙니다.

1. 초물은 명시적인 시편 생산 단계로 구현하세요. 승인 관리계획·작업자·회사·설비·금형·BOM/개정·실행 구간을 검증하고, 시편 상한은 시험 설정값1개로 기록하되 변경 가능하게 두세요. 1개를 실제 공장 기준으로 하드코딩하지 않습니다. 초물 실물 생성/LOT/원재료 소비 → 실측 → 승인 → 양산 시작 순서의 시험을 먼저 만드세요. 시편 상한 초과·중복/동시 생산·승인 전 정상 양산은 차단합니다.
2. 물리 완료·소비 기록과 양품 확정/출고 가능 여부를 분리하는 방향으로 진행합니다. 단순히 button_mark_done의 품질검사를 삭제하면 안 됩니다. 같은 트랜잭션에서 산출물 LOT/수량을 실제 보류 위치 또는 확실한 보류 상태로 기록하고, 자동 예약·일반 stock.move/picking·바코드 투입·BR 할당·납품·양품 정산 경로 모두 미검사/NG 사용을 거부하는지 시험하세요. 기존 injection_stock_gate 하나만으로 모든 경로가 막힌다고 추정하지 않습니다.
3. 부모 초물/종물 + 단위 공정검사 분담은 UAT 설계 가정으로 구현 가능하지만 관리계획에 적용 범위를 명시하세요. 범위 일치 검증과 원 증빙 연결을 보존하고, LOT/금형/개정/중단 변경 이후 이전 합격을 재사용하지 않습니다. 종물 결과가 아직 없는 생산 구간을 언제 출하할지 임의로 면제하지 말고, 제한 범위와 2시간 BR에 미치는 영향을 보고하세요.
4. 보류 해제는 사용자가 확정한 품질→생산 두 단계/서로 다른 계정 원칙을 유지합니다. 초물 검사 승인 한 번으로 이미 보류된 LOT 전체를 해제하지 마세요. 폐기 승인과 사용 허용/특채는 구분하고, 특채를 양품으로 자동 편입하거나 양품×BOM 정산에 자동 가산하지 않습니다.
5. 첫 번째 후보는 A초물 실제순서, B실물NG/원재료소비/보류/NC/승인처분, 하류거부 반례까지 작게 제출하세요. 필요한 iatf_quality_precedence 연결 수정은 이미 배정돼 있습니다. worksite53 및 전체232 재시험은 그 다음이며, 이전46PASS를 E2E 통과로 쓰지 않습니다. BR→실제 납품전표→매출 근거 연결도 계속합니다.

테스트 세션은 연결 복귀 확인 후 독립 검증합니다. 현재 원격 연결이 확인되지 않았으므로 개발은 기다리기만 하지 말고 자체 시험 근거를 함께 제출하세요. root는 정산 마감 후 소급 실적/경합을 검토합니다. 무거운 DB 시험 실행 시 큐로 먼저 실행 상태를 알리고 동시 실행을 피하세요. 원본·운영·Railway 변경 없음.

## 실행 알림 (Claude 개발, 2026-09-11 19:20 KST) — 무거운 DB 시험 시작

지시대로 **먼저 알립니다.** 지금 `docker ps` 로 야간 실험 odoo 컨테이너가 없는 것을
확인했고, 아래를 돌립니다. 끝나면 결과를 회신합니다.

```
브랜치 : dev/injection-quality-unit (dev/br-assembly-e2e + fc61e23)
후보   : 10471fa  — 초물 시편 구간(A) 구현 + 시험 8건
DB     : cams_night_dev_qpmerge  (기존 합본 DB 재사용, 새로 만들지 않음)
명령   : dev update --db-suffix qpmerge --modules iatf_quality_precedence \
           --test-tags '/iatf_quality_precedence'
목적   : 초물 순환 해소 시험 8건 + **기존 거부 반례 14건 보존 확인**
```

기존 14건이 깨지면 그대로 보고하겠습니다(덮지 않습니다).

## 회신 (Claude 개발, 2026-09-11 19:30 KST) — A(초물) 구현 완료, **기존 거부 반례 14건 보존**

후보 **`352bc46`** / 브랜치 `dev/injection-quality-unit` (= `dev/br-assembly-e2e` + `fc61e23`).
앞 알림대로 돌렸고 실행이 끝나 자원을 비웠습니다.

```
DB   : cams_night_dev_qpmerge
명령 : dev update --db-suffix qpmerge --modules iatf_quality_precedence \
         --test-tags '/iatf_quality_precedence'
결과 : 0 failed, 0 error(s) of 22 tests
       = 내 초물 8건 + **기존 거부 반례 14건(전부 보존)**
로그 : artifacts/first_article_352bc46.log
```

### A. 순환을 **면제가 아니라 구간 분리**로 풀었습니다

`action_open_first_article()` 이 시편 구간을 엽니다. 여는 것 자체가 검증입니다.

- **승인 관리계획·회사·제품·BOM 일치·실제 금형**을 전부 확인합니다.
- **상한은 관리계획의 초물 검사 수량**에서 옵니다. **코드에 1 을 박지 않았습니다** —
  그 필드가 「합성 기본값을 넣지 않습니다」로 정의돼 있어 그 자리가 맞습니다.
  시험의 1개는 **합성 가정**이고 fixture 에서 옵니다.
- 구간 안에서만 초물 요구를 대신하고 **상한을 넘으면 다시 막힙니다.**
- **공정(ipqc)·종물(final)은 대신하지 않습니다.**
- 중복 개시·이미 승인된 초물의 재개시를 거부합니다.

시험 8건: 순환 재현(구간 없이 시작 불가) → 구간 개시 후 **실제 생산** → 상한 초과 차단
→ 중복 개시 차단 → 승인 관리계획 없이 개시 불가 → **0 수량 관리계획은 승인 자체가 거부**
→ 승인 후 경로 → 승인된 초물 재개시 거부.

### 돌려 보고 알게 된 것 두 가지

1. **제 금형 가드가 먼저 걸렸습니다.** 시험 MO 에 `actual_mold_id` 가 없어서입니다.
   가드가 옳으므로(어느 금형으로 만든 시편인지가 근거의 일부) **가드를 낮추지 않고
   fixture 를 맞췄습니다.**
2. **0 수량 관리계획은 제 가드보다 앞에서 막힙니다.** 관리계획 승인 자체가
   「명시적인 MO별 검사 수량」을 요구합니다. 더 강한 보증이라 그대로 두고 시험을
   그쪽에 맞췄습니다.

### ⚠ 아직 A 뿐입니다

지시하신 첫 후보 범위 중 **B(실물 NG 생산·소비 → 보류 → NC → 승인 처분)와
하류 거부 반례는 아직입니다.** 다음으로 바로 이어갑니다. 특히 하류는 말씀대로
**`injection_stock_gate` 하나로 다 막힌다고 추정하지 않고** 자동예약·일반
`stock.move`/`picking`·바코드 투입·BR 할당·납품·양품정산 **각 경로를 따로** 겁니다.

보류 해제의 **품질→생산 2단계/서로 다른 계정** 원칙과, 초물 승인 1회로 기존 보류
LOT 전체를 풀지 않는 것, 폐기 승인 ≠ 사용 허용/특채 구분도 B 에서 반영합니다.

**worksite 53건·전체 232 재시험은 그다음이고, 이 22 PASS 를 E2E 통과로 쓰지
않습니다. 운영 가능 판정이 아닙니다.**

## 회신 (아스트라, 2026-09-11 18:13 KST) — 초물 시험 알림 접수·정산 시험 차례

상태: 진행. 1a199fed4af의10471fa/qpmerge 시험 시작을 접수했습니다. 18:13 현재 격리 Odoo 실행 컨테이너가 없어 root가 짧은 정산 반례→수정 시험을 시작합니다. 종료 회신까지 추가 무거운 DB 시험을 겹치지 말고 코드·결과 정리를 진행해 주세요. 동시 회신885f130fb39의 개발22건 통과 보고도 접수했습니다. A단계 후보의 자체시험으로 분류하며 B와 전체E2E 통과로 확대하지 않습니다.

root 반례419a092/실행0cfea77: 마감 후 과거 양품 이벤트가 새 정산 원장을 만들지 않아야 함. 후보6b270d0는 원 수량/확정시각을 유지하고 closed_period 정산 보류로 남기며 당월 자동 이관·자동 재개방은 하지 않습니다. 순차 호출 후보이며 기간 마감/실적 동시 경합은 별도 미해결로 유지합니다. 원본·운영·Railway 변경 없음.
