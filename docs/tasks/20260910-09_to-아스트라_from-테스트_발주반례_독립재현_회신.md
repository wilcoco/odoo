수신: 아스트라
발신: 테스트
상태: 진행 (독립 재현 완료 — 결함 재현이지 인수 통과 아님)

# 발주 단위/단가/미적재 반례 — 내 DB 독립 재현 회신 (N-RESIN-SILO-UNIT-PROBE)

배정(`20260910-08`)대로 **같은 고정 후보를 내 격리 DB에서 독립 실행**해 인수 근거를 남겼습니다.
**기존 시험 재실행과 새 독립 시나리오를 구분**했습니다.

## 새 독립 시나리오 (이번에 실행) — 발주 U1/U2/U3
- 고정 후보 **`e0696eb`**(root `b91412c` + 개발 SILO `9805e4f`) 를 내 branch `test/factory-acceptance` 에 병합(제품 로직 무변경).
- probe `2d4df91:probes/resin_silo_unit_probe.py` 반입. DB 가드에 내 격리 DB만 추가(테스트 안전가드, 제품 아님).
- 내 DB **`cams_night_test_main`**, `night_runtime.py test shell --script`, Odoo `18.0-20260609`, cron0/no-http/internal.
- **실행 전 injection_worksite schema 업그레이드 필요**(신규 `reorder_product_id` 등). 최초 `UndefinedColumn` ERROR 3건은
  스키마 미반영(준비) 문제였고 결함 아님 → `update --modules injection_worksite` 후 재실행.

**결과: 0 PASS / 3 FAIL / 0 ERROR** (로그 `artifacts/resin_probe_run3.log`, 회계전표 0·외부IO 없음·rollback 전후 동일)

| 사례 | 기대 | 내 DB 실제 | 판정 |
|---|---|---|---|
| U1 재고ton·구매ton → PO 20ton | 20 | **20000ton** (kg↔ton 미환산) | FAIL 재현 |
| U2 seller 1000/ton → 공급가 20000 | 1000/t·20000 | **1000000/ton·20000000** (1000배 이중환산) | FAIL 재현 |
| U3 2행 각10ton·10ton만 적재 → 미적재 10000kg | 10000 | `_resin_incoming_qty` **0kg** (미적재 인식 0) | FAIL 재현 |

→ **아스트라 측 결과(자기 DB, 0PASS/3FAIL)와 내 DB에서 독립적으로 일치**. 수정 소유 = **개발**.
U3 은 "미적재 10ton 을 0 으로 세는 버그"만 확정했고, 2차 발주 발생으로 확대하지 않았습니다(코드상 영향은 범위 밖).

## 기존 시험 재실행 (구분) — 정산 14건
- root `TestResinPOSettlement` 14개 = 0 failed/0 error (내 DB, 로그 `artifacts/resin_install.log`).
  정산 쪽 C4/C6/C7/C8/C9/C10 소프트웨어 검증 통과. **이건 재실행이며 위 새 시나리오와 별개.**

## 남은 미검증 (정직)
- 순수 cron 주기 스캔(C1)·9.001t 경계(C2) 는 이 probe 3사례 밖 → 별도 미검증.
- 실물 SILO 센서·PLC·공급사 송신·운영동일 환경(`18.0+e-20251008`)은 미검증(격리·합성·rollback 실행임).

## 근거 위치
- exchange `test/factory-acceptance` HEAD **`605210e`**, `docs/tasks/test-N-RESIN-SILO.md` §새 독립 시나리오,
  로그 `artifacts/resin_probe_run3.log`.

**개발이 수정 후보를 로컬 exchange 에 공유하면, 같은 probe·같은 기대값으로 재대사(0PASS→3PASS 기대)하겠습니다.**


## 회신 (아스트라, 2026-09-11 08:47 KST)

상태: 진행. 독립 재현 회신을 수신해 사용자께 먼저 알렸습니다. 기존 시험 재실행과 새 반례의 독립 재현을 구분해 근거로 보존합니다. 개발의 고정 수정 후보가 로컬 exchange에 오면 같은 시험·같은 기대값으로 재검증해 주세요. 후속 연결 검토는 로컬 main `2c3341b`의 기존 검토 문서 마지막 절에 있습니다. 원본·운영 반영은 하지 않습니다.
