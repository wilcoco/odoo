# 검증리포트 반영 수정 완료 (2026-07-16)

근거: `docs/회계개선_검증리포트.md` (판정 PASS, 지적 2건) → 후속조치 3건 전부 처리.

## 1. 수정 브랜치/커밋
- repo: wilcoco/odoo, 브랜치 `feat/pumui-approval`
- 수정 커밋: **`7f3b8df4704`** (push 완료, ls-remote 대조 일치)
- 변경 모듈: `account_kr_reports` (18.0.1.2.0) — 코드 8파일 + 문서 2건

## 2. 지적별 조치
### 지적 ① §27 청구서↔전표 잔액 대사 — **구현으로 정정**
- 사실관계 인정: 최초 완료 문서의 "체크리스트 지표로 커버" 주장은 코드 부재 상태의 오기였음.
- 구현: `kr.close.checklist.residual_mismatch` — 거래처별 AR/AP **원장 잔액**(aml amount_residual 합)
  vs **미결 청구서 잔액**(amount_residual_signed 합) 대사. 불일치 원인 = 수기 전표·미조정 결제.
  드릴다운 = 해당 거래처의 미결 AR/AP 원장 라인 (`action_residual_mismatch`).
- 검증: 정상 상태 불일치 0 → AR 수기 전표(777,777) 전기 시딩 → 해당 거래처 검출 ✓ (M1~M3)

### 지적 ② §34 마감 잠금 — **이력=구현 / 레벨설정=보류로 분리 분류**
- 구현: `kr.lock.log` + `res.company.write` 훅 — 잠금일 6종(fiscalyear/tax/sale/purchase/
  hard/period, `_fields` 존재 검사로 버전 안전) 변경 시 강화/완화/해제 자동 판별 불변 로그.
  super() 성공 후에만 기록(표준 `_validate_locks` 차단 시 이력 미생성). 읽기전용 ACL, 관리자 메뉴.
- 검증: 설정→강화 / 당김→완화 / 해제→release / 무변경 write 이력 없음(멱등) / 변경자 기록 ✓ (L1~L5)
- 잠금 *레벨* 조정(일반잠금=관리자)은 담당자 환경 설정 작업으로 보류 유지 — 완료 문서 4절에 사유 명시.

### 후속조치 2 — 배포 체크리스트 명문화
- `docs/회계개선_완료.md` 5-1절 신설: 담당자 외부 환경 배포 시 임시 base_automation 3종
  (총계 자동계산→zero_total+가드 / 고아결제 감지→체크리스트 / 승인번호 검사→unique 제약)
  비활성화 절차 + §32 인벤토리 동시 수행.

### 후속조치 3 — tests/ 승격
- 백로그로 등재 (`docs/회계리포트_개선계획.md`) — CI(run_tests.sh) 편입 시 rollback 배터리를
  odoo tests 로 전환. 이번 사이클에서는 미수행 (선택 항목).

## 3. 검증·배포
- 배터리 8/8 PASS (cams, `env.cr.rollback()`), 테스트 중 발견된 차단은 오두 표준
  `_validate_locks`(미조정 은행명세 기간 잠금 거부)로 본 구현과 무관 — 테스트 날짜를 데이터
  이전(1999)으로 조정해 통과.
- `-u account_kr_reports`: cams / test_cams1 / test_plan1 / cams_stg 4개 DB 완료, 신규 오류 0.
- 전수 대조 갱신: **구현 24 + 보류 9 = 33항목** (완료 문서 3·4절 동기화).

## 4. 재검증 포인트 제안
- §27: 복제 DB에서 AR/AP 수기 전표 시딩 → `residual_mismatch` 검출 + 드릴다운 도메인 확인
- §34: lock date 3방향 변경 → `kr.lock.log` direction 판별 + 무변경 멱등 + ACL(계정사용자 쓰기 불가)
