# 전사 강도 테스트 리포트 (2026-07-21, sim_hx_1)

급여(hr_payroll_kr) 포함 전 구성을 cams 복제본에서 고강도로 검증. 볼륨(단위실적 70건·2호기·3금형) + 적대 10종 + 급여 + 회계 + 마감 + 교차정합 7식.

## 결과 요약 — 26개 검사 전부 통과 (수정 0건 필요, 지적 4건)

### P1 볼륨 생산 ✓
- 형체력 필터(G5): 1100t 금형은 1300t 호기에만 배정 / 부하분산(G4): A품목이 6·7호기 분산
- 단위실적 70건(구분 1/3 혼합 채번), 시리얼 lot 70개, 사일로 FIFO 소진 정합(600→568kg=1.6kg×20)
- 개시 게이트 실증: 금형/원재료 미준비 시 progress 차단 → 장착·적재 후 통과

### P2 적대 배터리 10/10 ✓
재전송 ack(중복 MO 0) · 2xxx→1xxx 변환 조회 · 불량 시리얼 4종 거부 · 금형 불일치 개시 차단 ·
조립 accrual(클립만·수지/사출품 제외) · 부품정산 이중발행 차단 · 톤정산 확정 후 재집계/재발행 차단 ·
가→정 이중확정 차단(소급 80×15=1,200 정확) · 사일로 용량 초과 차단 · 마감 잠금 후 과거 전표 차단+이력

### P3 급여 ✓
- 근태 15일 집계(OT 6h 정확) → 일급제 BASIC 165만(15일×11만)·상여 분할·OT/야간수당·4대보험 노사분·NET
- 급여 확정 → 회계분개 2건 차변=대변(0.00)

### P4/P5 회계·마감 ✓
- 단계별 품의(계약금/중도금/잔금): 승인 후 계약금·잔금만 청구, 중도금 미청구 상태 유지
- 수정세금계산서 원본번호 연결, 원가 재집계 70행 — **출처 분포가 설계와 정확히 일치**(plc 44 = 정상실측 24+B 20 / standard 26 = 이상치 23+결측 3)
- 체크리스트 8지표 검출, 운영 태스크 생성

### P6 교차 정합 7식 ✓
단위MO=lot=record(70) / 재고 A10·B0·FIN20 / 부품 80×165=13,200 / 톤 1.2×140만=168만 / 사일로 568kg / 대차평형 0 / 급여분개 차대 0

## 지적 4건 (코드 수정 아님 — 설정·데이터·후속 확인)
1. **[급여-데이터] 간이세액 브래킷 시드 0건** → KRTAX=0인 채 급여 확정 가능. 실운영 전 kr.income.tax.bracket 데이터 입력 필수 + "브래킷 없으면 확정 경고" 안전망 추가 권장 (급여 세션에 전달).
2. **[급여-설정] 근무캘린더 tz** — 야간수당은 캘린더 tz 기준(모듈 로직 건전: 캘린더→사용자→Seoul 폴백 + config override). 운영 전 캘린더 tz=Asia/Seoul 확인 필수(미설정 시 야간시간 오집계).
3. **[계획-데이터] max_inventory_qty 미설정 시 풀캐퍼 상한 없음** — "생산일 풀캐퍼+안전재고 3일" 정책과 결합하면 수요 600에 계획 1,440 같은 대량 지시 발생(정책상 정상이나 상한 가드 비활성). 실값 템플릿의 최대재고 입력 필수 + 미설정 품목 경고(plan_issues) 추가 권장.
4. **[품질] IATF 검사서 draft 상태 흐름** — 시뮬에서 완료 액션이 모델별 상이. 검사 상태 전이 표준화는 IATF 세션 몫으로 전달.

## 재현 정보
- DB sim_hx_1 보존(모든 데이터·전표·payslip). 배터리 스크립트: /tmp/hx_p1.py, hx_p1f.py, hx_p2.py, hx_p3.py, hx_p3c.py, hx_p456.py, hx_p41.py
- 이전 수정분(계획 사출필드·engel LOT 제외·원가 unit_mo_id) 모두 이 강도에서 재검증됨.
