# 회계 개선 검증 리포트 (2026-07-16)

> **[재검증 추기 — 최종 판정: PASS, 지적 전량 해소]** 수정 커밋 `7f3b8df4704` (원격 일치, 호스트 diff 0) 재검증 완료.
> - §27: `kr.close.checklist.residual_mismatch` 구현 확인 — AR 수기 전표(777,777) 시딩 → 거래처 검출 + 드릴다운 도메인 ✓
> - §34: `kr.lock.log` + res.company.write 훅 — 강화/완화/해제 3방향 판별, 무변경 write 멱등, 변경자 기록, 읽기전용 ACL ✓ (레벨 설정은 보류+사유 명시로 분류 인정)
> - 회귀 6건(승인전 차단·결제 가드·승인번호 중복·대출 멱등·대차평형·체크리스트) 전부 PASS — 수정으로 깨진 것 없음
> - 신선 설치 0오류 · 복제본 -u 불변량 일치 · 신규 오류 0 · 전수 분류 33=구현24+보류9(누락 0) ✓
> - 배포 체크리스트(임시 자동화 비활성화) 완료 문서 5-1절 명문화 확인 ✓

- 대상: wilcoco/odoo `feat/pumui-approval` @ `295e69ac18b` (pumui_approval / account_kr_guard / account_kr_reports)
- 근거 계획: `docs/회계개선_검증계획.md` · 신호: `docs/회계개선_완료.md`
- 환경: 고정 커밋 워크트리(정적) + cams 복제 DB `acct_verify_1`(시나리오, 종료 후 삭제) + 빈 DB `acct_fresh_1`(설치성, 삭제). 개발 세션 자산 비접촉.

## 종합 판정: **PASS (지적 2건 — 분류 누락)**

## 게이트별 결과

### 게이트0 — 신호 신뢰성: PASS
- 주장 커밋 `295e69ac18b` == 원격 `origin/feat/pumui-approval` ✓ (ls-remote)
- 호스트 마운트 == 커밋 tip, diff 0 ✓ (마운트-원격 괴리 없음)
- 신호 파일 필수 항목(브랜치·DB목록·완료/보류 분류·DB설정 공개) 완비 ✓

### G1 — 커버리지: PASS (지적 2건)
- 완료 주장 항목(§3·4·6·7·10·12·14·15~21·23·25·26·28~31·33): 구현물 코드 존재 확인 ✓
- 보류 항목 11건: 사유 명시됨 ✓ (§22 2단계, §5·11·13 재현 필요, §32, §2·24 설정, §8, §9, §26 정밀, §31 2단계)
- **지적 ①: §27(청구서-전표 잔액 자동 대사) — 완료에도 보류에도 없음. 코드 흔적 0.**
  체크리스트 9지표에도 '잔액 불일치' 항목 없음. (품의-청구 차이 표시는 있으나 §27은 회계전표 대사라 별개)
- **지적 ②: §34(마감 잠금 완화/해제 이력) — 미분류. lock_date 관련 코드 0건.**

### G2 — 통합 정합: PASS (표본)
- 품의→청구→전기 차단이 한 체인으로 동작(§6·§7·§28 unapproved_pumui_moves 지표 동일 개념 참조) ✓
- 승인번호가 업로드 필드→unique 제약→신고 전 검증에서 같은 필드(kr_approval_number)로 관통 ✓
- 임시 자동화 이중실행 위험: 로컬 4개 DB에 base_automation 부재 확인(신호 파일 5장) — 담당자 외부 환경 배포 시 비활성화가 **배포 체크리스트로 이관 필요**(개발측도 명시, 결함 아님)

### G3 — 파괴 시나리오 배터리: 6/6 PASS (복제 DB, 전량 rollback)
| 검사 | 결과 |
|---|---|
| 미승인 품의 연계 청구서 action_post (서버 직접 호출) | 차단 ✓ "PUM-2026-0007 가 아직 승인되지 않았습니다" |
| 결제대기계정 제거 후 결제 전기 | 차단 ✓ (한글 안내) |
| 동일 승인번호 2건 저장 | UniqueViolation 차단 ✓ |
| 대출 스케줄 재생성 멱등 | 12건→12건, 지급완료 1건 보존 ✓ |
| K-GAAP 재무상태표 대차평형 | TA=TL+TE, 차이 0.00 ✓ |
| 마감 체크리스트 오염 검출 | 0원 청구서 seeded→검출 ✓ (zero_total) |

### G4 — 시스템: PASS
- 빈 DB 신선 설치(-i 3모듈): exit 0, 오류 0 ✓
- Odoo18 컨벤션: `<tree>`/`attrs=` 0건 ✓ · 신규 모델 10개 전부 ir.model.access 등재 ✓
- 복제본 -u 3모듈: 업무 불변량(전표·구매 건수/금액합) 일치, 신규 오류 0(기존 cams 드리프트 2건뿐) ✓

## 후속 조치 (개발 세션 회신 필요)
1. §27·§34 를 완료/보류 어느 쪽으로든 분류하고, 보류라면 사유를 완료 신호 문서에 추기.
   (§27은 체크리스트에 '청구서 잔액≠전표 잔액' 지표 1개 추가로 최소 구현 가능해 보임 — 판단은 개발측)
2. 담당자 외부 환경 배포 체크리스트에 "기존 임시 자동화 규칙(총계 자동계산·고아결제 감지) 비활성화" 포함.
3. (선택) 3개 모듈에 odoo 테스트 파일이 없음 — rollback 배터리를 tests/ 로 승격하면 CI(run_tests.sh)에 편입 가능.
