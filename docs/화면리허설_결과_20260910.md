# 화면 리허설 결과 — SQ 대응 모듈 (2026-09-10)

환경: 로컬 `rehearsal1` (= `cams_stg` 복제 → 8개 모듈 18.0.1.0.0 에서 최신으로 업그레이드, 운영과 같은 출발점)
코드: `wilcoco/odoo` 18.0 `17e9fac0eee` (= 운영 미러와 동일)
방법: Chrome 자동화로 실제 화면을 눌러 봄. 녹화 `sq_check_sheet_rehearsal_20260910.gif`(50프레임)

## 결론 한 줄

**핵심 흐름은 화면에서도 설계대로 동작한다.** 기준을 좁혀도 과거 판정이 안 바뀌는 것을 실제 화면에서 확인했다.
제품 결함은 **작은 것 4건**, 그보다 큰 것은 **메뉴 중복 88건**(충돌 해결의 부산물)이다.

## 확인한 흐름 (점검 양식 프레임 — 끝까지)

| 단계 | 결과 |
|---|---|
| 점검 시트 신규 → 시트명 입력 → 항목 추가 | ✅ 저장, 개정 차수 1→2 ("항목 추가: 작동유 온도") |
| 항목: 측정 / 수치 기입 / **목표±공차** 40 ± 10 / ℃ / 중요 | ✅ 하한 30 · 상한 50 **즉시 자동 계산**, 개정 3 ("목표값 - → 40.0, 공차 - → 10.0") |
| 「점검 실적 입력」 → 라인에 기준 30~50 복사, 시트 개정 차수 3 표시 | ✅ |
| 측정값 47 → 결과 **양호 자동 판정**, 완료 처리 → `CK-2026-0001` | ✅ |
| **시트 공차 10 → 5** 로 좁힘 → 하한 35 · 상한 45, 개정 4 | ✅ |
| **완료된 실적 다시 열기** | ✅ **30~50 / 47 / 양호 / 개정 3 그대로** — 소급 변조 없음 (`리허설_실적불변_20260910.png`) |
| 개정 이력 탭 | ✅ 4건, 누가·언제·무엇을 (`리허설_개정이력_20260910.png`) |

나머지 9개 화면(금형 점검·예열/온도·T/O·위험성평가·아차사고·배합일지·분쇄일지·예비부품 분류·적용표)은
신규 폼 렌더링·필수 필드·탭 구성만 확인 — **오류창 0건**.

## 발견 — 제품 (고쳐야 할 것)

| # | 무엇 | 심각도 | 근거 |
|---|---|---|---|
| P1 | **메뉴 중복 88건** — 73개 액션이 2곳 이상에 달림. SQ 메뉴 전부가 새 트리 「설비 및 금형 관리」와 옛 트리(금형/치공구·작업환경·비상 및 안전·추적성) 양쪽에 있음 | **높음** — 평가장에서 "어디로 가야 하나"가 흔들림 | `ir_ui_menu` 집계. 통합 시 "양쪽 메뉴 보존"의 부산물 |
| P2 | 예열/온도 이력 제목의 시각이 **UTC** (현지 09:55 → "00:55:19") | 중간 — 측정 시각 증빙이 9시간 어긋나 보임 | `iatf_mold/models/mold_temp_log.py:101` `fields.Datetime.to_string()` → `context_timestamp()` 로 |
| P3 | 신규 폼 제목이 모듈마다 "New" / "신규" 혼재 (iatf_traceability 는 한 모듈 안에서도 섞임) | 낮음 | `_("New")` 의 ko_KR 번역 부재 — 로그에 `no translation for language ko_KR` |
| P4 | 부품 적용표 신규 제목이 "→" 만 표시 | 낮음 | `_compute_display_name` 이 빈 값에서 화살표만 남김 |
| P5 | 점검 항목 목록의 「기준 방식」열이 좁아 "정성(기준 글…" 로 잘림 | 낮음 | 열 폭 |
| P6 | 대시보드 브레드크럼이 `iatf.dashboard,3` (모델명,ID 그대로) | 낮음 | `iatf.dashboard` 에 display_name 없음 |

## 발견 — 환경 (제품 결함 아님, 그러나 기록)

| # | 무엇 | 원인 | 조치 |
|---|---|---|---|
| E1 | 로그인 폼이 화면에 안 보임, 앱 아이콘 전부 기본 큐브 | DB 만 복제하고 **filestore 를 안 복사**해 프론트 JS 번들이 500 | 번들 레코드 삭제 → 재생성. **운영은 해당 없음** |
| E2 | 점검 시트 목록·폼에서 "이런! 문제가 발생했습니다" | 상시 컨테이너가 **8/19 자 낡은 `./addons_custom` 복사본**을 마운트 → 새 DB 와 모델 불일치 (`Missing model iatf.blend.log`) | 마운트를 `_wt-18.0` 워크트리로 교체 (`chore/mount-iatf-plugins` 741dbd3). **이전 로컬 검증이 낡은 코드로 돌았을 가능성은 없음** — 검증은 항상 `-v _wt-*/addons_custom` 로 별도 컨테이너에서 돌렸다 |
| E3 | "데이터베이스가 만료되었습니다" 배너 | `cams_stg` 가 체험판(7/19 만료) | 운영 구독 코드를 로컬에 넣으면 사라짐 (원도영 님에게 요청) |

> E2 는 리허설이 아니었으면 계속 몰랐을 것이다. 상시 컨테이너로 화면을 볼 때마다 낡은 코드를 보고 있었다.

## 확인 안 된 것 (덤프 뒤에)

- 707품목·524BOM 규모에서의 화면 속도
- 실제 설비·금형 대장에 연결된 상태의 흐름 (지금은 대장이 비어 있어 설비 연결 없이 진행)
- 담당자 관점의 용어·순서 (②담당자 UAT)

## 다음

1. **P1 메뉴 중복** — 옛 트리를 걷어내는 PR (iatf_menu). 어느 트리를 남길지는 결정 필요: 새 「설비 및 금형 관리」 하나로 모으는 것을 권함
2. P2 시각 — 한 줄 수정, 테스트 포함
3. P3·P4·P5·P6 — 한 PR 로 묶어 처리
