# 회계 개선 완료 신호 (오두 회계 사용 리포트 §2~§34 대응)

작성: 2026-07-16 · 작성 세션: IATF/SQ 세션 · 상세 계획/현황: `docs/회계리포트_개선계획.md`

## 1. 구현 브랜치
- **repo: wilcoco/odoo (github.com/wilcoco/odoo), 브랜치: `feat/pumui-approval`** (base 18.0)
- 최종 커밋: `295e69ac18b` (push 완료 확인: origin/feat/pumui-approval 동기화됨)
- 커밋 5건: pumui_approval(ffd24451) / account_kr_guard(b003ba10) /
  account_kr_reports P3(a3223790) / 자금현황·품의집계(8b5c27c5) / P4 대출·재무제표·템플릿(295e69ac)
- 모듈 경로: `addons_custom/pumui_approval`, `addons_custom/account_kr_guard`,
  `addons_custom/account_kr_reports` (의존: `iatf_approval` 결재엔진 — 같은 repo 18.0/feat 브랜치에 존재)

## 2. 개발·테스트에 사용한 DB (검증 세션은 이 DB들을 피해서 복제할 것)
| DB | 용도 |
|---|---|
| `cams` | 주 개발·검증 배터리 실행 (모든 검증은 rollback으로 수행했으나 -u 는 반영됨) |
| `test_cams1` | 회계 활동 있는 테스트 DB — 모듈 배포 + enterprise 설치됨 |
| `test_plan1` | 회계 활동 있는 테스트 DB — 모듈 배포 + enterprise 설치됨 |
| `cams_stg` | 스테이징 — 모듈 배포 + enterprise 설치됨 |
| (불가침) `cams_demo` | 테스터 전용 — 이번 작업에서 건드리지 않음. 검증 세션도 사용 금지 |
| (타 세션) `cams_sim`, `cams_sim3`, `cams_stg2`, `cams_stg3`, `cams_test` | 시뮬/타 워크스트림 소유 — 회피 권장 |

## 3. 구현 완료 항목 (§ 번호)
전수 대조: 리포트 §2~§34 총 33항목 = 구현 24 + 보류 9 (아래 4절, 전부 사유 명시) — 누락 없음.
(검증리포트 지적 2건 반영 후 갱신: §27 구현 추가, §34 이력=구현/레벨설정=보류로 분리)
- **§6·§7 품의서 시스템**: `pumui_approval` — 다단계 결재(상신/승인/반려+사유 필수), 계약금/중도금/잔금
  단계별 청구서 생성 위저드, 품의-청구-결제 금액 대사, **승인 전 전기 서버측 차단**(우회 방어)
- **§3** 사업자번호 중복·유사 상호 경고 / **§4·§14** 청구서 결제요약(지급액/잔액 표시)
- **§12** 결제대기(outstanding) 계정 미설정 시 결제 전기 차단 가드
- **§28** 마감 전 체크리스트 (9지표+부가세 3지표, 전부 드릴다운)
- **§27** 청구서↔원장 잔액 대사 — 체크리스트 `residual_mismatch` 지표: 거래처별 AR/AP 원장 잔액 vs
  미결 청구서 잔액 합 대사(수기 전표·미조정 결제 검출) + 해당 거래처 미결 원장 라인 드릴다운.
  (검증 지적 ① 반영 — 최초 완료 문서의 주장과 달리 미구현이었음을 인정하고 실제 구현)
- **§34** 마감 잠금 변경 이력 — `kr.lock.log`: res.company 잠금일 6종 write 시 강화/완화/해제
  자동 판별·불변 로그(읽기전용, 변경자·전후값), 관리자 메뉴. 잠금 레벨 설정 자체는 표준 설정 사용.
  (검증 지적 ② 반영 — 이력은 코드로 구현, 레벨 조정 가이드는 담당자 설정으로 잔존)
- **§19·§20** 거래처원장·계정별원장 바로가기 + 일계표/월계표(계정×일/월 피벗)
- **§10** 오늘 전표·일자별 조회 / **§30** 수금/지급 예정표(만기·잔액)
- **§15~§17** 과세/영세/면세 자동추정, 증빙종류, **승인번호 unique 중복차단**, 수정분-원본번호 연결
- **§18** 신고 전 검증(승인번호 누락/사업자번호 없는 거래처분/원본 없는 수정분)
- **§21** 자금현황(은행·현금 저널별 잔액, 당일 입출금, 대기 결제)
- **§29** 매입매출장 + 매출/매입처별 합계표 + **K-GAAP 재무제표**(재무상태표/손익계산서,
  계정유형 매핑 마스터 — 조정 가능, 대차평형 검증 통과)
- **§25** 대출관리(약정 조건 입력형: 매월/만기일시/1회/사용자지정 스케줄, 잔액 자동)
- **§15/§23 (역발상)** 업로드 표준 템플릿 다운로드(세금계산서 매출/매입·은행내역 CSV,
  오두 native import 헤더와 정합, UTF-8 BOM)
- **§26** 고정자산: enterprise `accountant`+`account_asset` 설치로 상각·자산대장 확보 (표준 모듈 사용)
- **§31** 프로젝트별 품의·청구 집계(1단계 피벗) / **§33** 삭제 대신 취소+사유 패턴(품의 반려사유 필수)

검증: pumui 12/12 · guard 7/7 · reports P3 7/7 · P4 20/20 (cams, `env.cr.rollback()` 배터리) —
대차평형·멱등(스케줄 재생성 시 지급완료 회차 보존) 포함.

## 4. 보류/다음 단계 항목 (§ 번호 + 사유)
| 항목 | 사유 |
|---|---|
| §22 은행 거래내역 자동매칭 2단계 | 실제 IBK/전북 다운로드 양식 샘플 필요 (템플릿 업로드 1단계는 완료) |
| §5 총계 0원 / §11 외화 오류 / §13 결제취소 꼬임 | 담당자 실환경에서만 재현 가능 — 재현 시나리오 확보 후 진단 |
| §32 자동화 규칙 인벤토리 | base_automation 규칙이 담당자 외부 환경에만 존재 (아래 5 참조) |
| §2·§24 초기 설정·계정 체계 | 담당자와 설정 세션 필요 (코드 아님) |
| §8 사용자 권한 매핑 | 실제 사용자 목록·역할 확정 필요 |
| §9 그리드 전표입력 | 별도 UI 판단 필요 (enterprise 설치로 표준 대안 확보, 효용 재평가) |
| §34 잠금 *레벨* 조정 | 변경 이력은 구현 완료(위 3절). 레벨 설정(일반잠금=관리자)만 담당자 환경 설정 작업 |
| §26 한국화 정밀(자본적지출 가산 검증) | enterprise 자산 사용 데이터 축적 후 |
| §31 프로젝트 손익 2단계(결제·세금계산서까지 연결) | 1단계 피벗 사용 피드백 후 |

## 5. 코드가 아니라 DB 설정으로 처리한 것
- **enterprise `accountant` + `account_asset` 설치** — test_cams1 / test_plan1 / cams_stg 3개 DB.
  (enterprise 소스는 절대 규칙에 따라 git 미포함 — 검증 세션이 복제 DB를 만들면 모듈 설치 상태는
  복제되지만, 새 DB에 새로 설치하려면 `/mnt/enterprise` addons-path 필요)
- **임시 자동화 규칙 관련**: 로컬 4개 DB(cams/test_cams1/test_plan1/cams_stg) 모두
  `base_automation` 테이블 자체가 없음을 확인 — 리포트에 언급된 임시 규칙(청구서 총계 자동계산,
  고아결제 감지 등)은 **회계 담당자의 외부 환경에만 존재**. 따라서 로컬에서 비활성화할 대상이 없었고,
  코드 승격분(승인 전 전기차단·결제계정 가드·체크리스트 검출)을 그 환경에 배포하는 시점에
  기존 임시 규칙 비활성화가 **배포 체크리스트 항목으로 필요** (미수행 상태임을 명시).
- 그 외 Live View 수정·계정 추가 등 DB-only 변경 없음. 모든 기능은 코드(브랜치)로 재현 가능.

## 5-1. 담당자 외부 환경 배포 체크리스트 (검증 후속조치 2 반영)
1. `-u pumui_approval,account_kr_guard,account_kr_reports` (iatf_approval 동반)
2. **기존 임시 base_automation 규칙 비활성화**: 청구서 총계 자동계산 → §28 zero_total 지표+
   전기 가드가 대체 / 고아결제 감지 → 체크리스트 지표가 대체 / 승인번호 중복 검사 →
   SQL unique 제약이 대체. (규칙 목록은 §32 인벤토리 추출과 동시 수행)
3. 잠금 레벨 설정(§34: 일반잠금=관리자) + 계정 정리(§2·§24) 설정 세션.

## 6. 검증 세션 참고
- 재현 절차: 복제 DB에 `-u pumui_approval,account_kr_guard,account_kr_reports`
  (의존 iatf_approval 자동 설치). odoo.conf addons-path 순서: enterprise → custom → core.
- cams의 gh_fcaio/morning_rc 로드 에러, cams_stg의 car.info.quick.wizard 에러는
  타 개발자 모듈의 기존 이슈로 본 작업과 무관.
